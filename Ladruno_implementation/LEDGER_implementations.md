---
title: Ledger — our own implementations
project: Ladruno
tags:
  - ledger
  - features
  - implementation
---

# Ledger — Ladruno implementations (new code we authored)

Every brand-new file / feature the Ladruno fork adds on top of vanilla
OpenSees: what it is, its class tag, where it lives, and the PR(s) that
shipped it. This is the counterpart to [[LEDGER_vanilla_files]] (which tracks
edits to *pre-existing* upstream files).

## Conventions

- **One row per feature**, not per file. List the owning files in the *Files* cell.
- *Status*: `shipped` (merged to `ladruno`), `draft` (plan/WIP), or `frozen`.
- **The banner feature list mirrors this ledger.** Every `shipped` feature
  should have a matching line in `Ladruno_scripts/banner_features.txt`. After
  editing that file run `python Ladruno_scripts/patch_banner.py` and rebuild.
- Class tags live in `SRC/classTags.h`; keep them recorded here so we never
  collide on a tag.
- **Reserved tags:** a tag pre-allocated by an ADR but not yet implemented is
  marked `— RESERVED, not yet built`; it is recorded here to prevent collisions
  but does **not** appear in `SRC/classTags.h` until the implementation merges.
  Class-tag bands are per-registry (Element / nDMaterial / uniaxial / Integrator /
  Recorder each have their own 33000-space), so the same number in two registries
  is not a collision.
- When a forward-looking plan in this folder ships, move it to
  `Ladruno_internal/implemented_<name>.md` and add/flip its row here.

## Ledger

| Feature | Kind | Class tag | Files | Status | PR(s) |
|---|---|---|---|---|---|
| **LadrunoTie — kinematic mesh-tie generator** ([[62_ladruno_kinematic_mesh_tie_adr]]) — setup-time generator (NO enforcement code) for a non-conforming explicit mesh-tie. Pairs each slave node to its nearest master facet (reusing the ADR-39 bucket-sort broad phase + `LadrunoContactProjection` closest-point projection + shape weights), then emits one ordinary `EQ_Constraint` per slave node per translational DOF (`u_s = Σ N_i u_{m,i}`); the user runs `constraints LadrunoProjection` (or any EQ-aware handler) to enforce it — exact, dt_cr-neutral, momentum-clean, no fictitious mass. The constructive successor to the shelved penalty route ([[61_ladruno_contact_bipenalty_adr]]). Named refusals: BLOCKER-1 (slave/master not node-disjoint, or a slave listed twice ⇒ MP-chain/double-constraint), BLOCKER-2 (a tied slave DOF carries no lumped mass), OQ-3 (slave off the master manifold ⇒ require conforming-at-interface geometry, no IC snapping). **P1** = solid–solid, node-to-segment collocation (nps 3/4): `LadrunoTie -slaveNodes ns s.. -masterFacets nps nf m.. [-dof nd d..] [-tol frac]`. **P2 (integral mortar, `-mortar`)** = tie the slave SURFACE to the master surface in the weak sense: assemble global mortar `D_IJ=∫N_I^s N_J^s dΓ`, `M_IK=∫N_I^s φ_K^m dΓ` over the clipped overlap (reusing `LadrunoMortarKernel::integratePair` verbatim), condense once at setup `u_s = P u_m`, `P = D⁻¹M`, emit per-slave rows. **KEY: pre-inverting D globally makes every P row tie to MASTER nodes only ⇒ no MP-chains ⇒ NO handler change + NO kernel change** (the ADR's "needs chain support" premise was avoidable). Variationally consistent / optimal for genuinely non-matching interfaces (no coincident nodes). `LadrunoTie -mortar -slaveFacets npsS nf s.. -masterFacets npsM nf m.. [-dof..] [-tol..] [-outward ox oy oz]`. Mortar guards: coverage-ratio (cover/fullCov via self-clip — refuse a slave protruding past the master), conforming-gap (g̃/area), post-solve partition-of-unity (catch ill-conditioned D). Shells (P3) deferred. | Constraint generator | — (no new tag; emits `EQ_Constraint`, enforced by handler 33001) | `SRC/domain/constraints/LadrunoTie.{h,cpp}`, `SRC/interpreter/{OpenSeesCommands.h,PythonWrapper.cpp,TclWrapper.cpp}`, `tests/test_ladrunoTie_{patch,mortar}.py`, `Ladruno_implementation/kinematic_tie_validation/proto_p{0,1,2}_*.py` | shipped (P1 + P2 mortar) | [#454](https://github.com/nmorabowen/OpenSees/pull/454), [#455](https://github.com/nmorabowen/OpenSees/pull/455) |
| **Finite-sliding NTS contact re-emission** ([[60_ladruno_finite_sliding_reemission_adr]]) — deformed-config NTS broad-phase re-emit so a slave that slides off its reference-config candidate segments re-pairs (the silent pass-through fix); migration-metric trigger in `Domain::commit()`→`domainChange()`, friction re-engages on a fresh slot at a crossing. **P0 = OpenSees-free metric header + oracle (#443), then the wiring (#444): Contact `-reemit`/`-resortFrac`/`-resortEvery`, engine `needsResort`/anchor+trigger store, the guarded `Domain::commit()` line, handler anchor registration. OFF ⇒ byte-identical.** P1 (#446) flips the handler broad-phase feed to the deformed config (+ clip-off + self-contact filter) ⇒ re-emit now EFFECTIVE (deformed candidate set tracks the slid slave). Scope = NTS only (mortar is brute-force). 5-lens pre-code gate. | Engine/handler behavior | — (handler 33002) | `SRC/domain/contact/LadrunoContactReemit.h`, `SRC/domain/contact/LadrunoContactDomain.{h,cpp}`, `SRC/analysis/handler/LadrunoContactHandler.cpp`, `SRC/interpreter/OpenSeesOutputCommands.cpp`, `SRC/domain/domain/Domain.cpp` (vanilla), `tests/test_adr60_reemit_p1.py`, `tests/test_adr60_reemit_p2_friction.py`, `Ladruno_implementation/contact_prototypes/proto_reemit{_selfcheck.cpp,.py}` | draft (P0+P1 effective re-emit + P2 friction validated; P1b persistent orientDir for curved masters next) | [#443](https://github.com/nmorabowen/OpenSees/pull/443), [#444](https://github.com/nmorabowen/OpenSees/pull/444), [#446](https://github.com/nmorabowen/OpenSees/pull/446), [#447](https://github.com/nmorabowen/OpenSees/pull/447) |
| **Finite-sliding NTS contact re-emission** ([[60_ladruno_finite_sliding_reemission_adr]]) — deformed-config NTS broad-phase re-emit so a slave that slides off its reference-config candidate segments re-pairs (the silent pass-through fix); migration-metric trigger in `Domain::commit()`→`domainChange()`, friction re-engages on a fresh slot at a crossing. **P0 = OpenSees-free metric header + oracle (#443), then the wiring (#444): Contact `-reemit`/`-resortFrac`/`-resortEvery`, engine `needsResort`/anchor+trigger store, the guarded `Domain::commit()` line, handler anchor registration. OFF ⇒ byte-identical.** P1 (#446) flips the handler broad-phase feed to the deformed config (+ clip-off + self-contact filter) ⇒ re-emit now EFFECTIVE (deformed candidate set tracks the slid slave). Scope = NTS only (mortar is brute-force). 5-lens pre-code gate. **Post-code remediation backlog R1–R8 (5-lens, core verified sound vs source): R2 deformation-invariant reference band (#448); R1 master-`mTags` membership fingerprint (drop friction + refuse-arm on a re-mesh/removal reorder, forward guard — surfaces immutable today) + R6 `Subdomain`/partitioned-host serial-only refusal (#451); R4 persistent per-`contactTag` `Trigger` (floor/hysteresis no longer vestigial) + R7 `-resortEvery` as the forced BSORT cycle-cadence (`Trigger.forceEvery`, floor fixed at 10) (#452); R5 verified empirically already-handled (slide-off departs cleanly — force→0, free fall, no phantom traction; regression test only) + R8 documented intentionally-not-surfaced (noisy 1/99-percentile signal; clip-off is the real safety) (#453). Backlog CLOSED except R3 (curved orientDir → dedicated ADR-47 #4a nodal-normal smoothing, deferred by decision; `-outward` interim).** | Engine/handler behavior | — (handler 33002) | `SRC/domain/contact/LadrunoContactReemit.h`, `SRC/domain/contact/LadrunoContactDomain.{h,cpp}`, `SRC/analysis/handler/LadrunoContactHandler.cpp`, `SRC/interpreter/OpenSeesOutputCommands.cpp`, `SRC/domain/domain/Domain.cpp` (vanilla), `tests/test_adr60_reemit_p1.py`, `tests/test_adr60_reemit_p2_friction.py`, `tests/test_adr60_reemit_p3_cadence.py`, `tests/test_adr60_reemit_p4_slideoff.py`, `Ladruno_implementation/contact_prototypes/proto_reemit{_selfcheck.cpp,.py}` | draft (P0+P1 effective re-emit + P2 friction validated; R2+R1+R6 remediation shipped; R3 curved orientDir next) | [#443](https://github.com/nmorabowen/OpenSees/pull/443), [#444](https://github.com/nmorabowen/OpenSees/pull/444), [#446](https://github.com/nmorabowen/OpenSees/pull/446), [#447](https://github.com/nmorabowen/OpenSees/pull/447), [#448](https://github.com/nmorabowen/OpenSees/pull/448), [#451](https://github.com/nmorabowen/OpenSees/pull/451), [#452](https://github.com/nmorabowen/OpenSees/pull/452), [#453](https://github.com/nmorabowen/OpenSees/pull/453) |
| **OpenSeesPyMP** — `import openseesmp`, MPI-parallel Python module (Patch 9) | Interpreter | — | `SRC/interpreter/PythonMPIModule.cpp` (+ vanilla edits, see [[LEDGER_vanilla_files]]) | shipped | [#1](https://github.com/nmorabowen/OpenSees/pull/1) |
| **EnergyBalance recorder** — per-region energy sidecar; energy math now lifted into the shared `EnergyBalanceKernel.h` (consumed by both this recorder and the Ladruno recorder's `EnergyBalanceSource`, ADR D8) | Recorder | 33000 | `SRC/recorder/EnergyBalanceRecorder.{cpp,h}`, `SRC/recorder/EnergyBalanceKernel.h`, `tests/test_energyBalanceRecorder.py` | shipped — Zone-A T1 (element-mass KE aliasing-fix regression + -time column layout) | [#2](https://github.com/nmorabowen/OpenSees/pull/2), [#24](https://github.com/nmorabowen/OpenSees/pull/24) |
| **ExplicitBathe — unified Noh–Bathe explicit integrator** (`ExplicitBathe` + orthogonal `-lnvd` / `-sms` / `-consistent` flags; hardened `dt_cr`, shared TU, DSYGV+β fix, `-lump`, MPI reduce). **ADR-52 W1-E2 #419 COLLAPSE: this ONE class now hosts the whole former family** — `ExplicitBatheLNVD` (33002) → `-lnvd <alpha>`, `ExplicitBatheSMS` (33009) → `-sms <dt>`, `ExplicitBatheSMSConsistent` (33010) → `-sms -consistent`, `ExplicitBatheLNVDSMS` (33011) → `-lnvd -sms`, `ExplicitBatheLNVDSMSConsistent` (33012) → `-lnvd -sms -consistent`. The five `.cpp/.h` are DELETED; the five command names + tags are **deprecated-but-recognized aliases** (flag combo derives the classTag via `tagForFlags`; both brokers route the six tags → `ExplicitBathe::makeForBroker`; `recvSelf` fills the param superset). Numerically byte-identical per combo. | Integrator | **33000** (+ deprecated aliases 33002/09/10/11/12) | `SRC/analysis/integrator/ExplicitBathe.{cpp,h}` (everything flag-gated here), `tests/{test_explicitBathe_integrator,test_explicitBatheLNVD_integrator,test_explicitBatheSMS_integrator,test_explicitBatheLNVDSMS_integrator,test_adr52_w1e2_explicitbathe_collapse}.py` | shipped — Zone-A dynamics battery (order/stability/crosscheck/spectral/wave/rigid-body/dt_cr/lump/cflAbort; LNVD relaxation→static) + the per-alias SMS/consistent batteries (still run under the deprecated names) + W1-E2 collapse byte-identity & LNVD×consistent cross-validation | [#2](https://github.com/nmorabowen/OpenSees/pull/2), [#9](https://github.com/nmorabowen/OpenSees/pull/9), [#324](https://github.com/nmorabowen/OpenSees/pull/324), [#419](https://github.com/nmorabowen/OpenSees/pull/419) |
| **Queryable critical time step** — query/recompute `dt_cr` (tangent/periodic); fixes mass-aliasing bug | Integrator API | — | (within explicit integrator TU) | shipped | [#3](https://github.com/nmorabowen/OpenSees/pull/3) |
| **HRZ mass-conserving lumping** ([[35_ladruno_hrz_lumped_mass_adr]]) — `-lump hrz` on all 3 explicit integrators (CDL/ExplicitBathe/LNVD); per-direction `α_d=m_d/S_d` scale, rotational DOFs by mean-α, positive + mass-conserving + rotation-aware; reduces to row-sum on regular elements; u-p/shell non-positive DOFs pass through; g++-verified OpenSees-free kernel | Integrator util | — | `SRC/analysis/integrator/LadrunoMassLumping.h` (`Ladruno::hrzLump`), `CTSLumping::HRZ` branch in `CriticalTimeStep.{h,cpp}`, parsers in `CentralDifferenceLadruno.cpp`/`ExplicitBathe.cpp`/`ExplicitBatheLNVD.cpp`, `tests/test_hrz_lumped_mass.py`, `tests/_hrz_verify/hrz_standalone.cpp` | shipped — Zone-A 6/6 + standalone | [#295](https://github.com/nmorabowen/OpenSees/pull/295) |
| **CriticalTimeStep per-element kernel** — extracted `lumpElementMass` / `elementLambdaMax` / `elementCriticalDt` (self-report-aware) so the dt_cr query AND the SMS pass share one D8-safe lump+eigensolve | Integrator util | — | `SRC/analysis/integrator/CriticalTimeStep.{h,cpp}` | shipped — behavior-preserving (18/18 regression) | [#295](https://github.com/nmorabowen/OpenSees/pull/295) |
| **CentralDifferenceSMS** ([[36_ladruno_selective_mass_scaling_adr]]) — selective (DT2MS-style) mass scaling on CDL: inflate sub-target elements' mass (`s_e=(dtTarget/dt_e)²`) by additive nodal injection so the bulk timestep governs; re-baseline + dtor-restore; `-cflAbort`/`-recompute` rejected; self-report/mismatch skipped; element-mass cap; v1 sequential + constraint/damping/MPI warnings | Integrator | 33007 | `SRC/analysis/integrator/CentralDifferenceSMS.{cpp,h}`, `LadrunoMassScaling.h`, CDL protected ctor, `tests/test_centralDifferenceSMS_integrator.py` | shipped — Zone-A 4/4 (incl. tiny-element stability win + dtor-restore); 3 milestone reviews folded | [#295](https://github.com/nmorabowen/OpenSees/pull/295) |
| **CentralDifferenceSMSConsistent** ([[38_ladruno_consistent_mass_scaling_adr]]) — CONSISTENT (Olovsson) mass scaling, the v2 of CentralDifferenceSMS: injects the centroidal scaling mass `M_bar_e=β[diag(m_a)−m mᵀ/M_e]` (row sums 0 ⇒ rigid translation gets no added inertia ⇒ **f1 preserved** while the relative modes are loaded; same betaK-damped β as v1). `M̃=M_lump+ΣM_bar` is SPD-but-non-diagonal ⇒ new base no-op `refineAccel()` hook (lumped path byte-identical) overridden to solve `M̃ a=r` by matrix-free Jacobi(lumped-diag)-preconditioned CG (3–21 iters; factored DiagonalSOE gives 1/mass as both r-recovery + preconditioner). NO Node mutation (no inject/restore). Reuses `buildMassScalingConsistent` (shared dt_e/self-report/betaK/MP-slave/cap). Oracle-first | Integrator | 33008 | `SRC/analysis/integrator/CentralDifferenceSMSConsistent.{cpp,h}`, `LadrunoMassScaling.h` (`buildMassScalingConsistent`/`consistentMatVec`/`consistentPCG`), CDL `refineAccel` hook, `tests/test_centralDifferenceSMSConsistent_integrator.py`, oracle `Ladruno_implementation/mass_scaling_consistent/` | shipped — Zone-A 7/7 (supra-stable necessity, **f1 −0.17% vs lumped −53%** via FFT, no-nodal-mass vs lumped control, reduce-to-base bit-identical, PCG≤30, constraint-touching-excluded OOB regression); numpy oracle proves f1 drift <0.3% vs 54–83% lumped; 3-lens adversarial review folded (1 critical: Transformation-handler FE-id OOB → master+slave MP exclusion) | [#320](https://github.com/nmorabowen/OpenSees/pull/320) |
| **ExplicitBatheSMS** ⟨**W1-E2 #419: COLLAPSED → `ExplicitBathe -sms`; `.cpp/.h` DELETED, tag 33009 a deprecated alias**⟩ ([[36_ladruno_selective_mass_scaling_adr]]) — LUMPED selective mass scaling on the Noh-Bathe `ExplicitBathe` (sibling of CentralDifferenceSMS): subclass via a new protected `ExplicitBathe` classTag ctor; `domainChanged` injects additive nodal mass via the shared `Ladruno::buildMassScaling` + restore lifecycle. ExplicitBathe assembles only the mass on the RHS, so the nodal injection is seen with NO solve-path change (the ADR-36 "trivial follow-up"). Command takes the Noh-Bathe `$p` first | Integrator | 33009 | `SRC/analysis/integrator/ExplicitBatheSMS.{cpp,h}`, `LadrunoMassScaling.h`, `ExplicitBathe` protected ctor, `tests/test_explicitBatheSMS_integrator.py` | shipped — Zone-A (supra-stable necessity, nodal-mass injection); base ExplicitBathe regression byte-identical | [#324](https://github.com/nmorabowen/OpenSees/pull/324) |
| **ExplicitBatheSMSConsistent** ⟨**W1-E2 #419: COLLAPSED → `ExplicitBathe -sms -consistent`; `.cpp/.h` DELETED, tag 33010 a deprecated alias**⟩ ([[38_ladruno_consistent_mass_scaling_adr]]) — CONSISTENT (Olovsson) mass scaling on the Noh-Bathe `ExplicitBathe` (sibling of CentralDifferenceSMSConsistent): centroidal `M̄=β[diag(m)−mmᵀ/Mₑ]` + matrix-free PCG (`consistentPCG`) refined at BOTH Noh-Bathe sub-step solves via a new no-op `refineAccel()` hook on `ExplicitBathe` (default no-op ⇒ ExplicitBathe AND lumped ExplicitBatheSMS byte-identical). Reuses `buildMassScalingConsistent` (slave+master MP exclusion). f1 −0.17% vs lumped −53% | Integrator | 33010 | `SRC/analysis/integrator/ExplicitBatheSMSConsistent.{cpp,h}`, `LadrunoMassScaling.h`, `ExplicitBathe` `refineAccel` hook, `tests/test_explicitBatheSMS_integrator.py` | shipped — Zone-A (supra-stable + no-nodal-mass, f1-preservation vs lumped FFT, reduce-to-base bit-identical, PCG≤30 at both sub-steps) | [#324](https://github.com/nmorabowen/OpenSees/pull/324) |
| **ExplicitBatheLNVDSMS** ⟨**W1-E2 #419: COLLAPSED → `ExplicitBathe -lnvd -sms`; `.cpp/.h` DELETED, tag 33011 a deprecated alias**⟩ ([[36_ladruno_selective_mass_scaling_adr]]) — LUMPED selective mass scaling on the Noh-Bathe + FLAC-LNVD `ExplicitBatheLNVD` (sibling of ExplicitBatheSMS): protected `ExplicitBatheLNVD` classTag ctor; `domainChanged` injects nodal mass via `Ladruno::buildMassScaling` + restore. No solve hook (mass on RHS). FLAC `alpha` separate from sizing. Command takes `$p $alpha` first | Integrator | 33011 | `SRC/analysis/integrator/ExplicitBatheLNVDSMS.{cpp,h}`, `LadrunoMassScaling.h`, `ExplicitBatheLNVD` protected ctor, `tests/test_explicitBatheLNVDSMS_integrator.py` | shipped — Zone-A (supra-stable, nodal-mass injection); base LNVD regression byte-identical | [#324](https://github.com/nmorabowen/OpenSees/pull/324) |
| **ExplicitBatheLNVDSMSConsistent** ⟨**W1-E2 #419: COLLAPSED → `ExplicitBathe -lnvd -sms -consistent`; `.cpp/.h` DELETED, tag 33012 a deprecated alias**⟩ ([[38_ladruno_consistent_mass_scaling_adr]]) — CONSISTENT (Olovsson) mass scaling on `ExplicitBatheLNVD` (sibling of ExplicitBatheSMSConsistent): centroidal `M̄` + matrix-free PCG at BOTH Noh-Bathe sub-step solves via a new no-op `refineAccel()` hook on `ExplicitBatheLNVD` (default no-op ⇒ LNVD + lumped LNVDSMS byte-identical). Reuses `buildMassScalingConsistent` (slave+master MP exclusion). f1 −0.17% vs lumped −53% | Integrator | 33012 | `SRC/analysis/integrator/ExplicitBatheLNVDSMSConsistent.{cpp,h}`, `LadrunoMassScaling.h`, `ExplicitBatheLNVD` `refineAccel` hook, `tests/test_explicitBatheLNVDSMS_integrator.py` | shipped — Zone-A (supra-stable + no-nodal-mass, f1-preservation FFT, reduce-to-base bit-identical, PCG≤30 both sub-steps) | [#324](https://github.com/nmorabowen/OpenSees/pull/324) |
| **Consistent mass-scaling energy conduit (V4)** ([[38_ladruno_consistent_mass_scaling_adr]], mass-scaling handoff §0 "V4") — closes the EnergyBalanceRecorder energy balance under CONSISTENT (Olovsson) scaling. The recorder builds KE from `Node/Element::getMass()` (the lumped diagonal `M_lump`) but the consistent path's cross-node `M̄ₑ` lives ONLY inside the integrator (matrix-free PCG operand, never written to Node mass) ⇒ recorded KE was `½vᵀM_lump v`, short of the true run KE `½vᵀM̃v`, so the consistent balance did NOT close (the lumped sibling's does — it injects into Node mass). The recorder holds only a `Domain*` (no integrator handle), so V4 adds a process-global **`Ladruno::MassScalingEnergyRegistry`** (owner-guarded; the active consistent integrator `publish()`es its per-element node-major `M̄ₑ` keyed by element tag at the end of `domainChanged`, `clear()`s on dtor/recvSelf) that the shared `EnergyBalanceKernel.h` queries per element to add the missing `½vᵀM̄ₑv` (vel is already node-major; no equation-number bookkeeping). `ConsistentBlock` gains an `eleTag`. EMPTY for the lumped path + every base integrator (`active()==false`) ⇒ recorded KE untouched, NO double count, zero overhead. Wired on all 3 consistent integrators (33008/33010/33012). NOT parallel-shared-node aware (= the separate T-MPI item). | Integrator/Recorder | — (no new tag) | `SRC/analysis/integrator/LadrunoMassScalingEnergy.{cpp,h}`, `LadrunoMassScaling.h` (`ConsistentBlock.eleTag`), `SRC/recorder/EnergyBalanceKernel.h` (hook), the 3 `*SMSConsistent.cpp` (publish/clear), `tests/test_massScaling_consistent_energy.py` | shipped — Zone-A 4/4: analytic `½vᵀM̃v` oracle (recorder KE == lumped+`M̄` to ~machine precision, ALL 3 integrators, `M̄`≈77% of KE on a deformation-rich IC) + CD mechanical-energy conservation (drift <5% vs pre-V4 reconstruction `½vᵀM_lump v+IE` that oscillates with the omitted `M̄`) | [#331](https://github.com/nmorabowen/OpenSees/pull/331) |
| **Consistent mass-scaling PARALLEL PCG (V5)** ([[38_ladruno_consistent_mass_scaling_adr]], mass-scaling handoff §0 "V5") — makes the CONSISTENT (Olovsson) path correct under OpenSeesMP (`system MPIDiagonal`). The serial `consistentPCG` is rank-local (local dot products, no shared-DOF `M̄` exchange) ⇒ wrong at partition boundaries. V5 adds a **distributed PCG** keyed on one ownership-free weight `wᵢ=1/multiplicityᵢ`: the matvec applies the (global, replicated) lumped diagonal WEIGHTED + off-diagonal `M̄ₑ` in FULL, then sum-assembles shared DOFs across ranks (diagonal collapses to full-once, off-diagonal accumulates); global inner products use the same weight + all-reduce; all CG control scalars are global ⇒ identical iteration count on every rank ⇒ collectives stay in lockstep (no deadlock). Collapses byte-identically to the serial PCG at `np=1`. The 3 consistent integrators live in the shared `OpenSeesLIB` and must NOT reference the MP-only `MPIDiagonalSOE` ⇒ runtime dispatch through 4 new `LinearSOE` base virtuals (serial no-op defaults; `MPIDiagonalSOE` overrides). Shared `refineAccel` body factored into `LadrunoConsistentRefine.h`. Lumped path is already parallel-correct (T-MPI), so this completes the parallel SMS axis. | Integrator/SOE | — (no new tag) | `SRC/analysis/integrator/LadrunoConsistentRefine.h` (new), `LadrunoMassScaling.h` (`ConsistentParOps`/`consistentParMatVec`/`consistentParPCG`), `SRC/system_of_eqn/linearSOE/LinearSOE.h` (4 base virtuals), `MPIDiagonalSOE.{h,cpp}` (overrides), the 3 `*SMSConsistent.cpp` (call the helper) | shipped — local 2-rank validation `Ladruno_implementation/mass_scaling_mpi/`: MPI np=2 == serial DiagonalSOE+`consistentPCG` reference (max abs diff 0 to recorder precision) AND np=1==np=2 AND consistent≠lumped; serial Zone-A 36/36 green (no regression). NOT CI-gated (single-process CI) | [#335](https://github.com/nmorabowen/OpenSees/pull/335) |
| **BezierTri6** — quadratic Bézier triangle element (Kadapa 2018) | Element | 33000 | `SRC/element/bezierTriangle/BezierTri6.{cpp,h}` | shipped | [#6](https://github.com/nmorabowen/OpenSees/pull/6), [#10](https://github.com/nmorabowen/OpenSees/pull/10) |
| **BezierTet10** — quadratic Bézier tetrahedron element (Kadapa 2018 §5), 3D sibling of BezierTri6: Bernstein basis, displacement + B-bar, all-positive lumped mass `ρVe/10`, 4-pt rule, `\|detJ\|` (orientation-robust), 4×4×4 collapsed-Duffy consistent mass; recorded via the Ladruno recorder inverted dispatch (`BezierTet10 → Tetrahedron_10N + CustomIntegrationRule + custom_rule_dimension=3`; FAMILY=bernstein, barycentric GP) | Element | 33001 | `SRC/element/bezierTetrahedron/BezierTet10.{cpp,h}`, `OPS_BezierTet10.cpp`, `tests/test_bezierTet10_element.py`, `tests/test_bezierTet10_corot.py`, `tests/test_bezierTet10_finite.py`, `tests/test_bezierTet10_fbar.py` | shipped — Zone-A T0+T1 (rank/6-rbm, constant-strain patch, quadratic-completeness via 3D blossom, uniaxial reaction+Poisson) + L0 smoke 4/4 + recorder round-trip 14/14. **`-geom corot` ADDED** (2nd [[solid_transformation_wrapper]] consumer; std+bbar; pure-`Bᵀσ` fCore + global-frame body/pressure/Q load contract; `-pressure`+corot rejected v1; widened sendSelf id slot 15; shared `formBAtGauss`/`formCore` single-pass assembly; plan `14_bezier_tet10_corot.md`, adversarial-reviewed 18/30 + high-effort code review) — Zone-A corot battery (rigid-rot incl >π std+bbar, linear-bit-identical seam, reduce-to-linear, objectivity, FD-tangent-gap-vanishes, bbar near-incompressible, cube-stack cantilever Newton w/ geometric-stiffening anchor, serialization round-trip); broker registration for BezierTri6+BezierTet10 added (were missing); 47/47 green. **`-geom finite` ADDED** (3rd [[solid_transformation_wrapper]] consumer; updated-Lagrangian: per-GP `F=I+Σuₐ⊗∂Nₐ/∂X` from REFERENCE Bernstein gradients → `FiniteStrainNDMaterial::setTrialF(F)` (e.g. `nDMaterial LogStrain`) → `∫σ_ij ∂Nₐ/∂x_j dv` (Cauchy σ) + full consistent tangent `a_ijkl=c_ijkl−σ_il δ_jk` via `getSpatialTangentTensor` (the 6×6 `getTangent` is lossy in (k,l)); spatial gradients via `F⁻¹`; shared single-pass `formResidAndTangentFinite`; serialization slot 15 already carries the method-id (NO change); body force on reference volume in the global-frame tail; `isFinite()` dispatch in `update`/`getResistingForce`/`getTangentStiff`, identity localize/globalize. **F-bar (bbar+finite) SHIPPED step 2 — CENTROID form** (dSNPO eq 15.5/15.10/15.11, ported from LadrunoBrick; design workflow UNANIMOUS centroid-not-mean-dilatation — brick is FD-validated, mean-dilatation is unvalidated research-grade w/ marginal benefit for a mild-locking quadratic tet; §15.1.9 patch is linear-simplex-only): `updateFinite` scales `F̄=(J₀/J)^(1/3)F` with `J₀` at the tet centroid `L=(¼,¼,¼)` (`centroidFbar` const helper, NOT the brick's (0,0,0)); residual unchanged (only σ̄); tangent adds the eq 15.10 coupling `∫Lfac·(G₀−g)dv`, `q_ij=⅓a_ijpp−⅔σ̄_ij` (the −⅔σ̄ COEFFICIENT TRAP, byte-equal to brick; GENERALLY UNSYMMETRIC → once-per-process FullGeneral advisory); `-bbar`+finite now accepted, `-pressure`+finite still rejected; std finite BIT-IDENTICAL when `!useBbar`. Two material guards mirror `OPS_LadrunoBrick`. design + review workflows) — Zone-A finite battery 16/16 (FD-consistent-tangent headline gate + symmetry, rigid-rotation objectivity incl >π/2, reduce-to-linear at small strain, homogeneous-deformation patch vs `logstrain_reference` oracle, det F≤0 step-cut guard, API guards, **load-driven multi-step Newton cantilever** through the geometric stiffness, serialization round-trip w/ `eleResponse('stiff')` element-state probe). **6-lens adversarial review** (23 findings → 6 confirmed, ALL test-coverage/consistency/docs — NO correctness bugs; core UL math + F⁻¹ push-forward + dv measure + `getSpatialTangentTensor` + both guards + serialization VERIFIED): fixes applied — degenerate-Jacobian `continue`→`return` consistency, getInitialStiff intentional-small-strain comment, the two coverage-gap tests above + `database_roundtrip` `probe_fn` element-state hook. **F-bar battery 8/8** (`test_bezierTet10_fbar.py`: FD-tangent UNSYMMETRIC-aware + ν=0.499 coefficient-trap guard, residual-reduces-to-std/tangent-differs, uniform-GP-pressure machine-level, **oracle-leg centroid-J₀** — hand-built F̄ at GP0 vs `logstrain_reference` pinning the centroid location + scaling against external truth, rigid-rotation objectivity, load-driven Newton FullGeneral). **5-lens F-bar review** (20 findings → 4 confirmed, ALL test-only — coefficient trap + centroid-¼ + byte-equal-to-brick + std-bit-identical VERIFIED): added the oracle leg + machine-level pressure bound + rotation test. **`-fbar {centroid|mean_dilatation}` SWITCH ADDED** (default centroid): mean-dilatation variant `F̄=(J̄/J)^(1/3)F`, J̄=(∫J dV₀)/V₀ (GP volume avg; =GP-mean for straight-sided), eq 15.10 coupling Ĝ→Ḡ=(Σ g·dv)/v (the (J₀,G₀)→(J̄,Ḡ) generalization — same q-coupling, FD-tangent is the arbiter); `fbarMeanDilatation()` helper, `fbarMode` member serialized iData 16→17. Mean-dilatation REDUCES to the small-strain volume-averaged `-bbar`. **HONEST CAVEAT (full-review finding): on the STRAIGHT-SIDED tets this element supports, Ḡ≈G₀ to high order (~5e-5 on a distorted+wiggled state) — the small-strain volumetric strain is ~linear in barycentric coords so its volume-mean ≈ its centroid value — so mean and centroid are PRACTICALLY NEAR-IDENTICAL here (centroid also ~reduces to the volume-avg bbar). The switch is correct + available (matters for curved tets / large deformation / explicit user control), NOT a big practical lever for straight-sided tets.** F-bar battery now 18/18, parametrized over BOTH modes (FD-tangent ν=0.3+0.499 + oracle-leg + uniform-pressure + residual-reduces + rigid-rotation + load-driven Newton all ×2; + modes-differ, mean-reduces-to-linear-bbar, fbarMode serialization). Mean-dilatation tangent has NO prior validated baseline — the passing FD-tangent is the proof the derivation is complete. **Full 5-lens review of the switch (22 findings → 3 confirmed, ALL test-only; the (J₀,G₀)→(J̄,Ḡ) tangent derivation VERIFIED complete, centroid/std bit-identical, serialization/parse correct): fixed = weight-aware oracle J̄, parametrized rotation+load-driven over modes, de-overclaimed the reduce-to-bbar docstring** | [#61](https://github.com/nmorabowen/OpenSees/pull/61) (element, merged 2026-06-01), [#108](https://github.com/nmorabowen/OpenSees/pull/108) (corot), [#145](https://github.com/nmorabowen/OpenSees/pull/145) (finite std, merged 2026-06-03), [#149](https://github.com/nmorabowen/OpenSees/pull/149) (F-bar: bbar + -geom finite) |
| **Ladruno recorder** (formerly "MPCO_Ladruno"; renamed 2026-05-31 to drop all MPCO naming — command `ladruno`, class `LadrunoRecorder`, files `LadrunoRecorder.*`/`Ladruno_*`, ns `ladruno`/`ladruno::detail`, `GENERATOR="Ladruno"`) — modular `.ladruno` recorder fork, node + element parity, multi-stage restaging; self-describing basis (consumes the `basisInfo`/`integrationPoints`/`integrationWeights` element probes), schema-v1 `MODEL/ELEMENTS/<name>` as a GROUP with `CONNECTIVITY` + `QUADRATURE`/{`GP_PARAM`,`GP_WEIGHT`}; first consumer = BezierTri6 (FAMILY=bernstein, barycentric GP); **standard-rule QUADRATURE by derivation** (`getStandardQuadrature` table → `NDIR`+`NUM_GP`+`QUADRATURE` for legacy GL quad/hex/tri/tet/line, no reader KeyError; higher-order quad9/tet10/hex20) + **belt-and-suspenders `GLOBAL_GP_COORDS`** (write-side `computeGlobalGP` basis evaluator incl. quad9/hex8/tet10 + hex20 serendipity, reference config, write-time round-trip oracle) + explicit `NDIR` (D4); **chunked extensible `[T×nIds×nComp]` time-series (ADR D3)** — `DATA` per result is one chunked+shuffle+deflate dataset growing one slab/step + `STEP[T]`/`TIME[T]` axes, replacing per-step `DATA/STEP_<k>` (kills 10⁵–10⁶-dataset blowup, compresses the time axis, O(1) slice reads); reader handles chunked-or-legacy transparently; **per-stage `KIND`** (`-kind transient|static|eigen`, default static, auto-`eigen` on modal results) + **`MODEL/LOCAL_AXES`** (per-class `<classTag>-<name>/{ID,FRAME[E×4 quaternion]}` from the element `"localAxes"` response → `quatFromMat`; never a silent identity fallback; ElasticBeam3d wired, other beams to follow); **energy result type (ADR D8): whole-model `RESULTS/ON_DOMAIN/energyBalance` + per-region `ON_REGIONS` (new `ResultFamily::OnRegions`) via `ladruno::EnergyBalanceSource` calling the shared `EnergyBalanceKernel.h`; `-G energy <regionTag...>` verb; `MODEL/SETS/SET_<tag>` writer**; **all 6 beam families answer `"localAxes"`** (#39); **D5 shared validator + write-time round-trip oracle + `FORMAT_VERSION=1` FROZEN** (#41, CI-gated); **parallel per-partition output** — each rank writes `<stem>.part-<N>.ladruno` + INFO partition manifest (the recorder TU has NO MPI — it is in the shared sequential `OPS_Recorder` lib, so the partition id comes from `sendSelf`/`recvSelf` config-broadcast on the PartitionedDomain path or `PMI_RANK`/`PMI_SIZE` env on the interpreter-per-rank `openseesmp` path; #42); **envelopes** — `-envelope` flag routes every non-modal channel through `EnvelopeSink` (componentwise MIN/MAX/ABSMAX + ARG_STEP under `RESULTS/ENVELOPES/<family>/<name>`, per-stage, periodic in-place rewrite; #43), with **self-describing component names** on envelope groups (finding (h); #44) and **element ENVELOPES carrying the structured per-column `COLUMN_MAP`** (gauss/section/fiber column structure, schema §7.4; the result-level `COMPONENTS` is empty for element results so the `COLUMN_MAP` is authoritative) | Recorder | 33001 | `SRC/recorder/LadrunoRecorder.{cpp,h}`, `Ladruno_{Types,Hdf5,Sinks,ResultIO,NodeResults,ElementResults,DomainResults}.{cpp,h}`, `EnergyBalanceKernel.h` | shipped | [#8](https://github.com/nmorabowen/OpenSees/pull/8), [#12](https://github.com/nmorabowen/OpenSees/pull/12), [#14](https://github.com/nmorabowen/OpenSees/pull/14), [#16](https://github.com/nmorabowen/OpenSees/pull/16), [#18](https://github.com/nmorabowen/OpenSees/pull/18), [#23](https://github.com/nmorabowen/OpenSees/pull/23), [#24](https://github.com/nmorabowen/OpenSees/pull/24), [#29](https://github.com/nmorabowen/OpenSees/pull/29), [#32](https://github.com/nmorabowen/OpenSees/pull/32), [#33](https://github.com/nmorabowen/OpenSees/pull/33), [#36](https://github.com/nmorabowen/OpenSees/pull/36), [#38](https://github.com/nmorabowen/OpenSees/pull/38), [#39](https://github.com/nmorabowen/OpenSees/pull/39), [#41](https://github.com/nmorabowen/OpenSees/pull/41), [#42](https://github.com/nmorabowen/OpenSees/pull/42), [#43](https://github.com/nmorabowen/OpenSees/pull/43), [#44](https://github.com/nmorabowen/OpenSees/pull/44), [#45](https://github.com/nmorabowen/OpenSees/pull/45) |
| **CentralDifferenceLadruno** — explicit leap-frog central difference done right (correct first-step starter, built-in `dt_cr`, clean full-step velocity, βK guard); coupled mode dropped → use `NewmarkExplicit 0.5` | Integrator | 33003 | `SRC/analysis/integrator/CentralDifferenceLadruno.{cpp,h}`, `tests/test_centralDifferenceLadruno_integrator.py` | shipped — full Linux link OK (#28); Zone-A CDL-1..10 battery | [#22](https://github.com/nmorabowen/OpenSees/pull/22), [#28](https://github.com/nmorabowen/OpenSees/pull/28) |
| **LadrunoArcLength** — adaptive static arc-length integrator. **PR-1 scope = stock-`ArcLength` identity (default-off) + Ramm desired-iteration radius adaptation `-adapt Jd ellMin ellMax [-p exp]`** (the radius cut/grow rule stock `LoadControl`/`DisplacementControl`/`MinUnbalDispNorm` carry but `ArcLength` never received → Abaqus modified-Riks parity); `Jlast←max(1,·)` div-by-zero guard for predictor-only convergence. DDM sensitivity machinery of stock `ArcLength` intentionally omitted. **`-stabilize` SHIPPED (follow-up PR, ADR [[20_ladruno_arclength_stabilized_adr]]):** Abaqus-`STABILIZE`-style viscous regularization — overridden `formTangent` adds `cOverDt·M*` to the diagonal (integrator-owned artificial `M*`, gershgorin/lumped/unity — NOT `getMass`, the BLOCKER); overridden `formUnbalance` subtracts the viscous force `cOverDt·M*·Δu` via `getB`/`setB`; **mutually exclusive with the arc-length quadratic** — `-stabilize` ⇒ viscous-regularized incremental load control (`-adapt` then drives the load increment `dLambda0`); `c` from `-cVisc` (explicit) or one-shot energy-fraction calibration (`-stabilize f`, default `f=2e-4`); `-adaptStab` tracks `f`; true unbalance `‖λp−f_int‖` recorded separately from the `f_v`-polluted SOE residual. **Layer-B SHIPPED (cut-and-retry, ADR §4.3):** scalar radius mutators `reduceStep(f)`/`increaseStep(f)`/`setArcLength(v)` (`arcLength2 *= f*f`, floor/`ellMax`-guarded, no reallocation) + a `commit()`-time snapshot of `{arcLength2, deltaLambdaStep, deltaUstep, currentLambda, sign}` restored by an **overridden `revertToLastStep()`** — stock `ArcLength` overwrites `deltaLambdaStep`/`deltaUstep` in `newStep()` BEFORE its own `b24ac<0` bail so "state preserved for free" is FALSE (review §2.10); exposed to Python/Tcl via the small `ladrunoArcLength <sub> <val>` integrator-object command (`OpenSeesCommands` → `PythonWrapper`/`TclWrapper`); the retry loop stays **script-owned** (`StaticAnalysis::analyze` already calls `revertToLastCommit`+`revertToLastStep` on failure — NO analysis-core change). Oracle = stock `ArcLength` bit-identity + the von Mises snap-through truss (limit ≈3.80). NB classTag 33004 is per-registry — coexists with `ELE_TAG_LadrunoIMKBeam2d=33004`. | Integrator | 33004 | `SRC/analysis/integrator/LadrunoArcLength.{cpp,h}`, `tests/test_ladrunoArcLength_integrator.py` | shipped — identity+`-adapt` (#157), `-stabilize` + Layer-B (#178); Zone-A AL-1/AL-3/AL-5/AL-9/AL-S | [#157](https://github.com/nmorabowen/OpenSees/pull/157), [#178](https://github.com/nmorabowen/OpenSees/pull/178) |
| **LadrunoDynamicRelaxation** — quasi-static solver by **explicit, matrix-free dynamic relaxation** (the explicit sibling of LadrunoArcLength): fictitious-mass leap-frog + **Cundall kinetic damping** driven to static rest, under the stock `DirectIntegrationAnalysis` driver with `algorithm Linear` + `system Diagonal` (NO analysis-core surgery). **`M*` = integrator-owned Gershgorin row-sum fictitious mass** (`m_i=(dt²/4)Σ_j|K_ij|`, scale-free; built once in `domainChanged` via an SOE-free element-stiffness probe), assembled onto the diagonal SOE by an overridden `formTangent()` so the solve degenerates to a `M*⁻¹` apply — deliberately NOT `getMass()` (zero on zero-density models, the ADR-20 BLOCKER). Modes `-mass {gershgorin\|lumped\|unity}`. True static residual `‖f_ext−f_int‖=‖M*·a‖` (no `f_v` pollution). Termination is **script-owned** via `Ladruno_scripts/relax_to_static.py` (velocity+displacement decay; the stock transient driver has no convergence early-exit). Differentiates from `ExplicitDifferenceStatic` (33001, user-tuned Rayleigh) by auto-tuning everything — the LS-DYNA `*CONTROL_DYNAMIC_RELAXATION` sibling Abaqus lacks. ~70% leap-frog reuse from CentralDifferenceLadruno. **v2 (same PR): `-damping viscous` (viscous-critical mode, lagged-velocity, `cVisc=4ζs/dt` with `s=√(1+ζ²)−ζ` so the explicit damping stays stable) + auto-refresh (`M*` rebuilt at each kinetic-energy peak where velocities are already zero ⇒ snap-through is stable with NO manual `-recompute`).** Deferred v3: per-region KE zeroing. ADR [[21_ladruno_dynamic_relaxation_adr]]. | Integrator | 33005 | `SRC/analysis/integrator/LadrunoDynamicRelaxation.{cpp,h}`, `SRC/analysis/integrator/LadrunoFictitiousMass.h` (shared artificial diagonal-mass machinery, also used by LadrunoArcLength), `Ladruno_scripts/relax_to_static.py`, `tests/test_ladrunoDynamicRelaxation_integrator.py` | shipped — v1+v2; Zone-A 13/13 (DR-1/6/6b/8/V/S) | [#165](https://github.com/nmorabowen/OpenSees/pull/165) (v1 + ADR-21), [#170](https://github.com/nmorabowen/OpenSees/pull/170) (v2 viscous + auto-refresh) |
| **LadrunoIndirectControl** — indirect / CMOD displacement-control static integrator (classTag 33006), **ADR-20 §8 follow-up #3**. Generalizes stock `DisplacementControl` (a single DOF) to a **weighted multi-DOF control quantity** `ζ = c·U = Σₖ coefₖ·U(nodeₖ,dofₖ)` (e.g. crack-mouth-opening `CMOD = u_A − u_B`), which stays **monotone through snap-back** — where the load factor AND the individual nodal DOFs both reverse — so it follows equilibrium branches that geometric arc-length and single-DOF `DisplacementControl` cannot (de Borst indirect control). Constraint per step `c·ΔU = Δζ`; mirrors `DisplacementControl`'s dUhat/dUbar bookkeeping with the scalar control component replaced by the dot product `c·dUhat`. Parser `integrator LadrunoIndirectControl $incr -dof $node $dof $coef <-dof …> <-iter $numIter $dmin $dmax>`; optional Ramm increment adaptation; no DDM sensitivity (out of scope, as LadrunoArcLength). | Integrator | 33006 | `SRC/analysis/integrator/LadrunoIndirectControl.{cpp,h}`, `tests/test_ladrunoIndirectControl_integrator.py` | shipped — Zone-A IC battery (reduce-to-DisplacementControl + CMOD snap-back) | [#184](https://github.com/nmorabowen/OpenSees/pull/184) |
| **LadrunoHHT** ([[52_ladruno_integrator_strengthening_adr]] W3-I2) — sensitivity-carrying (DDM) **subclass of `HHT`** that reuses the entire HHT algorithm (newStep/update/commit/tangent inherited unchanged) and adds the Direct Differentiation Method seam absent from `HHT` in vanilla OpenSees (DDM ships only on `Newmark`/`DisplacementControl`/`ArcLength`/`MinUnbalDispNorm`) → unblocks reliability / fragility / FORM on the numerically-damped α integrator. **Vanilla footprint = header-only** edit of `HHT.h` (private→protected promotion + ONE protected inline classTag ctor; `HHT.cpp` byte-identical — ledgered). The 8-method sensitivity seam mirrors Newmark, but the dynamic residual is evaluated at the HHT alpha-intermediate state `Ualpha=(1-α)Ut+αU` / `Ualphadot`: since `U/Udot/Udotdot` evolve by the SAME Newmark relations, `saveSensitivity`/`commitSensitivity`/`formSensitivityRHS`/`formIndependentSensitivityRHS`/`computeSensitivities` are identical to Newmark; only `formEleResidual`/`formNodUnbalance` change — (1) the damping multiplicator gains the α-weighting of `d(Ualphadot)/dh`, (2) `∂C/∂h` acts on `Ualphadot` not `Udot`, (3) an EXTRA element-only stiffness term `−K·(1−α)·dUₙ` (the `(1−α)` part of `d(Ualpha)/dh`, via `addK_Force` on the consistent tangent) that vanishes for Newmark (α=1). Mass terms unchanged (M acts at the full step). Parser `integrator LadrunoHHT $alpha <$gamma $beta>` (same args as `HHT`). Inherits `HHT::sendSelf/recvSelf` (no new persistent state). | Integrator | 33013 | `SRC/analysis/integrator/LadrunoHHT.{cpp,h}`, `SRC/analysis/integrator/HHT.h` (header-only vanilla promotion + protected ctor), `tests/test_adr52_w3i2_ladruno_hht.py` | shipped — Zone-A FD gradient-check (undamped + mass-prop damped) + sensitivity-off byte-identity vs vanilla HHT + α=1 reduces-to-Newmark-DDM; 22-agent adversarial review (0 blockers) | [#413](https://github.com/nmorabowen/OpenSees/pull/413) |
| **LadrunoGeneralizedAlpha** ([[52_ladruno_integrator_strengthening_adr]] W3-I2 PR2) — sensitivity-carrying (DDM) **subclass of `GeneralizedAlpha`** (Chung-Hulbert); the strict superset of `LadrunoHHT` with TWO spectral parameters: **αF** weights stiffness/damping (at `Ualpha`/`Ualphadot`) and **αM** weights inertia (at `Ualphadotdot`), where HHT has a single α on K/C and full-step M. Same **header-only** vanilla edit pattern (`GeneralizedAlpha.h` private→protected + protected inline classTag ctor; `.cpp` byte-identical — ledgered). `saveSensitivity`/`commitSensitivity`/`formSensitivityRHS`/`formIndependentSensitivityRHS`/`computeSensitivities` identical to Newmark (Newmark recurrence on `U/Udot/Udotdot`). **NB the base `GeneralizedAlpha` tangent (`αM·c3·M`) is inconsistent with its primal residual (inertia at the full-step `Udotdot` ⇒ effective M-coef `c3`) — see [[LEDGER_quirks]];** so the DDM is built **primal-consistent**, not tangent-consistent: `formEleResidual`/`formNodUnbalance` use the αF weighting on K/C (at `Ualpha`/`Ualphadot`) + the extra `−K·(1−αF)·dUₙ`, but the **mass term is the plain Newmark form with M at `Udotdot` (no αM)**; and `computeSensitivities` **re-forms the sensitivity-solve tangent with `c3·M`** (`sensTangentFlag` branch in `formEleTangent`/`formNodTangent`) instead of reusing the inconsistent factored primal tangent. The first (tangent-consistent) cut failed the Zone-A FD oracle ~2e-3 at αM=0.9 — the fix drops it to ~1e-9. Reduces to Newmark DDM at αM=αF=1; primal path byte-identical. Parser `integrator LadrunoGeneralizedAlpha $alphaM $alphaF <$gamma $beta>`. Inherits `GeneralizedAlpha::sendSelf/recvSelf`. | Integrator | 33014 | `SRC/analysis/integrator/LadrunoGeneralizedAlpha.{cpp,h}`, `SRC/analysis/integrator/GeneralizedAlpha.h` (header-only vanilla promotion + protected ctor), `tests/test_adr52_w3i2_ladruno_genalpha.py` | shipped — Zone-A FD gradient-check (undamped + mass-prop damped) + sensitivity-off byte-identity + αM=αF=1 reduces-to-Newmark-DDM; 12-agent focused review caught the base-tangent inconsistency (FD oracle confirmed, fix verified) | [#415](https://github.com/nmorabowen/OpenSees/pull/415) |
| **LadrunoStabilizedUnbalance** — true-equilibrium `NormUnbalance` convergence test (classTag 33000, CONVERGENCE_TEST registry), **ADR-20 §8 follow-up #4**. In the `LadrunoArcLength -stabilize` mode the SOE residual a stock `CTestNormUnbalance` norms is the **f_v-polluted** `λp − f_int − f_v`, so Newton is satisfied while the TRUE static unbalance is still nonzero by `‖f_v‖`. This test norms `‖λp − f_int‖` instead (a **stricter** criterion driving the artificial force below tol ⇒ a genuine equilibrium, not a regularized one). **Zero analysis-core surgery:** `setEquiSolnAlgo` captures the active integrator via `EquiSolnAlgo::getIncrementalIntegratorPtr`; if it is a stabilizing `LadrunoArcLength` it reads the new `getStabilizedTrueResidual()` accessor, else **degrades gracefully** to the stock SOE `‖B‖` (drop-in for `NormUnbalance`). Python `test LadrunoStabilizedUnbalance $tol $maxIter <$printFlag $normType>`. | Convergence test | 33000 | `SRC/convergenceTest/LadrunoStabilizedUnbalance.{cpp,h}`, `SRC/analysis/integrator/LadrunoArcLength.{cpp,h}` (added `getStabilizedTrueResidual` accessor), `tests/test_ladrunoStabilizedUnbalance_test.py` | shipped — Zone-A ST battery (graceful fallback + true-vs-polluted residual) | [#184](https://github.com/nmorabowen/OpenSees/pull/184) |
| **Stack profiler** — per-phase perf + memory profiler across implicit (Newton) and explicit paths (`06_profiler.md`). Engine module `SRC/utility/profiler/` (`PerfClock`/`Profiler`/`ProfilerMacros`/`ProfilerHDF5Writer`, replaces the Win32-no-op `Timer`); single shipped binary, runtime-gated (`profiler('start'[,'-deep'][,'-memory'])`/`stop`/`reset`/`report`/`memory`), HDF5 one-group-per-run mirrored by `Ladruno_tools/profiler_viewer/` Python `ProfilerResults`. P1 timing core + P2 phase seams (NewtonRaphson/Linear/Direct+Static analyze) + P5 command. **P4 memory COMPLETE: per-type Matrix/Vector/ID live/peak alloc counters** (`matrix_live`/`vector_live`/`id_live`) **+ TaggedObject live-component census** (`components_live` `[{classTag,count}]` via `MovableObject` ctor/dtor, gated on `mem()`). **P3 deep per-element-type timing**: `OPS_PROFILE_SCOPE_DEEP_NAMED` + per-element `OPS_PROFILE_FE_ELEM_SCOPE` in `IncrementalIntegrator`/`TransientIntegrator` `formTangent`/`formElementResidual` → `elem_by_type` `[{classTag,count,wall_ns,wall_ns_per_elem,fb_coupled}]` (the P0#1 per-element-class axis), deep-gated so an unprofiled run pays nothing. **P7 FastAPI backend** (`profiler_api.py`): thin HTTP layer over `ProfilerResults` (`/runs`, `/runs/{id}/{header,rollup,series,memory}`, `/diff`, `/health`, Swagger `/docs`); opens the HDF5 per request (no long-lived lock); `test_api.py` exercises every endpoint via `TestClient`. **P8 React+TS frontend** (`frontend/`): run picker + hand-rolled SVG icicle/flame (with `elem_by_type` drill-in) + time-series + memory/census + run-diff + dt_cr/oversample badges; pure client of the P7 API, no charting dep. **P0#3 per-step series**: `OPS_PROFILE_STEP` hook in the Static/Direct analysis drivers records step/t/dt/iters per committed step (armed by `profiler('start','-perStep')`) → activates the frontend time-series (iters/step + dt/step). **Run-header identity**: algorithm/integrator/solver names + dt range + dt_cr/oversample (P0#5) populated in `report` — labels runs for the diff, drives the dt_cr badge. **Viewer polish**: cross-run time-series overlay (the "vs" run's per-phase/iters/dt curves dashed/faded, matched by phase) + leak badge (memory census diff flagging classTags with more live objects than the baseline). **One-click launcher** (`Profiler_Viewer.bat` / `profiler_viewer.sh` → `launch.py`): stdlib bootstrapper provisions a venv, installs deps, builds the UI, and serves API+UI from one process (`profiler_api` mounts `frontend/dist` at `/`); cross-platform, zero manual setup. | Utility | — | `SRC/utility/profiler/*`, `Ladruno_tools/profiler_viewer/*` (+ vanilla seams, see [[LEDGER_vanilla_files]]) | shipped (P1/P2/P3/P4/P5/P7/P8 + per-step series + run-header + viewer polish); per-phase wall series + P0#4 pending | [#35](https://github.com/nmorabowen/OpenSees/pull/35), [#46](https://github.com/nmorabowen/OpenSees/pull/46), [#47](https://github.com/nmorabowen/OpenSees/pull/47), [#48](https://github.com/nmorabowen/OpenSees/pull/48), [#49](https://github.com/nmorabowen/OpenSees/pull/49), [#50](https://github.com/nmorabowen/OpenSees/pull/50), [#51](https://github.com/nmorabowen/OpenSees/pull/51), [#52](https://github.com/nmorabowen/OpenSees/pull/52), [#53](https://github.com/nmorabowen/OpenSees/pull/53), [#55](https://github.com/nmorabowen/OpenSees/pull/55), [#58](https://github.com/nmorabowen/OpenSees/pull/58) |
| **Analysis monitor** — live `recorder Monitor` that streams selected nodal scalars (disp/vel/accel/reaction per dof) to a SWMR-HDF5 sink a viewer process tails *while the run is still going* (`08_analysis_monitor.md`). Shape B (engine pushes from `record()` at each commit); two rate gates bound the data at explicit step counts — `-every K` step decimation + `-hz H` steady_clock wall throttle — so streaming never perturbs the solve (append is fire-and-forget, never blocks on a reader). One file, two lifetimes: the live sink is a valid at-rest results file. v1 = nodal scalar time-histories, sequential; element/region channels + profiler-telemetry tee + FastAPI `/live` SSE + React live mode + parallel path are follow-ups. Reuses NodeRecorder selection/response idiom; registered via the existing `recorder` command (type `Monitor`) so `addRecorder`/`remove recorder $tag`/tag-as-result come for free. | Recorder | 33002 | `SRC/recorder/LadrunoMonitorRecorder.{cpp,h}`, `LadrunoMonitorSink.{cpp,h}`, `Ladruno_scripts/test_monitor_smoke.py` (+ vanilla seams, see [[LEDGER_vanilla_files]]) | shipped (v1 merged 2026-06-01) | [#64](https://github.com/nmorabowen/OpenSees/pull/64) |
| **LadrunoBrick** — unified 8-node hexahedral solid element with a single `-formulation {std\|bbar\|uri\|ssp\|eas}` selector (B-bar ≡ selective/"partial" integration → one name not two flags; bbar⊥uri) + `-hourglass {stiffness\|physical\|viscous}` (uri). Small-strain: `std` (reproduces upstream `Brick` to ~1e-9) + `bbar` (reproduces `bbarBrick`) + `uri` (1-pt reduced + Flanagan–Belytschko `stiffness` / Belytschko–Bindeman `physical` assumed-strain / **`viscous`** rate-form damping for explicit — the cheap explicit hex OpenSees lacks) + **`ssp`** (stabilized single-point: bbar + a statically-condensed stabilization, a verbatim port of `UWelements/SSPbrick::GetStab` — NB despite the SSPbrick derivation this is **NOT** enhanced assumed strain; the stabilization modes are condensed from the **initial** tangent so `Bnot`/`Kstab` are CONSTANT — no per-step α state — and cure BOTH shear and volumetric locking across all ν; rebuilt in `setDomain` on the receive side so `sendSelf` needs nothing extra). `physical` is a shear-locking cure only (vol-locks at ν→0.5); `ssp` is the general-ν element. **RENAMED 2026-06-02 (PR-1): the selector `eas`→`ssp` (enum ordinal preserved SSP=3 = old EAS slot ⇒ serialized models reload as the same element). TRUE Simo-Rifai EAS shipped 2026-06-03 (PR-2) under `-formulation eas` (enum ordinal 4): a mixed assumed-strain hex — compatible strain B·u enriched by 9 element-internal enhanced params α (E9 = the Wilson incompatible-mode set, 3 natural bubbles × 3 dofs), solved each form pass by an inner Newton enforcing ∫MᵀσdV=0 then statically condensed `K*=Kdd−Kda Kαα⁻¹ Kad` (the inner Newton drives h→0 so `f*=∫Bᵀσ`, the EnhancedQuad contract). Full 2×2×2 (8 LIVE GPs). One-sided mode map `M_i=sym[(j0/j)J0⁻ᵀE_i]` (ADR eq E.8 — NOT a two-leg T0 strain transform; the adversarial sweep caught that), cached centroid `easJ0inv/easJ0det`. α is committed state (`alphaCommit`) carried by commit/revert/sendSelf (guarded extra Vector(9), gated on the formulation ordinal); `update()` solves α + strains the 8 mats so commit is correct under Linear too. v1 small strain only (`eas`+corot/finite parser-reserved — enhanced-F finite EAS is ADR 19's deferred follow-up, where Q1/E9 hourglasses in compression). Ported from 2-D `EnhancedQuad`. Validated `tests/test_ladrunoBrick_eas.py` 6/6: distorted-mesh constant-strain patch (∫M dV=0), reduce-to-std (α→0), bending-beats-std→Euler, near-incompressible non-locking, 8-live-GP, α commit/revert. ADR [[19_ladruno_brick_eas_simo_rifai]]. **A scalar `-stab β` EAS tangent regularization was added then REMOVED (ADR [[20_ladruno_brick_eas_stabilization]], rejected): the DEN-bar sweep showed bare `eas` has no reproducible stall to cure and β degrades convergence — `eas` is robust on notched inelasticity with a normal adaptive solver.** Three seams carved (kinematics ledger / geometry method / material adaptor) so `corot`/`finite` drop in additively via [[solid_transformation_wrapper]]. Non-negative lumped mass for explicit; does NOT inherit upstream `Brick::setParameter` damping `i<4`-over-8 bug. Gates: `std`/`bbar`↔upstream ~1e-9; `ssp`↔`SSPbrick` to ~1e-6 across ν∈{0,0.3,0.45,0.499} (the stabilization validating oracle — patch/rank cannot validate it; bending convergence + SSPbrick cross-check can). Plan: [[09_ladruno_brick]]. | Element | **33002** (band ≥33000, after BezierTri6=33000/BezierTet10=33001) | `SRC/element/ladrunoBrick/{LadrunoBrick.{cpp,h},OPS_LadrunoBrick.cpp,CMakeLists.txt}`, `tests/test_ladrunoBrick_{element,bending,nonlinear,explicit,recorder_loads,hourglass_energy,viscous_dissipation,singlepoint_output,asdconcrete,eas}.py`, `tests/test_ladrunoBrick_asdconcrete_bend.py` (Zone-B, gmsh) | shipped — std/bbar/uri(stiffness) #69, uri(physical) #72, ssp (shipped as `eas`, renamed 2026-06-02) + uri(viscous) #75; hourglass-energy report #84; **cumulative viscous-hourglass dissipation report** (committed `hgDissipated` accumulator, work integral against the FB rate damper, reported via `"hourglassEnergy"`/`"hgDissipation"`; serialized) PR #86; **single-point material-eval fix** (`isSinglePoint()`: ssp/uri-stiffness/uri-viscous evaluate the constitutive model once at slot 0, slots 1–7 are output mirrors — 8×→1× material evals for expensive materials like ASDConcrete3D; ASDConcrete3D integration doc `11_brick_asdconcrete_integration.md`) PR #94; **Tier-A damage-scaled hourglass `Kstab`** (softening support, item (a)): the STIFFNESS-stabilized single-point forms (ssp, uri+stiffness) degrade their constant ELASTIC `Kstab` by `s=max(floor,1−max(dₜ,d_c))` (floor=1%) so a cracked ASDConcrete3D element can localize yet keeps residual hourglass control; damage read via a cached `materialPointers[0]->setResponse("damage",…)` Response built in `setDomain` (null⇒fallback `s=1`, zero change for elastic/J2 — no NDMaterial base touch); the `"hourglassEnergy"` report carries the scale; uri+viscous (damping not stiffness) deliberately NOT degraded. Validated `tests/test_ladrunoBrick_asdconcrete.py` (lch handshake/mesh-objectivity: dissipation ∝ crack AREA ⇒ size ratio≈4 not 8 + absolute G_f; Tier-A: stabilization-energy ratio == max(floor,1−d) exactly across the damage range incl. floor) PR #101 (+ recvSelf clears the cached `damageResponse` so its raw material-ptr can't dangle on a class-change rebroker); Zone-A element + bending + nonlinear + explicit + recorder + hourglass-energy + viscous-dissipation + singlepoint + asdconcrete batteries; **Zone-B (gmsh) gmsh-meshed SENB notched-bend mesh-objectivity (load ~1% / dissipation ~3% across 2× refine) + ssp hourglass-energy-fraction-in-cracked-band (~3.7% < 10%)** — `conftest` zone_b gate relaxed to gmsh-only; **`getCharacteristicLength()` override** — geometry-true `lch = cbrt(V)` (2×2×2-Gauss reference volume; the hex analogue of BezierTet10 `cbrt(6V)`/BezierTri6 `sqrt(2A)`), new `charLength`/`characteristicLength` response (ID 9), Zone-A `test_char_length_is_cbrt_volume` pins `cbrt(a·b·c)` on a stretched box (≠ min edge) across std/bbar/ssp | [#69](https://github.com/nmorabowen/OpenSees/pull/69), [#72](https://github.com/nmorabowen/OpenSees/pull/72), [#75](https://github.com/nmorabowen/OpenSees/pull/75), [#84](https://github.com/nmorabowen/OpenSees/pull/84), [#94](https://github.com/nmorabowen/OpenSees/pull/94), [#101](https://github.com/nmorabowen/OpenSees/pull/101), charLength [#92](https://github.com/nmorabowen/OpenSees/pull/92), Zone-B [#117](https://github.com/nmorabowen/OpenSees/pull/117), rename→ssp [#150](https://github.com/nmorabowen/OpenSees/pull/150), true Simo-Rifai EAS [#151](https://github.com/nmorabowen/OpenSees/pull/151) (+ inner-Newton tol [#152](https://github.com/nmorabowen/OpenSees/pull/152)), eas `-stab` [#161](https://github.com/nmorabowen/OpenSees/pull/161) REMOVED via [#173](https://github.com/nmorabowen/OpenSees/pull/173) |
| **SolidTransformation** — geometry-method layer for solid elements (the solid analogue of beam `CrdTransf`/`CorotCrdTransf`, which are frames-only upstream). Decouples element *kinematics* (linear ⊂ corot ⊂ finite) from the constitutive/integration core, orthogonal to the *formulation* axis (std/bbar/uri/ssp). Corrected division of labour: the **formulation owns B-construction**; the transformation owns only `getStrainMeasure()`, `localizeDisp` (seam 2), `globalizeForce`/`globalizeStiff` (seam 3, +K_geo), and the recorder frame (`frameTimeVarying`/`getCurrentFrame`). **v1 = `SolidTransformationLinear` (identity)**: all seams are guarded no-op pass-throughs, so routing a small-strain element through it is bit-for-bit the direct kernel — that equivalence IS the seam proof. **Consumed by [[09_ladruno_brick]] `LadrunoBrick`** (holds `theGeom=new SolidTransformationLinear()`; calls `update`→`localizeDisp` for strain, routes `resid`/`stiff` through `globalize*`; serializes a method-id int in `idData(28)` as `+1000*methodID`, Linear=0 ⇒ byte-identical stream; rebuilds via `SolidTransformation::create()` in `recvSelf`). Element-agnostic — touches **no** `classTags.h` entry; **not a MovableObject** in v1. **corot (v2) IMPLEMENTED + green (PR #88):** element-level EICR — `R=polar(H)` (Procrustes best-fit of the nodal cloud, scaled Higham Newton), de-rotated small strain fed to the REUSED small-strain material lib (`ElasticIsotropic`/`J2Plasticity`, no finite material), exact residual `f=Rf_d−(1/n)RΣf_d−Γᵀm` + symmetric geometric tangent `R̄k_dR̄ᵀ+(G1+G1ᵀ)` (two O(strain) terms — polar-Hessian + material-∂R coupling — deferred to v2.1; residual exact ⇒ Newton converges regardless). Stateless (R from geometry; continuum nodes have no rotational DOFs); `std`+`bbar` only (`uri`/`ssp` parse-guarded off). **`SolidTransformationCorot` `MAX_NODES` raised 8→10 (allocation-only — all corot logic runs on the runtime `n`/`ndof`; the 8-node LadrunoBrick is unaffected) to admit a 2nd consumer, [[09_ladruno_brick]]'s tet sibling `BezierTet10 -geom corot` (plan `14_bezier_tet10_corot.md`).** Spin map Γ is SPATIAL (`Γ=R·ω_body`, `ω_body=(tr(S)I−S)⁻¹·2·axial(skew(RᵀδH))`). Validated: standalone Γ-vs-FD (caught a body→spatial frame bug pre-build) + 8-lens adversarial-review workflow (39 findings/**33 confirmed/6 refuted**; core math re-derived & confirmed correct) + 14 Zone-A tests incl. rigid-rotation>π, FD-tangent-gap-vanishes, **ADR Gate 5 load-driven cantilever** (matches `-geom finite` <1% at ~26% tip deflection) & **Gate 6 J2-plasticity-in-rotated-frame**. **v1 linear MERGED (PR #71).** **v3 `SolidTransformationFinite` IMPLEMENTED (updated-Lagrangian):** `getStrainMeasure()==DeformationGradient`; the element computes `F=I+Σuⱼ⊗∇ₓNⱼ` per GP, calls `FiniteStrainNDMaterial::setTrialF(F)`, assembles `∫Bᵀσ dv` + the consistent tangent `K_{(a,i)(b,k)}=∫ ∂Nₐ/∂xⱼ a_ijkl ∂N_b/∂xₗ dv` with `a_ijkl=c_ijkl−σ_il δ_jk` (full-gradient contraction; localize/globalize identity since the element owns the whole UL assembly). **KEY contract revision: `c=(1/2J)[D:L:B]` is NOT minor-symmetric in (k,l), so it CANNOT pass through a 6×6 — the material now exposes the FULL 4th-order tensor via `FiniteStrainNDMaterial::getSpatialTangentTensor(c[3][3][3][3])`; the old `getTangent` 6×6 is a lossy projection.** v3 scope: `-geom finite` + `-formulation {std\|bbar}` (uri/ssp+finite reserved). **`bbar`+`finite` = F-bar (dSNPO §15.1, the large-strain volumetric-locking cure):** the element drives the material with the modified gradient `F̄=(detF₀/detF)^(1/3)F` (F₀ at the centroid; `updateFinite`), residual unchanged (eq 15.9 — only σ̄ changes), and the consistent tangent gains the eq 15.10 coupling `∫Gᵀq(G₀−G)dv` with `q=(1/3)a:(I⊗I)−(2/3)σ̄⊗I` (eq 15.11, full `a=c−σδ` — **NOT** the material `c` alone; a naïve spatial shortcut drops the `−(2/3)σ⊗I`). This term is **generally UNSYMMETRIC** ⇒ needs `FullGeneral` (logged in [[LEDGER_quirks]]). Locked against the primary source (de Souza Neto, Perić & Owen 2008 Ch.15) after a 3-derivation adversarial workflow disagreed (all 3 reproduced the shortcut's wrong `(1/3)c` coefficient). Verified: FD consistent-tangent + symmetry + homogeneous-deformation patch + rigid-rotation objectivity + reduce-to-linear (std, 6/6 green); **F-bar: FD-tangent (unsymmetric-aware) + reduce-to-std-on-homogeneous-F + T4 volumetric-locking cantilever (std locks at ν→0.5, F-bar doesn't)**. Plan: [[solid_transformation_wrapper]]. | Element infra (geometry layer) | — (not a MovableObject; serialized method-id int only) | `SRC/element/solidTransformation/{SolidTransformation.h,SolidTransformationLinear.{cpp,h},SolidTransformationFinite.{cpp,h},SolidTransformationCorot.{cpp,h},CMakeLists.txt,Makefile}`; `SRC/element/ladrunoBrick/LadrunoBrick.{cpp,h}`, `OPS_LadrunoBrick.cpp`; `tests/{test_ladrunoBrick_finite.py,test_ladrunoBrick_corot.py}` | v1 linear shipped (#71); v3 finite written+green (PR #76); v2 corot shipped (PR #88); F-bar (bbar+finite) shipped (PR #104); 2nd consumer BezierTet10 corot (#108) | [#71](https://github.com/nmorabowen/OpenSees/pull/71), [#76](https://github.com/nmorabowen/OpenSees/pull/76), [#88](https://github.com/nmorabowen/OpenSees/pull/88), [#104](https://github.com/nmorabowen/OpenSees/pull/104), [#108](https://github.com/nmorabowen/OpenSees/pull/108) |
| **Finite-strain material wrapper (seam 3)** — logarithmic (Hencky) strain-space adaptor lifting any GREEN 3D small-strain `NDMaterial` to finite strain (**spatial** multiplicative plasticity, dSNPO 2008 Box 14.3 *MATISU*), reusing the inner return map *verbatim*: `bᵉᵗʳ=F_Δ bᵉ_n F_Δᵀ → εᵉ=½ln bᵉ → unchanged return map → τ → σ=J⁻¹τ`. Material side only: concrete `LogStrainNDMaterial` + fork-local base `FiniteStrainNDMaterial` (adds `setTrialF(F)`; **interface-only header shipped in #68**); adaptor owns committed `bᵉ` per GP + the repeated-eigenvalue degeneracy branch (D3, ours). **Element side = the [[09_ladruno_brick]] `finite` geometry method** (`LadrunoBrick`, classTag 33002, [[solid_transformation_wrapper]]); the two meet at seam 3 — now WIRED end-to-end (LadrunoBrick `-geom finite` drives LogStrain via `setTrialF`; FD-tangent green). `LogStrainNDMaterial` MERGED (#70), broker + contract-lock (#73). **Seam-3 tangent channel REVISED: added `FiniteStrainNDMaterial::getSpatialTangentTensor(double c[3][3][3][3])` (+ kernel `spatial_tangent_full`) because `c=(1/2J)[D:L:B]` is non-minor-symmetric in (k,l) and the 6×6 `getTangent` is lossy** — the element consumes the full 4th-order `c`. Plan + GREEN/YELLOW/RED matrix in `09_finite_strain_material_wrapper.md`. | Material | `ND_TAG_LogStrainNDMaterial` **33010** (element tag 33002 = companion) | `SRC/material/nD/{FiniteStrainNDMaterial.h,LogStrainNDMaterial.{cpp,h},LogStrainKernel.h}` | LogStrain shipped (#70); full-tensor channel + finite wiring (PR #76) | [#68](https://github.com/nmorabowen/OpenSees/pull/68), [#70](https://github.com/nmorabowen/OpenSees/pull/70), [#73](https://github.com/nmorabowen/OpenSees/pull/73), [#76](https://github.com/nmorabowen/OpenSees/pull/76) |

| **LadrunoJ2** — combined-hardening von Mises (J2) `nDMaterial`: Voce-saturation+linear **isotropic** + **Chaboche** superposed Armstrong–Frederick **kinematic** hardening (`α=Σₖαₖ`, design-for-N, ships N≤8 via `-kin N C₁ γ₁ …`; `γₖ=0`⇒linear Prager, N=1⇒single AF). Supersedes BOTH `J2Plasticity` (no kinematic) and `SimplifiedJ2` (linear-only + defects: wrong `getInitialTangent`, stub reverts, `exit(-1)`). Return map = **scalar Newton on Δγ** (Kobayashi–Ohno 2002): direction `n=M(Δγ)/‖M(Δγ)‖`, `M(Δγ)=sᵗʳ−Σαₖⁿ/(1+√⅔γₖΔγ)` — NOT a coupled tensor solve; dSNPO's radial reduction (eq 7.192) is the `γ=0` case. Analytic **consistent tangent** (dSNPO eq 7.213 structure + AF `∂n/∂ε` term) that reduces **term-by-term to `J2Plasticity`** when N=0. **All 5 dimensional views** (3D/PlaneStrain/AxiSymmetric/PlateFiber/PlaneStress) in one class via a `dim` mode + `vmap` index table; PlaneStress/PlateFiber enforce σ₂₂=0 by nested eps₂₂ Newton + static 33-dof condensation in `Dtan` (mirrors J2PlaneStress). `(2/3)Cₖ` numerator ⇒ standard Chaboche `Cₖ,γₖ` (uniaxial plastic modulus `=C`, saturation `σ_y0+C/γ`). IMPL-EX = structure-only hook (committed `Δγⁿ` stored). `setResponse` exposes stress/strain/tangent/**backStress**/**plasticStrain**/**equivalentPlasticStrain**. **FINITE-STRAIN LIFT (#97):** the return map is extracted **verbatim** into a header-only, OpenSees-free **`LadrunoJ2Kernel.h`** (`integrate()` is now a thin pack/call/warn wrapper, bit-identical), so the SAME verified map serves both this small-strain material AND the finite-strain path — **`nDMaterial LogStrain $t $j2`** wraps LadrunoJ2 (it presents the exact `J2ThreeDimensional` inner interface the LogStrain plastic-inner protocol needs: engineering-shear in/true-stress out, linear elastic, stateful εᵖ subtraction, constant `getInitialTangent`) so **`LadrunoBrick -geom finite`** gets large-strain J2. **EXACT** for the isotropic spine (objective under any rotation); **combined-hardening large-rotation objectivity is the dSNPO §14.11 v2 boundary** — the kinematic backstress does NOT co-rotate in the LogStrain wrapper (it lives in the inner's fixed frame); the kernel extraction is precisely what enables that v2 (a finite-strain-NATIVE J2 reusing the kernel with a co-rotated α). Deferred: tabulated+Bézier iso, `prager_nl` oracle mode, IMPL-EX code, **§14.11 backstress co-rotation (combined-hardening finite v2)**, `LadrunoJ21D`. **LEMAITRE COUPLED DUCTILE DAMAGE `-damage lemaitre $r $s $pD $Dc` (+`-autoRegularization $lch_ref` crack-band) MODE (ADR [[15_lemaitre_ductile_damage_adr]], v1 MERGED PR #115, build-validated; IMPL-EX `-implex` in follow-up PR): new shared `LadrunoDamage.h` (`Y=(a q²+b σH²)/2E`, R_v triaxiality) + `returnMapDamaged()` in the kernel. KEY: effective return is D-independent ⇒ reuses `returnMap` VERBATIM, D recovered exactly, the (Δγ,D) coupling lives entirely in the consistent tangent `(1−D)Dtan−σ̃⊗∂D/∂ε`; default off ⇒ bit-identical, NO new classTag, NO vanilla edit. **IMPL-EX `-implex`** extrapolates the damage `D~=D_n+dD_n` (frozen over the global iteration, effective plastic return stays implicit ⇒ constant SPD tangent `(1−D~)Dtan_eff`, no softening Newton-stall; D committed implicitly) via the kernel `dScaleOverride` hook. Standalone `ladruno_damage_check.cpp` 15/15 (tangent vs FD <1e-5, lchScale, triaxiality, IMPL-EX); Zone-A `test_lemaitre_damage.py` V0–V9 (incl. IMPL-EX) + 3D↔1D V4 oracle — **GREEN on a real build: damage 11/11 + existing 18/18 (V0 no-regression)**.** Plan: [[10_ladruno_j2_plasticity]]. | Material | **33011** (ND band ≥33010, after LogStrain=33010) | `SRC/material/nD/{LadrunoJ2.{cpp,h},LadrunoJ2Kernel.h}`, `tests/{test_ladrunoJ2_material.py,test_ladrunoJ2_kernel_cpp.py,test_ladrunoJ2_finite.py,test_ladrunoJ2_finite_element.py,ladrunoj2_reference.py,ladrunoj2_kernel_check.cpp}` | shipped — impl stranded on docs-only #82, re-landed via #87; Zone-A battery **9/9** + kernel-extraction & finite-strain lift (#97): g++ kernel↔numpy-oracle + FD-tangent (4 paths), finite combined-hardening matches direct Box-14.4 chain, exact plastic incompressibility, **exact isotropic finite objectivity**, §14.11 combined-rotation boundary pinned (strict xfail); element acceptance (LadrunoBrick -geom finite) CI-gated | [#82](https://github.com/nmorabowen/OpenSees/pull/82) (docs), [#87](https://github.com/nmorabowen/OpenSees/pull/87) (impl), dims+setResponse #90/#93, kernel+finite-lift [#97](https://github.com/nmorabowen/OpenSees/pull/97) |

| **LadrunoJ2Finite** — finite-strain-NATIVE combined-hardening J2 (`nDMaterial`, a `FiniteStrainNDMaterial` subclass driven by `setTrialF(F)`). Crosses the dSNPO **§14.11 "v2" boundary** that the v1 `LogStrain`-over-`LadrunoJ2` path cannot: it OWNS its finite-strain state (`F_n, bᵉ_n, ε̄ᵖ_n`, and the Chaboche backstresses `α_{n,k}` as **SPATIAL** tensors) and **co-rotates the backstress** by the incremental material rotation each step before the return map — `α̃_{n,k} = R_Δ α_{n,k} R_Δᵀ`, `R_Δ = polar(f_Δ)`, `f_Δ = F F_n⁻¹` — so combined hardening is **frame-indifferent under large rotation** (v1 leaves α in a fixed frame ⇒ non-objective). Reuses the verified return map (`LadrunoJ2Kernel.h`) **verbatim** + the Hencky kinematics/spatial tangent (`LogStrainKernel.h`) + shared σ_y (`LadrunoHardening.h`); same `-iso voce / -kin N C γ …` parse as LadrunoJ2; for a pure rigid increment `α̃=QαQᵀ` (cancels the rotation of `s`), and as `f_Δ→I` reduces to the small-strain case exactly. **Consistent tangent = channel A (analytic log-strain `c=(1/2J)[D:L:B]`) + channel B (the backstress co-rotation `∂α̃/∂F`)** — channel B is small (~1e-4..2e-3 rel) but straddles the tangent-FD gate, recovered **ANALYTICALLY** (∂R/∂F polar-derivative via Sylvester + ∂τ/∂α̃ return-map sensitivity — no FD; numeric R-perturbation retained behind `-DLADRUNO_J2FINITE_CHANNELB_NUMERIC`) and mapped into the element `cmat` via `Ḟ=LF`: `cmatB_ijkl=Σ_m(∂σ_ij/∂F_km)F_lm` — NO kernel re-derivation. **IMPL-EX `-implex` (Oliver–Huespe–Cante; PR [#134](https://github.com/nmorabowen/OpenSees/pull/134)):** an explicit reporting path — the implicit co-rotated return map still runs and is COMMITTED (committed history byte-identical to fully-implicit), only the reported stress/tangent freeze the extrapolated multiplier `Δγ̃=Δγ_n` (uniform step) and the committed log-strain flow direction `N_n` **co-rotated by the SAME `R_Δ=polar(f_Δ)`** as the backstress: `εᵉ̃=εᵉᵗʳ−Δγ̃Ñ_n`, `σ̃=elastic-D:εᵉ̃/J` (frame-indifferent; the backstress drops out ⇒ reported tangent = the **constant SPD elastic** operator, no plastic h, no channel B; one-step lag at first yield). Default off ⇒ bit-identical; NO kernel change (flow dir recovered as `Δεᵖ/Δγ`). Mirrors the LadrunoJ2 damage IMPL-EX, frozen quantity = Δγ here. NON-goals (v1): plane-stress finite (§14.7), tabulated iso. Plan/ADR: [[16_finite_native_j2_adr]]. | Material | **33012** (ND band ≥33010, after LadrunoJ2=33011) | `SRC/material/nD/{LadrunoJ2Finite.{cpp,h}}` (reuses `LadrunoJ2Kernel.h`, `LogStrainKernel.h`, `LadrunoHardening.h`), `tests/{ladrunoj2_finite_native_reference.py,test_ladrunoJ2_finite_native_reference.py,test_ladrunoJ2_finite_native_tangent.py,test_ladrunoJ2_finite_native_steprefine.py,ladrunoj2_finite_native_check.cpp,test_ladrunoJ2_finite_native_cpp.py,ladrunoj2_implex_reference.py,test_ladrunoJ2_implex_reference.py,ladrunoj2_finite_implex_reference.py,test_ladrunoJ2_finite_implex_reference.py,ladrunoj2_finite_implex_check.cpp,test_ladrunoJ2_finite_implex_cpp.py,ladrunoj2_finite_channelB_reference.py,test_ladrunoJ2_finite_channelB.py,ladrunoj2_finite_channelB_analytic_check.cpp,test_ladrunoJ2_finite_channelB_cpp.py,test_ladrunoJ2Finite_element.py}` | shipped — numpy oracle objective to ~1e-12 + tangent recipe exact-to-FD (~1e-14); g++ composition vs oracle 1e-9; **IMPL-EX: committed history == implicit (byte), explicit→implicit order≈2 under refinement, objective 7e-13, constant SPD tangent, g++ implex-check vs oracle 1e-9**; element acceptance (no-rotation spine ≡ LogStrain, rotating-plastic-path objectivity vs oracle, channel-B tangent-FD at a rotated plastic state, rigid-rotation, reduce-to-small-strain; +adversarial-review hardening & g++ channel-B-tensor gate & serialization round-trip) | [#127](https://github.com/nmorabowen/OpenSees/pull/127), IMPL-EX [#134](https://github.com/nmorabowen/OpenSees/pull/134), softening-guard + implex-dt-doc + analytic-channel-B [#139](https://github.com/nmorabowen/OpenSees/pull/139) |

| **LadrunoUniaxialJ2** — uniaxial twin of `LadrunoJ2` (`uniaxialMaterial`, for fibers/truss/zeroLength): `σ=E(ε−εᵖ)`, `|σ−X|−σ_y(p̄)`, Voce+linear **isotropic** (shared verbatim with the 3D class via `LadrunoHardening.h` — the **V7 1e-12 oracle contract**) + **Chaboche** superposed AF **kinematic** (`X=ΣₖXₖ`, ships N≤8 via `-kin N C₁ γ₁ …`; `γₖ=0`⇒linear Prager, saturation `Cₖ/γₖ`). Return map = **fixed direction `s=sign(σ_tr−X_n)`** (the 1D relative stress cannot rotate ⇒ NO `M(Δγ)` tensor-direction solve like the 3D kernel) + **scalar Newton on `Δp̄`**; analytic tangent `E_alg=E·h/(E+h)`, `h=σ_y'+Σₖ aₖ/(1+γₖΔp̄)²`. Real `getInitialTangent`=E (fixes the `SimplifiedJ2` modal bug), real `revert*`/`setParameter`/`Print`, **no `exit()`**. `setResponse` exposes stress/strain/tangent/**backStress**/**plasticStrain**/**equivalentPlasticStrain**/**plasticWork** (the last three = the §6.1 fracture/LCF-wrapper hooks). **NOT a Steel02/Steel4 replacement** — it is (1) the verification oracle pinning the 3D Chaboche calibration under uniaxial stress and (2) a true multi-AF ratcheting fiber; the steel roadmap (fracture/LCF wrapper, local-buckling core flag, weld/HAZ) layers on top. `setParameter` dispatch left extensible for the future buckling-length key (`setTrialStrain` stays scalar — buckling needs geometry not richer strain). Deferred: tabulated iso (gated on the 3D one), the steel layers. **LEMAITRE DUCTILE DAMAGE `-damage lemaitre $r $s $pD $Dc` (+`-implex`) MODE (same shared `LadrunoDamage.h`, ADR [[15_lemaitre_ductile_damage_adr]], v1 MERGED PR #115; IMPL-EX follow-up PR): triaxiality≡1/3 ⇒ R_v≡1 ⇒ Y=σ̃²/(2E); the V4 3D↔1D 1e-6 oracle at triaxiality 1/3. Default off ⇒ bit-identical. Reuses the `Cwp` plastic-work accumulator as energy diagnostic. IMPL-EX (committed `CdD` increment, frozen `D~` ⇒ SPD tangent) build-validated (V9 refine→implicit).** Plan/ADR: [[13_ladruno_uniaxial_j2_adr]]. | Material | **MAT_TAG 33000** (first Ladruno *uniaxial*; 33000-band is per-registry, no collision w/ ELE/ND/INTEGRATOR 33000) | `SRC/material/uniaxial/LadrunoUniaxialJ2.{cpp,h}`, `SRC/material/nD/LadrunoHardening.h` (shared, also consumed by `LadrunoJ2`), `tests/test_ladrunoUniaxialJ2_material.py` | shipped — Zone-A battery **9/9** (elastic; **reduce-to-`HardeningMaterial`** 1e-9; monotonic kin≡iso; Bauschinger; AF saturation `σ₀+C/γ`; **V7 reduce-to-3D-`LadrunoJ2`** uniaxial-stress 1e-7 oracle; state recording; **stress-controlled ratcheting** single-AF constant-rate signature; **consistent-tangent FD** one-step-from-zero) + 3D `LadrunoJ2` regression unchanged **9/9** | [#99](https://github.com/nmorabowen/OpenSees/pull/99), polish [#102](https://github.com/nmorabowen/OpenSees/pull/102) |

| **LadrunoRCConcrete** — reinforced-concrete plastic-damage `nDMaterial` (Phase 1 of the RC shell stack, ADR [[19_ladruno_rc_shell_adr]]). Header-only OpenSees-free **`LadrunoRCKernel.h`** (clone of the `LadrunoJ2Kernel` "one core, many views" doctrine) reproducing the **ASDConcrete3D** plastic-damage spine VERBATIM (spectral effective-stress split + `recompose`, dual `dt`/`dc` scalar damage, Lubliner–Lee–Fenves biaxial envelope, `computePjj` 6×6 projectors via the compact `E[a]·(b<3?1:2)·E[b]` form, `HardeningLaw::evaluateAt` + the E-consistent `buildBackbone` c-tor/`adjust()` that caps every secant slope at E and forces the first segment elastic) PLUS the one Phase-1 physics addition — **MCFT compression softening on the STRENGTH axis** (`σ=(1−dt̄)ST+β(1−dc̄)SC`, `β=clamp(1/(0.8+170 ε₁),floor,1)`, ε₁=in-plane principal tensile strain; **Derivation B**, adversarially verified — β scales the assembled compressive cone ONLY, never the strain abscissa / equivalent-strain measure / effective stress / damage). `betaOn=false` ⇒ **byte-faithful to ASDConcrete3D**. ONE multi-dim class (3D/PlaneStress/PlateFiber via `dim`/`vmap` + `getType`/`getCopy`, the LadrunoJ2 pattern, NOT the ADR's three separate classes) — the shell path is `LayeredShellFiberSection → getCopy("PlateFiber")` (order 5, **guarded σ₃₃=0 Newton that propagates `STATUS_NO_CONVERGE`**, not ASD's silent return-0). ENGINEERING-shear convention (matches ASDConcrete3D + the OpenSees 3D nDMaterial interface), so the view does NO tensor↔engineering factor conversions. Projector-based secant tangent `W_B=I−dt̄·PT−(1−β(1−dc̄))·PC` + the `−170β²` β cross-term (fixed-projector v1, equibiaxial-blended). Drops into the **UNMODIFIED** `ASDShellQ4`+`LayeredShellFiberSection` (zero element/section edit). **A1 β-gate proven twice — numpy oracle (`tests/_testbed/rc_shell_ref.py`) + a standalone g++ build of the kernel: `\|σc\|/fc'=β` exact to 1e-6, the forbidden abscissa-insertion misses.** **PHASE 2a SHIPPED — fixed-crack aggregate-interlock shear BOUND (`-interlock`, default OFF ⇒ bit-identical to Phase 1):** freezes the in-plane crack NORMAL `(c,s)` at the first crossing of `eps_cr` (`-crackStrain`, default `ftmax/E`), then **CLIPS the already-assembled (damage-reduced) crack-plane shear** `τ_sm = m_σ·sig_ip` to `±v_ci,max` (formulation B — NOT a bare-elastic substitution; the bare-elastic-then-cap form was REJECTED in adversarial review for a cracking-instant discontinuity + tangent inconsistency). `v_ci,max = 0.18·√fc'/(0.31+24w/(a_g+16))` (Vecchio–Collins), `w = macauley(eps_n)·s_theta` (eps_n normal to the FROZEN plane; = ε₁ at capture) grown **monotonically**; `s_theta`=`-crackSpacing`→`lch`→1, `a_g`=`-agg` (16 mm), residual floor `-betaSrMin`. Below the cap the stress is UNCHANGED (continuous, no double-count). **Consistent tangent:** sub-cap ⇒ baseline; capped ⇒ rank-1 removal `Dtan_ip −= (1−betaSrMin)·m_ε⊗(m_σ·Dtan_ip)`, exact because `m_σ·m_ε=(c²+s²)²=1` pins the crack-shear (the `v_ci,max(w)` normal→shear coupling is a deliberately-omitted 2nd-order term, matching the Phase-1 secant). New `RCHist` state `{cracked,crackC,crackS,wmax,betaSr}` (+5 doubles) + 8 Params scalars folded into sendSelf/recvSelf (`RC_DATA` 226→239). `setResponse` adds `betaShear`/`crackState`. **MCFT `v_ci,max` is SI-unit-bound (N,mm)** — see [[LEDGER_quirks]]. **Hardened via a 59-agent adversarial review** (0 critical; the major tangent-consistency + discontinuity cluster drove the switch to formulation B; off-axis-crack + classTag + parser-guard fixes folded in). **PHASE 2b.1 SHIPPED — cyclic crack-shear via incremental friction-slip (`-cyclic` sub-flag, default OFF ⇒ 2a byte-identical):** the crack-plane shear becomes an INDEPENDENT incremental friction state (FSAM `InterLocker_improved` crib) `τ_cr=clamp(τ_cr,committed + G·(γ_nt−γ_nt,committed), ±v_ci,max(w))`, `G=E/2(1+ν)`, slip origin frozen at capture; `w` now REVERSIBLE (current opening, not the monotone max) ⇒ crack closure raises the cap (capacity recovery). State += `{tauCr,gammaCr}` (+2 doubles); Param += `interlockCyclic` (RC_DATA 239→242); `crackShear` response added. Consistent tangent = remove smeared `m_ε⊗(m_σ·D)` + add friction `G·m_ε⊗m_ε` (floored `betaSrMin·G` when capped); standalone FD gives `Dtan[3][3]=G` on the elastic-unload branch. Cap stays MCFT `v_ci,max(w)` (Coulomb `μσ_n` deferred). **PINCHING is a PANEL phenomenon (2b.2):** a material point at fixed crack + constant normal strain gives a FAT loop (the `±v_ci/G` elastic band is sub-step); a pinched waist needs crack open/close during the cycle (principal rotation), only in a real panel. **PHASE 2b.2a SHIPPED (test-only, no kernel change)** — proves the material in its REAL host: `LadrunoRCConcrete` end-to-end in **`ASDShellQ4`+`section LayeredShell`** (PlateFiber view + σ33 Newton; until now only stdBrick). New `tests/test_ladrunoRCConcrete_shell.py` (Zone-A, no gmsh): membrane tension cracks+softens (`Nxx` peaks `ft·h`); **cyclic membrane shear caps `Nxy` at `±v_ci,max·h` both signs** (PlateFiber×condensation×cyclic in the real shell); interlock load-bearing. + serialization round-trip test (cracked cyclic state survives FE-database `save`/`restore`). **PHASE 2b.2b SHIPPED — second orthogonal crack (X-cracking) + cyclic interlock-surface wear (`-xcrack`, default OFF ⇒ 2b.1 byte-identical):** crack 2 (orthogonal normal `(−s,c)`) freezes when the strain normal to that plane reaches `eps_cr`; the cap then uses the GOVERNING (most-open) crack opening ⇒ BIDIRECTIONAL capping (single crack only capped one shear direction). Cyclic wear = irreversible Archard knockdown `kn=max(degMin,1−degKappa·clamp(slipCum/degSlipRef,0,1))` driven by CUMULATIVE crack sliding `slipCum` (peak-slip saturates cycle 1; cumulative gives gradual decay 2.90→2.50→2.10 over 3 cycles). Single friction state + governing cap ⇒ NO double-count (the design-workflow's two-state SUM double-counted `≈2·v_ci,max`; corrected). Tangent-neutral wear (committed slipCum) ⇒ benign 2b.1 tangent unchanged. Serialization +schema-version field (hard-checked) + `{cracked2,slipCum}` + 4 Params; `xcrackState` response. Designed via a 3-formulation design→critique workflow. PINCH waist still a panel effect (deferred). **PHASE 4a SHIPPED — IMPL-EX (`-implex` [`-implexAlpha a`] [`-implexControl tol redLim`], default OFF ⇒ implicit/byte-identical, no new classTag):** pulled forward from Phase 4 because the cyclic-softening panel validation (2b.2c) will NOT converge with vanilla Newton (implicit consistent tangent goes indefinite on the softening branch; `LadrunoRCConcrete` had no implex while ASDConcrete3D does — empirically falsified on a meshed squat panel). Two-phase scheme mirroring `ASDConcrete3DMaterial::compute(do_implex,…)`: `setTrialStrain` runs the EXPLICIT pass (damage thresholds xt,xc + MCFT β from `x_ext=x_n+tf·(x_n−x_{n-1})`, FROZEN ⇒ secant tangent, no softening-rate/no β cross-term); `commitState` re-integrates IMPLICITLY at the converged strain to advance the TRUE thresholds, measure `implexError`, roll n→n-1. RCHist += `{xt_old,xc_old,eps1_old}`; Params += implex config (propagated by getCopy); runtime += `{dtime_n,dtime_n_commit,dtime_0,commitDone,implexError}`; serialization **schema v1→v2** (RC_DATA 242→262, hard-checked); `implexError` response (id 11). `implexTimeFactor()` GUARDS static-analysis `ops_Dt` (load-factor pseudo-time erratic/resets at `loadConst` ⇒ raw ratio detonates the extrapolation): fall back to `α` when dt≤0/non-finite, clamp to `2α`. **3-agent adversarial review (0 bugs in state-machine + serialization; scalar counts balance at 262): fixed (BUG-2) `-numericalTangent` FD path bypassed under implex (mixed explicit-base+implicit-columns = O(implexError/h) garbage, mirrors ASD `tangent && !implex`); (BUG-1) corrected a header overclaim — PT/PC projectors NOT frozen (consistent w/ the material's fixed-projector-secant philosophy; DAMAGE is frozen = the robustness that matters), full PT-freeze = scoped follow-up.** Exact on proportional paths (error ~1e-12), O(Δt) lag at rate changes. `tests/test_ladrunoRCConcrete_implex.py` Zone-A 6/6 (off-identical, tracks-implicit, error-active-on-rate-change, SPD secant, FD-bypass equivalence, save/restore continuation). **PHASE 2b.2c.1 SHIPPED — `-shearRetention {mcft|const|dsfm|rots}` crack-shear retention curves (default `mcft` ⇒ 2b.2b byte-identical):** wires the long-reserved `shearRetMode` (parser had only a NOTE placeholder) into a flag selecting the CYCLIC crack-plane slip stiffness `G_slip` in the friction predictor — `mcft`=full `G` (shipped), `const`=`μ·G` (`-shearRetFactor`, default 0.4; Rots/Červenka), `dsfm`=`G·(0.31/denom)` width-degraded (DSFM), `rots`=rotating-coaxial ⇒ fixed-crack shear skipped ≡ interlock-OFF. The `v_ci,max(w)` CAP is unchanged in every mode; all modes reduce to `mcft`. `const`/`dsfm` imply `-interlock -cyclic`, `rots` implies `-interlock`. Param += `shearRetFactor`; serialization **schema v2→v3** (hard-checked, rejects v2). Zone-A material 25/25 (const(μ=1)≡mcft to 1e-9; const unload==μ·G; dsfm unload==G·0.31/denom closed-form; cap still ±v_ci,max; rots≡interlock-off; C++≡numpy oracle over a reversing const path) + full RC suite 35/35, no 2a/2b regression. **PHASE 2b.2c.2 SHIPPED (test-only, no kernel change) — rigid-rotation objectivity gate** (keystone acceptance, deferred since 2a): the constitutive frame-indifference identity `σ(Q E Q^T)==Q σ(E) Q^T` over an arbitrary cracked/cyclic/X-cracked history is the property that makes the corotational-element route (the ADR's supported large-rotation path; it feeds the material the de-rotated `Q^T E Q`) objective for the DIRECTIONAL crack/interlock state. `tests/test_ladrunoRCConcrete_objectivity.py` drives the homogeneous Penalty `stdBrick` probe through tension-then-reversing-shear in the reference frame AND a frame rigidly rotated about z by 30°/90°/127° (+63° with `-xcrack`+wear), prescribing the full symmetric strain `u=E·X` from rotated basis tensors `Q A Q^T`/`Q B Q^T`; asserts the trajectories coincide after the Q-transform to `<1e-5·peak`. **PASS 4/4, NO kernel change** — objective BY CONSTRUCTION (crack normal captured from the strain principal dir co-rotates; `m_ε`/`m_σ` built from it co-rotate; the friction predictor/`v_ci,max`/wear use frame-invariant scalars `g_nt`/`e_n`/`slipCum`). Discharges the ADR Zone-A objectivity item (a) (corotational route → PASS); the §14.11 `setTrialF` material-view xfail (item b) is unchanged (Phase 4). **PHASE 2b.2c.3 RESOLVED by verification (test-only, no kernel change) — crack-closure on the NORMAL direction is ALREADY correct in the cloned spine:** `ASDConcrete3D`'s `StressDecomposition` recomputes the spectral T/C split every step from the live effective stress with independent `dt`/`dc` and `cdf=0` — that per-step recompose IS unilateral normal crack closure (tensile `dt` does not bleed into the compressive cone, so a closing crack recovers full compression). The fixed-crack addition is correctly **shear-only**; adding a separate fixed-crack normal reassembly would double-count the recompose. 4 new Zone-A gates in `test_ladrunoRCConcrete_material.py`: (i) crack in tension then compress past peak ⇒ compression FULLY recovers (== virgin compression run); (ii) crack→close→reopen ⇒ reopened tension follows the DAMAGED envelope ≪ virgin elastic (tension damage irreversible); (iii) same full recovery with the fixed-crack interlock ON (freezing the normal does not corrupt closure). Honest caveat: this is the spectral (rotating) closure the objectivity gate showed is frame-indifferent; a *directional* fixed-crack normal traction–separation law is a separate deliberate future addition, not needed for the membrane-shear physics. **Cyclic material physics is now COMPLETE** (compression softening · interlock bound · cyclic friction-slip · X-cracking+wear · retention curves · IMPL-EX · objectivity · crack closure). **PHASE 2b.2c.4 — Tran–Wallace squat-wall pinching: HARNESS built, validation deferred.** `tests/_testbed/rc_wall_harness.py` (NOT a pytest gate — under `_testbed`, no `test_` prefix): a structured `NX×NY` ASDShellQ4 grid on a `LayeredShell` = 4 `LadrunoRCConcrete` concrete layers (full cyclic stack `-beta -lublinerReduced -interlock -cyclic -implex`) + 2 smeared `PlateRebar(Steel02)` web-steel layers (no gmsh, env-portable). Status: assembles + runs + dissipates on the first drift cycle (`V≈±146 kN` at 0.3 mm, closed-loop area>0), WALLS on convergence at ~0.6 mm+ (the classic cyclic-softening RC-wall barrier). **PHASE 2b.2c.4a SHIPPED — deferred item (1) RESOLVED via quasi-static EXPLICIT (`CentralDifferenceLadruno`):** the fork's monotonic solvers (ArcLength/IndirectControl/DR/robust_drive) all trace ONE equilibrium path — a load REVERSAL isn't one, so none do cyclic; the cyclic tool is EXPLICIT (forms NO tangent ⇒ the indefinite-softening-tangent stall that walls the implicit harness at 0.6 mm doesn't exist). A gmsh-meshed 4×3 squat wall (`-beta -interlock -cyclic -xcrack`) **completes the full ±8 mm reversing schedule** (15000 steps, ~1.8 MN peak, physical); the implicit barrier is gone. PANEL-SCALE ABLATION (load-bearing proof): cyclic friction-slip + X-crack wear ON ⇒ **~28% LESS hysteretic energy + ~11% lower peak** vs the monotone `-interlock` bound (invisible at single-element scale = concrete-damage-dominated; clear at mesh scale once principal rotation engages the interlock). `tests/test_ladrunoRCConcrete_wall.py` (Zone-B, gmsh): (i) explicit completes cyclic softening; (ii) cyclic degradation load-bearing. EXPLICIT GOTCHAS (LEDGER_quirks): element mass via material `-rho` (not nodal); **ASDShellQ4 has NO per-element `dt_cr`** (`criticalTimeStep()`=-1) ⇒ manual `dt≈0.2·h/√(E/ρ)`; NO equalDOF (stability ignores constraints — prescribe rigid-top drift via per-node `sp`); mass-proportional damping only. Still deferred: (2) calibrate to **Tran–Wallace RW-A20-P10** (or a PEER squat-wall), assert pinching SHAPE + cumulative hysteretic ENERGY vs measured loops; (3) optional gmsh graded mesh + boundary elements. NEXT: Phase 3 tension-stiffening + directional lch, Phase 4 finite-strain view, Phase 5 `LadrunoSolidShell` (punching). Plan/ADR: [[19_ladruno_rc_shell_adr]] (Phase 2a + 2b.1 + 2b.2a + 2b.2b + 2b.2c subsections). **USER/DEV GUIDE: [[LadrunoRCConcrete_guide]]** (command grammar, the four physics layers, the views/shell seam, recording, honest limits). **PHASE 3a SHIPPED — tension stiffening (`-tensStiff {vc|cm}` [`-tensStiffC c`] [`-tensStiffAlpha a`], default OFF ⇒ baseline-identical):** an opt-in stress FLOOR on the LIVE in-plane principal tensile axis `p1` — inject `Δ=σ_ts(ε1)−n^Tσn` along `p1` (rank-1, only when `Δ>0`), active ONLY post-crack (`ε1≥ε_cr`) so the elastic pre-crack branch is untouched. `σ_ts=ft/(1+√(c·ε1))` (`vc`/Bentz, `c`=`-tensStiffC` default 500, must be >0) or `α·ft/(1+√(500·ε1))` (`cm`/Collins–Mitchell, `α`=`-tensStiffAlpha` default 1); `vc(c=500)≡cm(α=1)`. `ft`=tension-backbone peak (`ftPeak`, cached by setupParams + serialized — recvSelf does NOT call setupParams). `ε1`=COMPOSITE membrane principal tensile strain (same ε1 the MCFT β uses — perfect-bond compatibility). EQUIBIAXIAL (degenerate `p1`): floor BOTH in-plane normals to `σ_ts` (inject `g` to each, `ts_inj=(1,1,0)`, `ts_meas=(0.5,0.5,0)` ⇒ self-consistency `ts_meas·ts_inj=1`; the naive rank-1 `(0.5,0.5)` reuse reaches only HALFWAY — a 59-agent-review-class bug caught here by the equibiaxial gate). Consistent tangent (tangentMode 0, !implex): `ts_inj⊗(dσ_ts/dε1·de1/dε − d(n^Tσn)/dε)`, spanning ALL 6 columns of the in-plane rows (the floor pins `n^Tσn` independent of bare stress ⇒ removes its eps_zz column too — FD-caught), `dp1/dε` omitted (fixed-projector secant, like β). IMPL-EX: `ε1` frozen from extrapolated committed eps1 (like β), tangent cross-term dropped. Params += `{tensStiffMode,tensStiffC,tensStiffAlpha,ftPeak}` (+4 scalars); serialization **schema v3→v4** (hard-checked, rejects v3). **MONOTONIC-scope (v1):** `σ_ts(live ε1)` re-inflates on unload (no `ε1max` memory) ⇒ use for pushover; combined TS+interlock validated for proportional (non-rotating) loading (TS uses live `p1`, interlock uses frozen crack ⇒ axis rotation leaks shear onto the frozen plane). Gated 3 ways: numpy oracle T1 (uniaxial closed-form `σ_xx==σ_ts(ε1)`, 359 binding steps) + standalone g++ (`tests/_testbed/rc_tensstiff_gpp.cpp`: floor + equibiaxial-both-normals at 1e-16 + FD tangent on the pinned direction) + OpenSees Zone-A 11/11 (`tests/test_ladrunoRCConcrete_tensstiff.py`: pre-crack untouched, closed-form floor, above-bare, vc≡cm, cm(α), equibiaxial, PlateFiber-shell `Nxx==σ_ts·h`, TS+interlock proportional, load-unload-tracks-live-ε1, schema-v4 round-trip). Hardened via a 3-agent adversarial review (degen 2× under-delivery + `-tensStiffC>0` guard + PlateFiber/interlock/cm/unload coverage gaps folded in), then a **38-agent find→verify workflow review** (9 findings, 4 confirmed, all TEST-COVERAGE not correctness): wired the g++ FD tangent gate into CI via `test_ladrunoRCConcrete_tensstiff_cpp.py` (the only TS-tangent check — a converged static stress is tangent-independent), added the degen-tangent analytic cross-check to the g++ gate, parser-negative tests (`-tensStiffC<=0`, bad mode), NON-default serialization round-trips (c=2000, cm α=0.6 — catch dropped wire slots), an IMPL-EX+TS test, a non-proportional TS+interlock robustness test, and a cm-mode `-tensStiffC` footgun WARNING. Zone-A now 58/58. **PHASE 3b SHIPPED — crack-band (Bažant–Oh) regularization (`-autoRegularization $lch_ref`, default OFF ⇒ baseline-identical):** faithful clone of `ASDConcrete3D`'s opt-in regularization — `fractureEnergy` (specific area under softening) + `regularize` (iterative post-peak strain rescale so `g_reg=G_f0·(lch_ref/lch)` ⇒ physical `g_reg·lch` MESH-OBJECTIVE) + `adjustBackbone` re-enforcement (E-cap, monotone plastic strain, non-decreasing damage), all in `LadrunoRCKernel.h`. Latch `lch=ops_TheActiveElement->getCharacteristicLength()` (EAS-aware) or `-lch` override ONCE at first `setTrialStrain`; each GP/layer copy regularizes with its OWN element lch. **D5 Option A** (scalar in-plane lch, ZERO vanilla edit). **LOUD FAILURE** (no silent fallback) if autoReg on but no lch resolvable; the failure does NOT latch (so a step-retry can't silently proceed un-regularized). `getCopy` PROPAGATES the latch (mirror ASDConcrete3D copy-ctor — else a copy of a regularized instance double-scales). schema v4→v5. Params += `{autoReg,lchRef}`, runtime += `{regularizationDone,regLch}`. Gated 3 ways: numpy oracle R1 (`g_reg·lch` const across lch) + standalone g++ `rc_reg_gpp.cpp` (energy-objectivity + lch==lch_ref no-op + steep-damage plastic-strain monotonicity) + CI wrapper + OpenSees Zone-A 5/5 (`tests/test_ladrunoRCConcrete_reg.py`: energy×lch objectivity lch=50/25/12.5, no-op reduce, element-lch path, parser guard, schema-v5 round-trip). **3-agent adversarial review caught: (MAJOR) the missing `adjust()` re-enforcement → post-peak plastic strain went non-monotone, q/damage off ~6% on steep backbones, energy-invisible to all gates → FIXED (adjustBackbone in the scaling loop + after); (Med) `getCopy` double-regularize → latch propagated; (Med) loud-fail self-latch → don't latch on failure.** **PHASE 3b-struct SHIPPED — structural mesh-objectivity gate (test-only, no kernel change):** the axis-aligned Bažant tension-bar — a row of `ASDShellQ4` with a 10%-thinner LEFT band to localize the crack deterministically (thickness not material ⇒ backbone + regularization stay identical and the band factor cancels between meshes), the SAME 100×50 membrane specimen meshed 2×1/4×2/8×4 (`lch`=50/25/12.5, each element latching its OWN `getCharacteristicLength()` via the `getCopy` latch, NO `-lch`), driven to full tensile softening in STATIC displacement control — dissipates a MESH-OBJECTIVE total energy (reg-ON energy ratios 1.000/1.000/1.000 across the 4× refine, peak within 0.7%), while the un-regularized NEGATIVE CONTROL spuriously HALVES (fine/coarse 0.50 — the band is one element wide) and the un-regularized FINEST mesh even snap-back-DIVERGES (brittle localization — the disease itself). Proves the `g_reg·lch` mechanism STRUCTURALLY (the reg unit battery only proved it at a material point). `tests/test_ladrunoRCConcrete_meshobj.py` Zone-A 4/4. STILL STAGED: the inclined-crack / notched panel (snap-back-prone Zone-B; additionally surfaces the single-scalar-`lch` √2 mis-regularization of ~45° struts, an Option-A residual). Phase 3 now COMPLETE → next Phase 4b finite view, Phase 5 solid-shell. | Material | **33015** (ND band; ONE multi-dim class — 33013 InitDefGrad, 33014 StagedStrain took the earlier tags) | `SRC/material/nD/{LadrunoRCKernel.h,LadrunoRCConcrete.{cpp,h}}`, `tests/test_ladrunoRCConcrete_material.py`, `tests/test_ladrunoRCConcrete_shell.py`, `tests/test_ladrunoRCConcrete_implex.py`, `tests/test_ladrunoRCConcrete_objectivity.py`, `tests/test_ladrunoRCConcrete_wall.py` (Zone-B), `tests/test_ladrunoRCConcrete_tensstiff.py`, `tests/test_ladrunoRCConcrete_tensstiff_cpp.py` (compiles+runs the g++ tangent gate in CI), `tests/test_ladrunoRCConcrete_reg.py`, `tests/test_ladrunoRCConcrete_reg_cpp.py`, `tests/test_ladrunoRCConcrete_meshobj.py`, `tests/_testbed/rc_shell_ref.py`, `tests/_testbed/rc_tensstiff_gpp.cpp`, `tests/_testbed/rc_reg_gpp.cpp` | Phase 1 built + Zone-A 4/4 (#155/#192). Phase 2a built + Zone-A 8/8 (#239). **Phase 2b.1 built + Zone-A 14/14** (+cyclic reversal re-caps ∓v_ci,max + CLOSED-loop energy dissipation; unload stiffness == G + closed-form elasto-friction line; crack-closure cap recovery; monotonic == actual 2a-run plateau; C++≡oracle over a reversing path; **assembled-in-OpenSees tangent Dtan[3][3]==G sliding / betaSrMin·G capped**); standalone g++ FD confirms cyclic tangent. **Hardened via a 39-agent adversarial review (0 correctness bugs — friction law, cyclic tangent, σ33-Newton purity, serialization all re-derived clean): folded in capture-step stress CONTINUITY (seed tauCr=clip(τ_sm)), `-cyclic`⇒`-interlock` implication, cap-utilization betaShear in cyclic, closed-loop energy + closed-form + assembled-tangent tests, FSAM-divergence ADR note.** Deferred (noted): serialization schema-version field (pre-existing since 2a) | [#155](https://github.com/nmorabowen/OpenSees/pull/155), [#192](https://github.com/nmorabowen/OpenSees/pull/192), Phase 2a [#239](https://github.com/nmorabowen/OpenSees/pull/239), Phase 2b.1 [#245](https://github.com/nmorabowen/OpenSees/pull/245), Phase 2b.2a [#246](https://github.com/nmorabowen/OpenSees/pull/246), Phase 2b.2b [#253](https://github.com/nmorabowen/OpenSees/pull/253), Phase 4a IMPL-EX [#263](https://github.com/nmorabowen/OpenSees/pull/263), Phase 3a tension stiffening [#273](https://github.com/nmorabowen/OpenSees/pull/273), Phase 3b crack-band lch [#277](https://github.com/nmorabowen/OpenSees/pull/277), Phase 3b-struct mesh-objectivity gate [#281](https://github.com/nmorabowen/OpenSees/pull/281) |
| **LadrunoRCFiniteStrain** — finite-strain (Hencky) view of the RC plastic-damage material ([[19_ladruno_rc_shell_adr]] Phase 4b; `nDMaterial`, a `FiniteStrainNDMaterial` subclass driven by `setTrialF(F)`, consumed by `element LadrunoBrick -geom finite`). Seam: `B=F Fᵀ` → `εᵉ=½ln B` → the SHARED `LadrunoRCKernel::returnMap3D` (verbatim) → Cauchy `σ=τ/J` + spatial tangent `c=(1/2J)[D:L:B]` (`LogStrainKernel.h`). **WHY a native subclass and NOT the generic `LogStrain` wrapper:** the RC spine is a plastic-DAMAGE law (stiffness degrades), and `LogStrainNDMaterial` commits `bᵉ` by recovering `εᵉ=C₀⁻¹:τ` through the inner's INITIAL tangent — exact only for a non-degrading inner; for a damage inner that recovery is shrunk by `(1−d)` so the committed `bᵉ` drifts (see [[LEDGER_quirks]]). The native view avoids it: the RC kernel carries NO tensorial plastic strain (inelasticity = scalar damage thresholds xt/xc + effective-stress recompose; `returnMap3D` is pure in the TOTAL strain), so the elastic left Cauchy–Green is just the TOTAL `B=F Fᵀ`, recomputed from the element's F each step — there is no `bᵉ` to track or mis-commit; the committed state is the SAME `RCHist` as the small-strain class. ALL RC physics flags (`-beta/-interlock/-cyclic/-xcrack/-shearRetention/-tensStiff/-autoRegularization/-implex`) ride the shared kernel UNCHANGED; **IMPL-EX carried through** (explicit-extrapolate thresholds in setTrialF, implicit re-integrate + roll at commit). **OBJECTIVITY (§14.11 split):** the isotropic-damage spine is objective by construction (`B=F Fᵀ` co-rotates; spectral split + scalar thresholds frame-indifferent) — VERIFIED `σ(QF)==Qσ(F)Qᵀ` at two rotation magnitudes; the fixed-crack normal / interlock direction are DIRECTIONAL internal variables in a fixed frame ⇒ NOT co-rotated by the `setTrialF` material view ⇒ a cracked point is not frame-indifferent under large rotation (documented boundary; the objective route is the corotational ELEMENT, validated for the small-strain material in `test_ladrunoRCConcrete_objectivity.py`; a co-rotating-crack finite-native view to flip it is deferred — the RC analog of LadrunoJ2Finite's channel-B). NON-goals (v1): co-rotating directional crack; no separate PlaneStress/PlateFiber finite view (3D-only, for the solid-shell / `-geom finite`). | Material | **33018** (ND band; 33016 reserved LogStrain2D, 33017 reserved LadrunoConcrete3D) | `SRC/material/nD/{LadrunoRCFiniteStrain.{cpp,h}}` (reuses `LadrunoRCKernel.h`, `LogStrainKernel.h`, `FiniteStrainNDMaterial.h`), `tests/test_ladrunoRCFiniteStrain.py` | shipped — Zone-A 9/9: reduce-to-small-strain, ELASTIC tangent==FD (validates the spatial-tangent seam; the damage tangent is a deliberate secant ⇒ FD gate stays elastic), patch `σ==(small-strain RC at ½lnB)/J` (stress seam through damage — same-binary cross-check, no numpy oracle), rigid-rotation stress-free, isotropic objectivity ×2 rotations, IMPL-EX tracks implicit, det F≤0 guard, DB round-trip. Full RC Zone-A 77/77, no regression | Phase 4b [#282](https://github.com/nmorabowen/OpenSees/pull/282) |

| **InitDefGradNDMaterial** (`nDMaterial InitDefGrad`) — multiplicative, finite-strain staged-activation wrapper: the large-strain sibling of `InitStrainNDMaterial`. Makes a finite-strain continuum element **born stress-free at the deformed geometry** when appended mid-stage (staged construction). Wraps an inner `FiniteStrainNDMaterial` (e.g. `LogStrain`) and **IS-A** `FiniteStrainNDMaterial`, so `LadrunoBrick -geom finite` (and any future F-based solid) consumes it with **zero element edits** (passes the element's `dynamic_cast<FiniteStrainNDMaterial*>`). At activation it captures the **birth deformation gradient `F0`** and thereafter forwards the **relative** gradient `F_rel = F·F0⁻¹` to the inner — the multiplicative split `F = F_rel·F0` (Lee). **OBJECTIVE** where the additive `InitStrain` offset is not: under a post-birth rigid rotation `F→Q·F`, `F_rel→Q·F_rel` so `C_rel=F_relᵀF_rel` is unchanged ⇒ zero spurious stress (subtracting `u0` gives `F_naive=F−F0+I=Q·F0−F0+I≠I`). **KEY simplification — the shipped seam is SPATIAL, not PK1:** (1) the incremental gradient `F_Δ=F_rel·F_rel,n⁻¹=F·F_n⁻¹` is **invariant to `F0`** (F0 only sets the stress-free initial state, never the step response); (2) the inner returns true **Cauchy σ** (reference-independent), so **no `P=P_rel·F0⁻ᵀ` push-back and no `J0` integration weight** are needed (the element already integrates `∫bᵀσ dv` on the current config with its own `det F`; the inner's `getJ()=J_rel=det F/det F0` is unused by assembly) — the (PK1-dialect) design note's energy-consistency bookkeeping is **subsumed**. So the wrapper is a near pass-through: capture `F0` once, forward `F_rel`, delegate stress/tangent/state. **Trigger = AUTO-CAPTURE on the first `setTrialF`** (= the birth gradient, since `addElement→setDomain→update` evaluates at the committed deformed state; each mid-stage append gets a fresh wrapper ⇒ multi-lift falls out free) — Truss `initialDisp` lifted to a full 3×3. Opt-out `-noInitF` (pass-through ≡ bare inner); explicit `-F0 f11..f33` for a known birth; `setResponse("F0")` reports the captured gradient. Degenerates to identity at `t=0` (`F0=I`). For **small-strain** elements the additive `InitStrain` offset is still correct + cheaper — this earns its keep only on `-geom finite`. Design note: [[staged_deformation_gradiend]] (+ [[constraints_reference_position]] SP/MP/element reference-frame trichotomy). | Material | **`ND_TAG_InitDefGradNDMaterial` 33013** (ND band ≥33010, after LadrunoJ2Finite=33012) | `SRC/material/nD/InitDefGradNDMaterial.{cpp,h}`, `tests/test_initDefGrad_material.py` | shipped — Zone-A battery: `-noInitF` pass-through ≡ bare inner (stress+force), explicit-`F0` stress-free birth, **post-birth rigid-rotation objectivity** (the additive-offset failure mode), reduce-to-relative (`InitDefGrad(-F0=G)` at `F=M` Cauchy σ == bare inner at `F=M·G⁻¹`), **wrapped consistent-tangent == FD of force** (the wrapper preserves Newton), **genuine two-phase staged construction** (deform+commit holder → append auto-capture brick on same nodes → born ~zero force, holder unchanged, reported `F0`==deformed gradient) | [#163](https://github.com/nmorabowen/OpenSees/pull/163) (merged), manifest backfill [#167](https://github.com/nmorabowen/OpenSees/pull/167) |
| **StagedStrainNDMaterial** (`nDMaterial StagedStrain`) — **small-strain, dimension-general (2D+3D), AUTO-CAPTURING** staged-activation wrapper: the small-strain sibling of `InitDefGrad`. Makes a **small-strain** continuum element (`FourNodeQuad`, `stdBrick`, plane-strain/stress, axisymmetric, tris/tets) **born stress-free at the deformed geometry** when appended mid-stage. **Fixes the three ways the upstream `InitStrain` bites staged use:** (1) `InitStrain` is **3D-only** (`getOrder()` hardcoded 6) and `exit(-1)`s on a non-3D inner (kernel kill) → `StagedStrain` is **order-general** (`ε0` sized to `inner->getOrder()`; `getCopy(type)` adapts the inner to PlaneStrain/PlaneStress/AxiSymm/3D) and **fails gracefully**; (2) `InitStrain`'s `ε0` is **fixed** → `StagedStrain` **auto-captures `ε0`** on the first `setTrialStrain` (= birth), so **one tag births a whole lift**, each per-GP copy snapshotting its own birth strain; (3) `InitStrain` **adds** a prestrain → `StagedStrain` **subtracts** the captured birth strain (`ε_rel = ε − ε0`) for stress-free birth. Born **genuinely virgin** (zero stress AND zero plastic history); tangent is an **exact passthrough** (`ε0` constant ⇒ no FD gate, unlike the finite wrapper). Additive ⇒ **exact for small strain only** (finite uses `StagedDefGrad`/`InitDefGrad`). **COMPOSES with the upstream `InitStrain`** for staged-birth-plus-prestrain by nesting `StagedStrain` **OUTERMOST**: `element → StagedStrain(InitStrain(real, ε_pre))` ⇒ born carrying exactly `σ(ε_pre)` (nest the other way and the prestrain is captured into `ε0` and cancels). Opt-out `-noInit` (≡ bare inner); explicit `-eps0 e1 e2 …`; `setResponse("eps0"/"totalStrain")`. **Also registers the `StagedDefGrad` alias for `InitDefGrad`** so the `Staged*` family reads coherently (`StagedStrain` small / `StagedDefGrad` finite). Design note: [[staged_deformation_gradiend]] (+ [[constraints_reference_position]]). | Material | **`ND_TAG_StagedStrainNDMaterial` 33014** (ND band ≥33010, after InitDefGrad=33013) | `SRC/material/nD/StagedStrainNDMaterial.{cpp,h}`, `tests/test_stagedStrain_material.py` | shipped — Zone-A 2D+3D battery: `-noInit` ≡ bare (quad+brick), two-phase staged stress-free birth (`FourNodeQuad` 2D + `stdBrick` 3D), dimensional sweep (PlaneStrain/PlaneStress/AxiSymm/3D capture at the right order), reduce-to-relative, tangent==inner, J2 born-virgin (no inherited plastic strain), **composition `StagedStrain∘InitStrain` → `σ(ε_pre)`**, graceful-fail where `InitStrain` `exit(-1)`s, `sendSelf`/`recvSelf` round-trip | [#182](https://github.com/nmorabowen/OpenSees/pull/182) (small-strain 2D+3D), review fixes [#183](https://github.com/nmorabowen/OpenSees/pull/183) |
| **LadrunoIMKBeam** — 3D concentrated-plasticity beam-column macro element with **uncoupled** moment-rotation IMK rotational hinges (one independent uniaxial law per bending axis, per end). Built to dodge the rigid-diaphragm spurious-axial-`P` failure of fiber-hinge `forceBeamColumn`+`HingeRadau` (fiber hinge couples P-M → contaminated moment capacity + fragile solver). **Displacement-driven stiffness formulation**: basic 6×6 `[N,Mz_i,Mz_j,My_i,My_j,T]`, exact series `F = F_elastic + F_hinge` (**no n-factor** — that was a two-element-Python-recipe hack), each bending axis a tiny **2×2 internal Newton** on hinge rotations (`θ_h = v − F_el·M(θ_h)`, `J = I + F_el·diag(k_t)`); softening `k_t<0` resolved at element level. **Material-agnostic** — takes pre-built `uniaxialMaterial` moment-rotation tags (`-matZ`/`-matY`, `-hinge both\|i\|j`; **per-end asymmetric `-matZi`/`-matZj`/`-matYi`/`-matYj`** override the symmetric law and are independent of `-hinge`), so steel (`IMKBilin`/`Bilin`) and concrete (`ModIMKPeakOriented`/pinching) differ only by tag, and the two beam ends can carry different deteriorated backbones. Column-face offset + geometric NL delegated to `CrdTransf3d` (`geomTransf -jntOffset`). Exact `getInitialStiff` (clean initial-stiffness Rayleigh; no inflated stiffness → Zareian-Medina βK pathology absent). Single-tag recorders: `basicForce`/`basicDeformation`/`hingeRotation`/`hingeMoment` + `material <0-3>` passthrough. **NON-goals (v1):** no P-M, no biaxial My-Mz coupling (block-diagonal — future `LadrunoIMKColumn`); no element distributed loads (`-mass` lumped only). (Asymmetric end laws SHIPPED, see status.) Mechanics also reachable vanilla via `forceBeamColumn`+`HingeRadau`+`section Aggregator{IMK Mz, IMK My, Elastic P, Elastic T}` — this is a usability/robustness packaging. **2D COMPANION `LadrunoIMKBeam2d` (classTag 33004):** planar reduction — basic 3×3 `[N,Mz_i,Mz_j]`, strong-axis only (`-matZ`/`-matZi`/`-matZj`, no `-matY`; input `A E Iz transfTag`). The per-axis hinge solve (2×2 internal Newton + flexibility condensation) is extracted **verbatim** into header-only `LadrunoIMKHinge.h` (`ladrunoIMKSolveAxis`/`ladrunoIMKInitBlock`, OpenSees-free) shared by BOTH classes — 3D calls it twice (Mz/My), 2D once (Mz); the 3D refactor to call it is bit-identical (same Zone-A battery green). **ndm-dispatch:** the single `LadrunoIMKBeam` command builds 2D or 3D from the model dimension (ndm 2/ndf 3 → 2D, ndm 3/ndf 6 → 3D), same UX as `elasticBeamColumn` (no separate command). Plan/ADR: [[14_ladruno_imk_beam]]. | Element | **33003** (3D) + **33004** (2D `LadrunoIMKBeam2d`); Element band ≥33000, after LadrunoBrick=33002 | `SRC/element/ladrunoIMKBeam/{LadrunoIMKBeam.{cpp,h},OPS_LadrunoIMKBeam.cpp,LadrunoIMKBeam2d.{cpp,h},OPS_LadrunoIMKBeam2d.cpp,LadrunoIMKHinge.h,CMakeLists.txt}`, `tests/{test_ladrunoIMKBeam_element.py,test_ladrunoIMKBeam2d_element.py}` | shipped — v1 PR [#111](https://github.com/nmorabowen/OpenSees/pull/111); Zone-A battery **6/6** (v1: elastic-degenerate ↔ `elasticBeamColumn` 1e-9; single-hinge series vs independent python `a(θ−thⱼ)=M(thⱼ)` predictor; no-material-is-elastic guard) (v2 ADR-§6: **diaphragm axial immunity** — `rigidDiaphragm`-injected spurious axial leaves moment-rotation bit-identical (≤1e-7) & element converges where the equivalent fiber-hinge `forceBeamColumn`+`HingeRadau` (MinMax-fracture fibers) does NOT; **column-face offset** — hinge moment vs joint reaction differ by exactly `V·offset` via `geomTransf -jntOffset`; **Lignos-Krawinkler cyclic** — `Bilin` end hinge under a symmetric protocol: elastic-interior series identity `a(θ−θₕ)=M` exact + bare-material drive matches hinge moment ~3e-8 across full cyclic path incl. deterioration + real hysteretic energy); **asymmetric end laws** `-matZi/-matZj/-matYi/-matYj` (parser-only — the element already had 4 independent hinge slots — per-end override of the symmetric law, independent of `-hinge`; Zone-A test: A@i + B@j both driven to yield, each end's reported (θ,M) matches its OWN bare material). **2D companion `LadrunoIMKBeam2d` + shared `LadrunoIMKHinge.h` kernel + ndm-dispatch ([#131](https://github.com/nmorabowen/OpenSees/pull/131)):** Zone-A `test_ladrunoIMKBeam2d_element.py` — ndm-dispatch + elastic-degenerate ↔ 2D `elasticBeamColumn` 1e-9, no-material-is-elastic guard, single-hinge series vs python predictor, **axial immunity** (equalDOF-injected spurious axial leaves M-θ bit-identical), column-face offset `V·offset`, asymmetric `-matZi/-matZj`, Lignos-Krawinkler cyclic `Bilin` (series identity + bare-material delegation + hysteresis); 3D battery unchanged after the kernel extraction | [#111](https://github.com/nmorabowen/OpenSees/pull/111) (v1), [#113](https://github.com/nmorabowen/OpenSees/pull/113) (v2 ADR-§6 tests), [#116](https://github.com/nmorabowen/OpenSees/pull/116) (asymmetric end laws), 2D + shared kernel [#131](https://github.com/nmorabowen/OpenSees/pull/131) |

| **LadrunoRebarBuckling** — rebar-buckling **wrapper** `uniaxialMaterial` over any tension-compression bare-bar material (designed for [[13_ladruno_uniaxial_j2_adr]] `LadrunoUniaxialJ2`, composes over `Steel02`/`Steel4`). In compression past a slenderness-dependent onset it applies the **Dhakal–Maekawa** buckled-average degradation `σ_buckled = r(e,λ)·σ_bare` (`r∈[resid,1]`, `λ=s/d`), leaving the wrapped core byte-untouched (its V7 oracle property preserved) — a clean opt-in geometric overlay, stackable `Fatigue ∘ RebarBuckling ∘ J2`. Tension + pre-onset pass through unchanged; **`-lsr 0` = exact identity gate**. Buckled average referenced to the tensile-unload anchor `e_cross = ε_max − σ_max/E` (own minimal cyclic state — an `ε_max` anchor + cached onset backbone `fStarL` — NOT the `ReinforcingSteel` `TBranchNum` machine). Onset `eStar = −max(7, 55−2.3√(f_y/E·2000)·λ)·ε_y`; residual `fStar = fStarL·α·(1.1−0.016√(f_y/E·2000)·λ)` floored at `−0.2 f_y`; intermediate branch `r=1−(1−fStar/fStarL)(e+ε_y)/(eStar+ε_y)`, deep branch ramps to the floor. **Analytic consistent tangent** `dσ/dε = r·k_bare + σ_bare·(∂r/∂ε)` (improves on `ReinforcingSteel`'s central-difference `Buckled_mod_Dhakal`). DM formula ported verbatim from `ReinforcingSteel::Buckled_stress_Dhakal(ess,fss)` but in **engineering strain** on a generic wrapped bar (RS works in log strain on its own monolithic backbone); the generic onset backbone `fStarL` is read via a committed-clone probe of the wrapped bar. Real `revert*`/`Print`/`setParameter` (own `lsr`/`alpha`, forwards the rest to the bar)/`setResponse` (`stress`/`strain`/`tangent`/`reduction`, forwards core responses like `plasticStrain`/`backStress` to the bar), nested-material `sendSelf`/`recvSelf`, **no `exit()`**. **Both laws monotonic shipped:** `-model dm` (clean `r·σ` branch) + `-model ga` **Gomes–Appleton** (`σ = f_sup·g − (factor+g)(f_sup·g − σ_bare)/(1+g)`, `factor = min(1,fs_buck)·β·(1−r)+r`, `fs_buck = √(32/(e_cross−ε))/(3πλ)`; GA knobs `-reduction r` (RS GABuck `r`, wrapper default 0 = full so `lsr>0` buckles — deviates from RS `r=1`=off) + `-fsufrac g`; GA needs NO `-fy`; hybrid analytic tangent `ρ·k_bare + (∂σ/∂factor)·factor'` with `factor'` by cheap closed-form FD — better than RS's `Buckled_mod_Gomes`; faithfully reproduces RS's shadowed-`beta`/`gama` quirk). **v2 CYCLIC re-straightening SHIPPED** — a `{PASS, BUCKLING, RESTRAIGHTEN}` committed-latch state machine carrying the buckling RAISE `g = σ − σ_bare ≥ 0` (`σ = σ_bare + g`, `g ∈ [0, g_law]`, `g_law = σ_v1 − σ_bare`; v1 is exactly `g == g_law` ⇒ reduce-to-v1 by literal code reuse). On reload the raise is HELD on the compression side then DECAYED to 0 over `L_rs` (SMOOTHSTEP `B(q)=1−(3q²−2q³)`, C1-clean both seams) tracking the LIVE `σ_bare`; the RESTRAIGHTEN→BUCKLING re-entry is **C0-continuous** via a strain-based smoothstep carry to the deepest-bow envelope (residual-gap memory). `k_rev = getInitialTangent`. New `-restraighten c <v>\|lambda` selector: `L_rs = max(c·\|e_rev−e_cross_rs\|, eY)` or `f(λ)·eY` (`f(λ)=0.5+0.10·max(0,λ−5)`, clamp [0.5,6]). Serialization grown to `Vector(28)` with the schema version in `dataID(3)` (NOT `data(0)`=model; hard-errors on an unsupported version). Plan/ADR §9: [[14_ladruno_rebar_buckling_adr]]. | Material (wrapper) | **MAT_TAG 33001** (Ladruno *uniaxial* band, sibling of LadrunoUniaxialJ2=33000; per-registry, no collision w/ ELE BezierTet10=33001) | `SRC/material/uniaxial/LadrunoRebarBuckling.{cpp,h}`, `tests/test_ladrunoRebarBuckling_material.py` | shipped — Zone-A battery: DM B0/B1/B2/B2b/B3/B5 + GA GA0/GA1/GA2/GA3/GA5 + B6 composition + B7 round-trip + **v2 cyclic B4a/B4b/B4c/B4d/B4f/B4-GA/B4h/B4i** (reduce-to-v1, rejoin, hold-raise, Phase-2 bracket, C0 re-entry, law-agnostic, serialization round-trip, L_rs floor) — 27/27 green | [#119](https://github.com/nmorabowen/OpenSees/pull/119) (DM), [#121](https://github.com/nmorabowen/OpenSees/pull/121) (GA), [#124](https://github.com/nmorabowen/OpenSees/pull/124) (tests B6/B7), [#135](https://github.com/nmorabowen/OpenSees/pull/135) (v2 cyclic re-straightening) |

| **LadrunoBondSlip** — 1D bond-slip `tau`-`s` `uniaxialMaterial` (CEB-FIP Model Code 2010 backbone) for the bar-axial slot of the embedded-rebar element ([[20_ladruno_embedded_reinforcement_adr]] D4); **v2 of the RC-3D plan, first slice**. v1 = MONOTONIC backbone + elastic (k0) unload/reload, sign-symmetric. Backbone: linear `s<s0` (D4.1 initial-slip regularization — kills the power-law `dτ/ds→∞` at s→0; `s0` default `0.1·s1`, `k0=τmax(s0/s1)^α/s0`) → ascending power law `τmax(s/s1)^α` → plateau `τmax` → softening to `τf` (NEGATIVE tangent ⇒ caller uses disp/arc/IMPLEX control, D4.2) → residual `τf`. **D4.3 fracture-energy reg.:** `-Gf` overrides `s3` so the softening triangle dissipates `Gf` per unit interface area (`s3=s2+2Gf/(τmax−τf)`); the element scales `τ` by `perimeter·L_trib`. `getInitialTangent=k0` (finite). Parse `uniaxialMaterial LadrunoBondSlip tag τmax s1 s2 s3 τf α <-Gf Gf> <-s0 s0>`; setResponse `bondStress`/`slip`/`tangent`. Registered: classTags.h, uniaxial CMake, Python + Tcl command maps, broker, header-stamp glob. | Material | **MAT_TAG 33002** (Ladruno *uniaxial* band, after LadrunoUniaxialJ2=33000, LadrunoRebarBuckling=33001) | `SRC/material/uniaxial/LadrunoBondSlip.{cpp,h}`, `tests/test_ladrunoBondSlip_material.py` | shipped — CI green (Zone-A Ubuntu build + classTag/manifest gates); Zone-A battery (backbone-vs-oracle, k0-finite, sign-symmetry, Gf-overrides-s3, elastic-unload) | [#168](https://github.com/nmorabowen/OpenSees/pull/168) |

| **LadrunoQuad** — unified 4-node bilinear plane (plane-stress/strain) continuum element, the 2D port of [[09_ladruno_brick]]: `-formulation {std\|bbar\|ssp\|eas}` × `-geom {linear\|corot\|finite}` + single-mode Flanagan–Belytschko hourglass + damage-scaled `Kstab` + ASDConcrete crack-band `lch=√A` handshake. std↔`FourNodeQuad`, bbar↔`ConstantPressureVolumeQuad`, ssp↔`SSPquad` (port `GetStab`), eas↔`EnhancedQuad` (Q1/E4, small-strain only; eas+finite reserved — Q1/E4 hourglasses in finite compression). Geometry via [[solid_transformation_wrapper]] lifted to 2D (`SolidTransformation2DCorot` 2×2 polar / `SolidTransformation2DFinite` 2×2 **F** + plane condensation; F-bar power **½** not ⅓). Plane-strain concrete via a 3D-in-plane-strain adaptor (ε₃₃=0); plane-stress concrete via `LadrunoRCConcrete` (33015) PlaneStress (plain ASDConcrete3D has no plane view). ADR [[25_ladruno_plane_elements_adr]]; frontier/contribution thesis [[26_ladruno_plane_frontier_adr]]. **2D is the proving ground for lifting coupled-field localization (gradient-damage/phase-field) to the brick.** | Element | **ELE_TAG 33007** (Ladruno element band, after LadrunoEmbeddedNode=33006; renumbered from the ADR-reserved 33006, which a sibling PR took for LadrunoEmbeddedNode) | `SRC/element/ladrunoPlane/{LadrunoQuad.{cpp,h},OPS_LadrunoQuad.cpp,CMakeLists.txt}`, `tests/test_ladrunoPlane_element.py` | shipped — **P1** (std + bbar, `-geom linear`; ssp/eas reserved → P2/P3) — Zone-A 13/13: std↔`quad`(FourNodeQuad)+CST↔`Tri31` byte-reduce (PlaneStrain+PlaneStress), rank/3-rbm, bbar anti-locking @ν=0.4999, constant-stress patch (std+bbar), reserved-formulation guards. **P2 (ssp + crack-band lch + Tier-A damage-scaled Kstab) shipped via #209.** | [#207](https://github.com/nmorabowen/OpenSees/pull/207) (P1), [#209](https://github.com/nmorabowen/OpenSees/pull/209) (P2) |
| **LadrunoCST** — unified 3-node constant-strain triangle, the thin 2D sibling of `LadrunoQuad`: `-formulation std` only (1-pt triangle is rank-sufficient — no hourglass; bbar/ssp/eas have nothing to average) + `-geom {linear\|corot\|finite}` + crack-band `lch=√(2A)`. std↔upstream `Tri31`. Ships as the trivial baseline / triangular-mesh fallback / future E-FEM carrier ([[18_ladruno_efem_adr]]) — **honestly low-value** (plain CST volumetric-locks and mesh-biases localization; see [[26_ladruno_plane_frontier_adr]] §CST). ADR [[25_ladruno_plane_elements_adr]]. | Element | **ELE_TAG 33008** (after LadrunoQuad=33007; renumbered from the ADR-reserved 33007) | `SRC/element/ladrunoPlane/{LadrunoCST.{cpp,h},OPS_LadrunoCST.cpp}`, `tests/test_ladrunoPlane_element.py` | shipped — **P1** (std, `-geom linear`); mirrors `Tri31` (covered in the 13/13 plane battery) | [#207](https://github.com/nmorabowen/OpenSees/pull/207) |
| **LogStrain2D** — 2D Hencky/log-strain finite-strain adaptor (2×2 **F**, plane-strain ε₃₃=0 / plane-stress σ₃₃=0 thickness-stretch condensation) feeding `LadrunoQuad`/`LadrunoCST -geom finite`; the 2D sibling of [[09_finite_strain_material_wrapper]]'s `LogStrainNDMaterial` (33010). May ship instead as `PlaneStrain`/`PlaneStress` getType views of the existing 33010 class (decided at ADR-25 P5). ADR [[25_ladruno_plane_elements_adr]]. | Material | **`ND_TAG_LogStrain2D` 33016 — RESERVED, not yet built** (ND band ≥33010, after LadrunoRCConcrete=33015) | `SRC/material/nD/LogStrain2D.{cpp,h}` | draft (ADR) | _PR pending_ |
| **LadrunoConcrete3D** — CDPM2-grade solid-concrete plastic-damage `nDMaterial` ([[31_ladruno_concrete3d_adr]]); the solid/triaxial sibling of `LadrunoRCConcrete` (33015, shell/MCFT). Header-only OpenSees-free **`LadrunoConcrete3DKernel.h`** (LadrunoJ2Kernel "one core many views" doctrine): effective-stress **Menétrey–Willam** 3-invariant surface (friction `m0`, eccentricity `e` from Kupfer `fcc/fc`, Willam–Warnke Lode `r(θ,e)`; Grassl et al. 2013 Eq.18 with the `[1−qh1]` ellipsoidal hardening cap), **non-associated flow** (dilatancy `Df`, Lode-INDEPENDENT potential Eq.22-25), **confinement-aware ductility** hardening (`qh1/qh2/κp`, Eq.30-36; ductility `xh(σ̄_V)` Eq.33-36). **Semi-implicit return** (θ frozen at trial ⇒ radial deviatoric return, sidesteps the Lode-corner singularity): perfect-plastic 3-unknown `(ξ,ρ,Δλ)` Newton (analytic 3×3 Jacobian) + hardening 4-unknown `(ξ,ρ,Δλ,κp)` Newton (analytic 4×4 Jacobian) + hydrostatic-tension apex fallback + HONEST convergence (independent `f` at the returned stress's own Lode angle). Full-tensor via spectral split (cyclic-Jacobi 3×3 eig → principal return preserves eigenvectors → recompose). **Analytic consistent tangent** = spectral lift (de Souza Neto Box A.6) of the principal Jacobian (IFT on the inner Newton residual; Lode directional gradient by an isolated scalar micro-FD) — **NON-symmetric** for non-associated flow AND ~2% even associated (frozen-eigenvector recompose) ⇒ Tier-1 needs an **unsymmetric solver UNCONDITIONALLY**. **Effective-stress plasticity is MONOTONIC (no peak)** — the failure surface is reached exactly at `κp=1`; the peak/softening is the DAMAGE part (P2 — **oracle-complete through P2e**: dual scalar `ω_t`/`ω_c` on the spectral T/C split `σ=(1−ω_t)σ̄_t+(1−ω_c)σ̄_c` Eq.1, crack-band `G_f`/`G_c`, **automatic per-step unilateral crack-closure** via re-splitting the converged effective stress, the single-step tensor update + numerical AND **analytic dual-projector damaged consistent tangent** `D_dam:C_eff − σ̄_t⊗∂ω_t/∂ε − σ̄_c⊗∂ω_c/∂ε`; **the dual-damage stress update AND the analytic damaged tangent are now BOTH ported to the C++ kernel — P3a/P3b** — only the cyclic `β_c` temper + the nDMaterial wrapper still owed). Verified byte-for-byte against the numpy oracle (`concrete3d_ref.py`): perfect-plastic stress + tangent to machine precision (~1e-13 / ~2e-10), hardening to the oracle's numerical-Jacobian reference floor (~1e-8), via the committed g++ oracle-numeric-dump self-check (`concrete3d_kernel_check.cpp` + `gen_concrete3d_fixture.py` → `concrete3d_oracle_fixture.txt`). Known gap (inherited from the oracle, handoff §6): the low-`κp` tiny-surface/off-meridian first-yield APEX regime needs sub-stepping + a dedicated apex sub-algorithm. **C++ KERNEL DAMAGE stress update ported (P3a, g++ byte-verified ~1e-14):** `equivStrainGeneral`/`alphaCompression`/`damageDrivers`/`solveOmegaBracketed` + `damagedUpdate` (dual-damage nominal `σ=(1−ω_t)⟨σ̄⟩₊+(1−ω_c)⟨σ̄⟩₋`, re-split unilateral, the physical `ω`-floor); `State` extended with `sigEff` + 6 damage-history fields; `returnMap` returns nominal `σ` + keeps `sigEffImplicit`=effective; 4 `DMG` g++ fixture cases reproduce the oracle `damaged_step_tensor` to machine precision. **C++ KERNEL ANALYTIC DAMAGED TANGENT ported (P3b):** `isotropicTangent` (Box A.6 spectral derivative w/ l'Hôpital), `invert6`, `scalarDriver`/`dscalarDsig` (the isolated micro-FD scalar grads), and `damagedTangent` (`D_dam:C_eff − σ̄_t⊗∂ω_t/∂ε − σ̄_c⊗∂ω_c/∂ε`, the `[1,1,1,2,2,2]` Voigt-weight on the tensor grads only); `returnMap` upgrades `Dtan6` from the P1 effective tangent to this damaged tangent when `doTangent && status==0`. Verified by a SELF-CONTAINED g++ check (B4): the analytic damaged tangent == a numerical central-difference of the SAME C++ damaged nominal stress (the operator the global Newton consumes) — `dmgtan` rel ~1e-7 worst (≤1e-10 tension/shear/reversal), so no cross-platform FD-fixture noise; the stress is already pinned to the oracle by B3. Also cross-checked directly against the oracle `damaged_tangent_analytic` (~1e-10..1e-7). **STILL OWED:** the **nDMaterial wrapper** (`LadrunoConcrete3D.{cpp,h}` + FEM_ObjectBroker + parser + the `eccentricityFromKupfer` foot-gun guard + the unsymmetric-solver warning + the 33017 define) + the cyclic `β_c` temper (P2f). | Material | **`ND_TAG_LadrunoConcrete3D` 33017 — DEFINED (nDMaterial wrapper shipped)** (ND band; 33016 reserved for LogStrain2D) | `SRC/material/nD/LadrunoConcrete3D.{h,cpp}`, `SRC/material/nD/LadrunoConcrete3DKernel.h`, `tests/_testbed/concrete3d_ref.py`, `tests/_testbed/gen_concrete3d_fixture.py`, `tests/_testbed/concrete3d_oracle_fixture.txt`, `tests/_testbed/concrete3d_kernel_check.cpp`, `tests/test_ladrunoConcrete3D_material.py` (oracle+kernel), `tests/test_ladrunoConcrete3D_element.py` (openseespy nDMaterial); registered in `SRC/classTags.h`, `FEM_ObjectBrokerAllClasses.cpp`, `OpenSeesNDMaterialCommands.cpp`, `SRC/material/nD/CMakeLists.txt` | kernel: P0 surface (#240) + P1 perfect-plastic return map (#240) + hardening qh1/qh2/ductility (#244) + consistent tangent + spectral return (#247); review fixes + dev handoff (#248). **C++ return-map port + analytic tangent + g++ oracle-dump gate (#249)** — Zone-A 12/12 (oracle P0/P1/hardening/tangent + the C++ kernel byte check). **Hardened via a 5-lens adversarial review (PR #249):** (1) the test file was missing the `zone_a` CI marker since #240 ⇒ the WHOLE file (incl. these gates) never ran in CI — FIXED (`pytestmark`); (2) the hardening apex branch could falsely report `converged=True` teleporting a deep-COMPRESSION trial to the tension vertex with κp<κp_n (~2% of converged returns inadmissible) — FIXED with an admissibility gate (κp≥κp_n, Δλ≥0, finite, on-surface) + safe elastic-predictor fallback (deliberately diverges from the oracle's arbitrary apex teleport); fuzz regression asserts 0/180k inadmissible; (3) NaN-leak on degenerate params (ft=0⇒m0=∞) — FIXED with a finite-fallback net in returnMapTensor; (4) g++ check strengthened with per-entry + asymmetry-norm tangent checks (rel-Frobenius hid the small non-symmetric terms) + the apex/NaN regressions; (5) the self-referential fixture test (regen overwrote the committed artifact) — FIXED (regen→tmp + assert committed up-to-date). Apex *tangent* (rank-deficient ~0) still owed (returns elastic surrogate, flagged) — P2. **P2 damage oracle (numpy, Zone-A 16/16):** P2a tensile `ω_t` + crack-band `G_f` (#259); P2b compressive `ω_c` + `α_c` T/C split + crack-band `G_c` (#261); **P2c UNIFIED TENSOR dual-damage update (`spectral_split_principal`/`apply_damage_principal`/`damaged_stress_tensor`/`drive_damaged_unified`) — the single constitutive update the wrapper will call: Eq.1 spectral split, Eq.37/43-49/56-57 histories re-pinned from the arXiv full text, automatic per-step unilateral crack-closure (ADR §4.3 BLOCKING) — reduces EXACTLY to P2a (4.9e-13) / P2b, DT3 reversal recovers full stiffness, DT4 frame-objective (#284)**. **P2d single-step tensor update `damaged_step_tensor`(state,deps)→σ_nom (= the C++ `setTrialStrain` contract) + the NUMERICAL damaged consistent tangent `damaged_consistent_tangent` (central diff; the reference the C++ ANALYTIC dual-projector tangent `(1−ω)dσ̄/dε − σ̄_t⊗∂ω_t/∂ε − σ̄_c⊗∂ω_c/∂ε` will be FD-checked against — P1-tangent pattern): TD0 == path driver, TD1 reduces to elastic C / P1 effective tangent pre-damage, TD2 softening tangent DEGRADED + INDEFINITE (C[0,0]<0, λ_min(symC)<0 = the Tier-2 motivation), TD3 non-symmetric (‖C−Cᵀ‖/‖C‖≈0.31), TD4 frame-objective, TD5 finite across a reversal + an eigenvalue crossing (#285)**. **P2e ANALYTIC dual-projector damaged tangent `damaged_tangent_analytic` = `D_dam:C_eff − σ̄_t⊗∂ω_t/∂ε − σ̄_c⊗∂ω_c/∂ε` (the C++ deliverable): `D_dam`=`isotropic_tangent` spectral derivative (de Souza Neto Box A.6, l'Hôpital at eigenvalue coalescence), `∂ω/∂ε` via IFT on the bracketed ω-solve chained through the histories (scalar sub-grads `∂ε̃/∂σ̄`,`∂x_s/∂σ̄`,`∂α_c/∂σ̄` by isolated micro-FD, `∂λ_extreme/∂σ̄` by eigenprojection with the `[1,1,1,2,2,2]` tensor-contraction weight, `∂‖Δε_p‖/∂ε` closed form). FD == the P2d numerical ref to rel ~1e-10 in tension/confined-compression/shear-nonassoc/reversal; reduces to elastic/effective pre-onset; the `σ̄_lat=0` Macaulay kink is a valid subgradient (axial agrees, ~zero-stress lateral differs). pytest `test_p2e_analytic_damaged_tangent_gate` (this PR)**. Damage gates use a bracketed (bisection-safeguarded) `ω`-solve (a raw clamped Newton spuriously HEALED the crack — PR #261 review CRITICAL). **The C++ kernel damage subsystem (stress P3a + analytic tangent P3b) is now COMPLETE. nDMaterial WRAPPER SHIPPED (`nDMaterial LadrunoConcrete3D` classTag 33017 DEFINED):** thin OpenSees wrapper over the kernel (LadrunoJ2 pattern), Tier-1 implicit (IMPL-EX/viscous = P3), flat-`Vector` serialization (kernel state is all fixed-size scalars — no borrowed sub-objects, so the ADR R11 hybrid-serialization concern does not apply), engineering↔tensor shear conversion at the boundary (strain ×0.5 in / ×2 out; tangent shear-COLUMNS ×0.5 since the kernel tangent is in the tensor convention `dσ_ij=2G dε_ij`). **Phase-2 reduced views SHIPPED (#299, the LadrunoJ2 `dim`-mode doctrine):** ALL dimensional views served by the ONE class — 3D + PlaneStrain/AxiSymmetric/PlateFiber/PlaneStress reached via `getCopy(type)` (the parser always builds the 3D prototype). The kernel return map always runs on the full 6-comp tensor; `setupDim()` fills a reduced-order `vmap`; the element vector is mapped to/from the reduced ordering (with the engineering-shear ×0.5/×2 in the reduced path); PlaneStress/PlateFiber enforce σ_22=0 by a nested Newton on ε_22 + static condensation of the 33 dof (de Souza Neto §9.2.3 — the tensor-tangent column-halving COMMUTES with the rank-1 condensation, so condense-then-halve is exact). `dim`+`cEps22` carried in send/recvSelf (recvSelf rebuilds the view via `setupDim`). Verified SELF-REFERENTIALLY (no upstream CDPM2 peer): a reduced view from `getCopy(type)` (via `quad`/`bbarQuad`) vs the shipped 3D material under the matching out-of-plane constraint — PlaneStrain in-plane stress == the 3D ε_22=0 slice; PlaneStress in-plane stress == the 3D σ_22=0 Newton (elastic + a nonlinear damaging replay); both elastic tangents pinned to the CLOSED-FORM plane-strain/plane-stress isotropic moduli; AxiSym build+run smoke (`bbarQuad`). NB unconfined plane-STRESS softening is snap-backy ⇒ the σ_22=0 nested Newton can stall post-peak (Tier-3 territory); robust pre-peak / under confinement. Parser `nDMaterial LadrunoConcrete3D tag E nu fc ft Gf Gc <-e|-kupfer> <-Df> <-As> <-rho> <-hardening qh0 Hp> <-ductility Ah Bh Ch Dh> <-lch> <-autoRegularization>` with foot-gun guards (E>0, 0≤ν<0.5, fc,ft>0, ft<fc⇒m0>0, Gf,Gc>0, As≥1, e∈(0.5,1]) + a NON-SYMMETRIC-tangent ⇒ unsymmetric-solver WARNING. Recorders: stress/strain/tangent/effectiveStress/damage[ωt,ωc]/kappaP/plasticStrain (kernel `damagedUpdate`/`returnMap` extended with optional ω out-params — backward-compatible, g++ byte-check unaffected). Banner + manifest (33017 active) + `classTags.h`/broker/command-map/CMake all wired. **P3 robustness — Tier-2 IMPL-EX ORACLE slice done (#301, numpy) + ADVERSARIAL-REVIEW-HARDENED (#304):** `damaged_step_implex` reports an EXPLICIT stress from EXTRAPOLATED internals (plastic-strain increment + dual damage frozen at the committed-history rate, `x~=x_n+(Δt/Δt_n)·dx_n`, ratio CLAMPED to [0,R_max]) so the algorithmic tangent is the degraded-elastic secant `D_dam(ω~):C0`, while committing the exact IMPLICIT variables. **6-finding adversarial-review workflow (0 refuted) folded in (#304):** (NUM-1) the secant is symmetric-part SPD **only in SINGLE-SIGN principal regimes** — on a mixed-sign high-ω state (tension crack + lateral compression, ω_t>~0.97) `D_dam:C0` is non-commuting + INDEFINITE (λ_min≈−5e2); the unconditional-SPD claim was FALSE (genuine dual-damage consistency and unconditional SPD are mutually exclusive) → restated honestly + PINNED by gate PI5; (ALG-2/NUM-2/NUM-3) the unguarded `r=dt/dt_n` let an adaptive dt-growth over-extrapolate ω past [0,1) (stress collapse + indefinite tangent) / inject unbounded plastic strain / run damage backward on negative dt → **clamped** `r∈[0,2]`; (ALG-1/GAT-2) the PI3 O(Δt) gate was brittle (global-max overstress dominated by an irreducible onset C0-kink lag, passed only at a hardcoded N) → re-gated on the SMOOTH-region fixed-strain order across ≥3 levels (clean ~O(Δt²)). GATES `run_p3_implex_gate`/`test_p3_implex_gate`: PI1 single-sign SPD across snap-back vs Tier-1 INDEFINITE (TD2); PI2 committed==Tier-1 byte-exact; PI3 smooth-region order≥1 (~2) monotone; PI4 error monitor; PI5 secant==`d(σ_rep)/d(Δε)` (FD) + mixed-sign SPD-limitation pinned; PI6 non-uniform-dt robustness (the clamp). ADR §4.4 BLOCKING honored (freeze plastic too) + CORRECTED (conditional SPD). **IMPL-EX C++ KERNEL PORT + `-implex` WRAPPER SHIPPED (#309):** `returnMap` gained a `dt` param + a Tier-2 branch (kernel `State` += `wt/wc/dwt/dwc/depl[6]/dt_n`; `Params.implexRmax=2`) — runs the implicit solve for the committed internals + reports the EXPLICIT extrapolated stress + the `D_dam(w~):C0` secant (`sigEffImplicit` stays implicit for the LogStrain b^e contract). Wrapper `-implex` parser flag + committed/trial IMPL-EX state + `dt=ops_Dt` (ASDConcrete3D pattern) + serialization (+12 fields). g++ byte-check B5 (`NIMPLEX`: committed implex state + step incl. a dt-JUMP → reported stress == oracle `damaged_step_implex` to ~1e-16, clamp matches). Element tests: `-implex` compression end-to-end (peak≈fc + ω_c), tension softening on a SYMMETRIC solver (`ProfileSPD`, single-sign SPD payoff), DB round-trip of the IMPL-EX state. **POST-#309 (detail in [[LadrunoConcrete3D_dev_handoff]] §0): Duvaut–Lions `-eta` viscoplastic relaxation (oracle #316 + C++/`-eta` wrapper #318); P2f cyclic `β_c` Eq.50 restored = faithful-CDPM2 more-ductile compression (oracle + kernel #321); P2g monotone-`ω` no-heal cyclic damage = running-max drive + secant unload + SPD unload tangent (oracle + kernel + wrapper #325); P2h `-ctTemper {none|alphat|proj}` compression→tension damage temper (oracle + kernel + wrapper #327); Tier-3 explicit (the robustness trilogy completes) = element battery only, NO kernel change — the core demo (#328: g++ B7 do_tangent-independence gate `t3=0` + free-dynamics CentralDifference run) + the quasi-static enhancement (#333: prescribed support-motion drives the `-rho` cube through a clean peak+softening backbone under BOTH CDL + ExplicitBathe on `system Diagonal` + `algorithm Linear` — no global tangent, no step-cutting; backbone tracks the numpy oracle; explicit completes where fixed-step implicit Newton stalls at the limit point — and CORRECTS #328's gotcha that a ramped prescribed-SP fails under CentralDifference: it does not, it is support-motion input). **P2i multiaxial-damage apportioning (#336, oracle + C++ kernel + g++ byte-check): the TENSILE ω-solve is driven by `E·ε̃` (Eq.37 equivalent strain) instead of the extreme tensile principal `max⟨σ̄_i⟩₊`, gated by a real tensile principal (`max(σ̄)>1e-6·ft`); the COMPRESSIVE channel KEEPS the extreme principal (USER decision — `ε̃` is ft-scaled so `E·ε̃` could never onset ω_c). Changed at the 3 oracle sites + mirrored in `LadrunoConcrete3DKernel.h` `damagedUpdate`/`damagedTangent` (`Dt=E·et`, tensile drive-gradient `dDt_deps=E·det_deps` replacing the `Emax` eigenprojection ⇒ no `W6` weight; `Emin` kept for compression). Gate `run_p2i_gate`/`test_p2i_multiaxial_apportioning_gate` I1–I4: uniaxial byte-identical (E·ε̃==σ̄_t); BOTH equibiaxial AND triaxial tension escalate `E·ε̃` above ft (lower per-principal onset) with ordering uni<TRI<BI (hydrostatic-triaxial APEX-CAPPED < deviatoric biaxial); no spurious compression→tension damage; analytic==numerical tangent at an unequal-biaxial damaged state. New discriminating g++ `dmg_biaxial_tension` case (two positive principals) byte-matches the oracle nominal stress 2.2e-16 + tangent 1.2e-9; uniaxial g++ cases unchanged. NO wrapper/serialization/classTag change.** **P4 finite-strain (#339, validation gate ONLY — no source change): the 3D material lifts to finite strain through the GENERIC `nDMaterial LogStrain` (33010) wrapper consumed by `element LadrunoBrick … -geom finite` (F→setTrialF→`b=F Fᵀ`→`εᵉ=½ln b`→unchanged small-strain return→Kirchhoff τ→Cauchy σ=τ/J). The generic wrapper is EXACT here (vs LadrunoRCConcrete needing a native subclass) because the kernel exposes `sigEffImplicit`=UNDAMAGED effective stress for the bᵉ recovery (ADR R3), AND because LadrunoConcrete3D is ISOTROPIC (scalar ωt/ωc, no tensorial/kinematic internal var) ⇒ `σ(QF)=Qσ(F)Qᵀ` EXACT, NO de Souza Neto §14.11 boundary (the clean contrast with LadrunoJ2Finite's non-co-rotating Chaboche backstress). `tests/test_ladrunoConcrete3D_finite.py` 4/4 (self-referential — the small-strain material IS the reference; runs on the existing binary): reduce-to-small-strain, rigid-rotation stress-free, OBJECTIVITY through a damaged state (~1e-8), the LogStrain Hencky SEAM through damage (GP Cauchy == small-strain σ at ½ln b /J).** **P5a confined-fiber view ORACLE slice (#341, §4.6 "Mander by mechanism", oracle-only): the SAME kernel condensed against a PASSIVE hoop spring — `drive_confined_fiber` mirrors `drive_damaged_unified` but the lateral Newton targets `σ_lat_eff + σ_hoop(ε_lat)=0` (σ_hoop=min(K·ε_lat,f_y), tension-only circular hoop) instead of free/active; the non-associated dilatancy mobilizes the hoop pressure self-consistently ⇒ confinement strength+ductility EMERGE (no pre-baked Mander backbone). `run_p5_gate`/`test_p5_confined_fiber_gate` F1–F4: F1 reduce-to-1D (hoop_K=0 == drive_damaged_unified σ3=0, 0.00); F2 fcc/fc AND ε_peak grow monotonically (1.00→1.45→2.11 / 0.0012→0.0034); F3 HEADLINE fcc/fc reproduces the Mander circular-hoop formula at the SELF-mobilized p_conf@peak ≤2.9% for p/fc∈[0.085,0.244]; F4 hoop yield caps p. Material gate 25/25. C++ view+condensed-1D-tangent+fiber integration = P5b.** NEXT = P5b C++ confined-fiber view → P6 auto-hybrid (plastic-dissipation regularization DEFERRED: approach A — qh2 cap — empirically WORSENS the D3 spread 10–100×; per-vol plastic work is lch-independent so W_p·lch∝lch; FE-visible spread already small ~0.33·Gf). | [#240](https://github.com/nmorabowen/OpenSees/pull/240), [#244](https://github.com/nmorabowen/OpenSees/pull/244), [#247](https://github.com/nmorabowen/OpenSees/pull/247), [#248](https://github.com/nmorabowen/OpenSees/pull/248), [#249](https://github.com/nmorabowen/OpenSees/pull/249), [#259](https://github.com/nmorabowen/OpenSees/pull/259), [#261](https://github.com/nmorabowen/OpenSees/pull/261), [#284](https://github.com/nmorabowen/OpenSees/pull/284), [#285](https://github.com/nmorabowen/OpenSees/pull/285), [#286](https://github.com/nmorabowen/OpenSees/pull/286) P2e analytic tangent, [#287](https://github.com/nmorabowen/OpenSees/pull/287) PE2 cross-platform fix, [#288](https://github.com/nmorabowen/OpenSees/pull/288) adversarial-review fixes (ω floor + DT3), [#289](https://github.com/nmorabowen/OpenSees/pull/289) P3a C++ kernel damage stress update, [#290](https://github.com/nmorabowen/OpenSees/pull/290) implementation guide, [#291](https://github.com/nmorabowen/OpenSees/pull/291) P3b C++ kernel analytic damaged tangent, [#292](https://github.com/nmorabowen/OpenSees/pull/292) nDMaterial wrapper (33017 defined), [#293](https://github.com/nmorabowen/OpenSees/pull/293) shipped-material handout, [#294](https://github.com/nmorabowen/OpenSees/pull/294) wrapper shear/multiaxial convention tests, [#299](https://github.com/nmorabowen/OpenSees/pull/299) Phase-2 reduced views (PlaneStrain/AxiSym/PlateFiber/PlaneStress), [#301](https://github.com/nmorabowen/OpenSees/pull/301) P3 Tier-2 IMPL-EX oracle + falsification gate, [#304](https://github.com/nmorabowen/OpenSees/pull/304) IMPL-EX adversarial-review fixes (conditional SPD + r-clamp + smooth-region PI3), [#309](https://github.com/nmorabowen/OpenSees/pull/309) IMPL-EX C++ kernel port + `-implex` wrapper (B5 byte-check + symmetric-solver element test), [#316](https://github.com/nmorabowen/OpenSees/pull/316) Duvaut–Lions `-eta` oracle, [#318](https://github.com/nmorabowen/OpenSees/pull/318) Duvaut–Lions `-eta` C++ kernel + wrapper, [#321](https://github.com/nmorabowen/OpenSees/pull/321) P2f cyclic `β_c` (Eq.50, oracle + kernel), [#325](https://github.com/nmorabowen/OpenSees/pull/325) P2g monotone-`ω` no-heal cyclic damage (oracle + kernel + wrapper), [#327](https://github.com/nmorabowen/OpenSees/pull/327) P2h `-ctTemper {none\|alphat\|proj}` compression→tension temper (oracle + kernel + wrapper), [#328](https://github.com/nmorabowen/OpenSees/pull/328) Tier-3 explicit (g++ B7 do_tangent-independence gate + free-dynamics CentralDifference demo), [#333](https://github.com/nmorabowen/OpenSees/pull/333) Tier-3 explicit quasi-static + cross-integrator (prescribed-motion peak+softening backbone, CDL + ExplicitBathe, oracle-backbone match, implicit-stall contrast; corrects #328's prescribed-SP gotcha), #336 P2i multiaxial-damage apportioning (oracle + C++ kernel + g++ `dmg_biaxial_tension` byte-check — tensile ω-drive `E·ε̃` Eq.37, compressive keeps extreme-principal; uni<tri<bi escalation), #339 P4 finite-strain validation gate (`nDMaterial LogStrain` over the 3D material — isotropic objectivity exact, no §14.11 boundary; `tests/test_ladrunoConcrete3D_finite.py` 4/4; no source change), #341 P5a confined-fiber view ORACLE (§4.6 hoop-spring condensation, "Mander by mechanism" — `drive_confined_fiber` + `run_p5_gate` F1–F4; fcc/fc reproduces Mander ≤2.9% from a self-mobilized hoop pressure; oracle-only, C++ view=P5b), #381 P5b confined-fiber C++ VIEW (kernel `driveConfinedFiber` — nested 3-unknown lateral Newton condensing {11,22,12} so the EFFECTIVE lateral stress balances the passive hoop `σ_hoop(ε_lat)=min(K·ε_lat,fy)`, then `damagedUpdate`; condensed consistent tangent `C_RR=Cdam_RR−Cdam_RL(Ceff_LL+H)⁻¹Ceff_LR` mixing the DAMAGED output rows + the EFFECTIVE constraint block + the hoop stiffness `H` on the lateral diagonal, reducing to a plain static condensation where ω→0; new `DIM_BEAMFIBER` view `getCopy("BeamFiber")` order-3 {00,01,02} consumed by stock `NDFiberSection3d`; parser `-hoop K <-hoopFy fy>` + send/recv +2 fields; oracle gained the byte-faithful single-step primitive `confined_step` (gate F5==`drive_confined_fiber` 0.0); g++ `CFIB` byte-check 3 cases — reduce-to-free/confined/hoop-yield ~1e-11; element `zeroLengthSection`+`NDFiber` reduce-to-oracle + confinement-gain tests. Lateral balance on the EFFECTIVE stress matches the shipped P5a oracle (ω≈0 at peak ⇒ Mander match unaffected — LEDGER_quirk)) |
| **LadrunoEmbeddedRebar** — embedded-reinforcement **coupling** element ([[20_ladruno_embedded_reinforcement_adr]], **v2 slice 2**) tying one rebar node to a host solid element's nodes via precomputed shape-function weights `N_i`, so a rebar mesh embeds in a **non-matching** concrete mesh. Pure coupling (bond) — the rebar's own axial stiffness lives on a separate `corotTruss`/beam along the bar. External nodes `[rebar, host_1..host_M]`; gap `g = u_rebar − Σ N_i u_host_i` decomposed along the bar axis `dir` into axial slip `s` + transverse `g_t`; traction `t = F_axial(s)·dir + kt·g_t`, `F_axial` = perfect (`kAxial·s`) or bond (`LadrunoBondSlip` σ(s)·`bondScale`, `bondScale=perimeter·L_trib`). `P = Bᵀt`, `K = BᵀDB`, `D = (dF/ds)·dir⊗dir + kt·(I−dir⊗dir)` (anisotropic, D6). **MODE P** (penalty) — implicit + explicit (adds stiffness only). **MODE T (transformation/DOF-elim) DEFERRED:** the perfect-bond `u_rebar=ΣN_i u_host_i` needs a **multi-retained** constraint the stock single-retained `MP_Constraint` can't express (a separate larger lift — recorded). Host-agnostic (weights precomputed by the apeGmsh generator); rebar element type (truss\|beam, D6) is the user's choice on the rebar mesh. Parse `element LadrunoEmbeddedRebar tag rebarNode {nHost h1..hN \| -host eleTag} {-shape N1..NN \| -xi x1..x_ndm} -dir dx dy dz (-bond matTag [-bondScale bs] \| -perfect kAxial) [-kt {kt\|auto}] [-ktAlpha a] [-corot {-xiB.. \| -shapeB..}] [-enforce {penalty\|al}] [-bipenalty {-dtcr dt \| -wcap β}]`; setResponse `slip`/`bondStress`/`axialForce`/`force` + `kt`/`penaltyEnergy`/`constraintViolation`/`bondEnergy`/`dir`/`augLambda`/`gap`/`mpenalty`/`dtcr`. **§10 Mode-P roadmap (ADR 20):** `-host`/`-xi` host-query forms (#175); `-kt auto` host-stiffness-scaled penalty + energy/diagnostic responses (#177); `-corot` co-rotated bar axis lifting the small-rotation limit (#180); `-enforce penalty\|al` augmented-Lagrangian kernel (per-step Uzawa, gap→0 at moderate kt; bond-slip axial untouched) (#181). **`-bipenalty` (§10.6, build-order item 5):** mass penalty `m_p` lumped on the rebar node so the explicit `dt_cr` of the GLOBALLY assembled system (host carries real mass) is BOUNDED + reported via `eleResponse "dtcr"`. `k_eff=max(k_axial,kt)`; two budgets `-dtcr dt` (`m_p=k_eff(dt/2)²`, no host) / `-wcap β` (`m_p=k_eff/(β·ω_host)²`, `ω_host=√(‖K_host‖/‖M_host‖)`). Lumped-on-slave keeps M diagonal (DiagonalSOE-safe); element refuses Rayleigh factors (no spurious dt shrink); gated on `-enforce penalty` (AL⇒`m_p=0`). Default OFF⇒mass≡0, bit-identical. **Adversarial-review correction:** bipenalty does NOT make the element visible to the per-element `CriticalTimeStep` EIGENSOLVE (that BC/host-mass-blind problem sees this coupling element as `λ_max=0` — the massless free host slaves out the constraint). **§10.6.1 seam (SHIPPED):** a vanilla `virtual double Element::getExplicitCriticalTimeStep()` (default −1) folded into `CriticalTimeStep`'s per-element loop carries the element's self-reported `2√(m_p/k_eff)` into the running min (skipping the eigensolve for it), so `ops.criticalTimeStep`/`-cflAbort` now honor the embedded bound. Vanilla `Element.{h,cpp}` + `CriticalTimeStep.cpp` edits ([[LEDGER_vanilla_files]]). Test `test_bipenalty_governs_cfl_critical_step` (embedded `1e-3` bound governs over the host brick's ~0.04). | Element | **ELE_TAG 33005** (Ladruno element band, after LadrunoIMKBeam2d=33004) | `SRC/element/ladrunoEmbeddedRebar/{LadrunoEmbeddedRebar.{cpp,h},OPS_LadrunoEmbeddedRebar.cpp,LadrunoEmbeddedKernel.{cpp,h},CMakeLists.txt}`, `tests/test_ladrunoEmbeddedRebar_element.py` | shipped (Mode P + §10 quick-wins/AL); **bipenalty (§10.6) code-complete + Zone-A battery** (m_p formula + self-reported dt_cr, both ω_host paths, -wcap host/massless guards, AL-disables-bipenalty, explicit SDOF stable below / unstable above the -dtcr target); build verified via CI. **getDamp crash fix (2026-06-09):** the no-op `setRayleighDampingFactors` suppresses base `Element::getDamp`'s lazy scratch alloc ⇒ `theMatrices[-1]` OOB crash on the first IMPLICIT (Newmark/HHT) transient step (the quasi-static + explicit legs never exercise it); added zeroed element-owned `getDamp`/`getRayleighDampingForces` overrides (D==0 for a pure penalty coupling) + a `Newmark` smoke regression `test_transient_newmark_smoke`. Mirrors the RBE3 (33011) fix; see [[LEDGER_quirks]]. **ADR 23 Phase 0 — shared `LadrunoEmbeddedKernel.{h,cpp}`** (compiled-helper TU per ADR 23 D1/§13 M3, NOT a header-only OpenSees-free leaf: `maxAbsDiagonal` / `effectiveCouplingStiffness` / `massPenaltyDtcr`+`Wcap` / `criticalTimeStep` / `computeGap` / `assembleResistingForce` / `assembleTangent` — the constitutive-agnostic numerics; **bit-identical** refactor of the rebar, gated by its Zone-A battery; reused by the planned `LadrunoEmbeddedNode` 33006) | [#169](https://github.com/nmorabowen/OpenSees/pull/169), [#171](https://github.com/nmorabowen/OpenSees/pull/171), [#175](https://github.com/nmorabowen/OpenSees/pull/175), [#177](https://github.com/nmorabowen/OpenSees/pull/177), [#180](https://github.com/nmorabowen/OpenSees/pull/180), [#181](https://github.com/nmorabowen/OpenSees/pull/181), bipenalty [#186](https://github.com/nmorabowen/OpenSees/pull/186), CFL seam [#190](https://github.com/nmorabowen/OpenSees/pull/190), shared kernel [#199](https://github.com/nmorabowen/OpenSees/pull/199) |
| **LadrunoEmbeddedNode** — general **node-to-host** coupling element ([[23_ladruno_embedded_node_adr]]; user/dev guide [[LadrunoEmbeddedNode_guide]]; **Phase 1 = the isotropic translational tie U**) tying one constrained node's translations to a host element's nodes via shape-function weights `N_i`, so an arbitrary node embeds in a **non-matching** host mesh (mesh stitching, point embedding, SSI). **v1 SCOPE (ADR §14 credibility re-scope, 2026-06-07): SUPPORTED CORE = the U translational tie + `g0` stress-free birth (#214) + penalty/AL/bipenalty enforcement for the U tie + `-kt auto` — the only path validated as "world-class" (battery = [[27_ladruno_embedded_node_validation_plan]], benchmarked vs Abaqus `*EMBEDDED REGION` / LS-DYNA `*CONSTRAINED_BEAM_IN_SOLID`). UR / UP / D9 / `-corot` are KEPT but EXPERIMENTAL (flag-gated, NOT validated): UR = `½curl(u)` SPIN not moment transfer (CST/TET4 → rigid spin); UP = niche poromechanics; D9 = interface/contact-flavored (LadrunoContact/LadrunoBondSlip lineage), uncoupled friction only approximate. Must-fix before D9 promotion: `getInitialStiff` per-direction INITIAL tangent (no `setTrialStrain` side effect); + retroactive sendSelf version field.** **Isotropic sibling of `LadrunoEmbeddedRebar`** sharing `LadrunoEmbeddedKernel` (ADR 23 Phase 0): same penalty/AL + `-k auto` + bipenalty machinery, but `D_u = K_u·I` (no bar axis, no axial/transverse split, no bond-slip; the isotropic tie is already frame-objective ⇒ the U/UP/UR **penalty** tie needs **no `-corot`** — LEDGER_quirk; `-corot` exists only for the D9 **material** frame, v2 below). External nodes `[cNode, host_1..host_M]`; gap `g = u_c − Σ N_i u_host,i` (translational); traction `t = K_u·g` (+ `λ` for AL); `P = Bᵀt`, `K = BᵀD_uB` assembled compactly via the kernel then **scattered** into the full per-node-ndf layout (`nDOF = Σ ndf_i`; constrained/host nodes may carry more DOFs than ndm — only the first ndm translational DOFs coupled). **Phase 1b adds UP** (`-pressure`): also ties the constrained node's pressure DOF (index ndm) to the host's interpolated pressure, `g_p = p_c − Σ N_i p_host,i`, `t_p = K_p·g_p` (+ `λ_p` AL) — a scalar coupling scattered to the pressure slots, **activated only if all coupled nodes are u-p** (ndf≥ndm+1) else degrades to U-only; `-kp` value (host pressure-block auto-scale deferred); the pressure mode is NOT bipenalty-bounded (implicit-recommended, M1). **Phase 2 adds UR** (`-rot`): ties the constrained node's ROTATION DOFs to the host CONTINUUM rotation `θ=½curl(u)=skew(∇u)` at the embedded point — `3D`: `θ=½Σ_i(∇N_i×u_i)` (3 rotations); `2D`: the single drilling `R_z=½(∂N/∂x·u_y−∂N/∂y·u_x)`. Reads the host cartesian gradients `∂N_i/∂x` via a NEW vanilla `Element::getInterpolationGradients(ξ,dNdx)` (default −1; overridden on `LadrunoBrick` via `shp3d` + `BezierTet10` via `computeJacobian`) — supplied explicitly with `-dNdx` (the gradient analog of `-shape`, any host), queried with `-gradXi ξ..`, or auto-queried by `-xi` on a gradient-capable host. `g_r=θ_c−θ_host`, `t_r=K_r·g_r` (+`λ_r` AL), `D_r=K_r·I`; the rotation `B`-block (cNode rot → +I, host trans → −½skew(∇N_i)) scattered to the cNode rotation slots (disjoint from U/UP). `K_r` via `-kr` (numeric) or `-kr auto`=`krAlpha·K_u·lch²` (first `getCharacteristicLength()` call in the family; needs `-host`). **Activated only if the cNode carries rotation DOFs** (ndf≥ndm+nrot: 3D→6, 2D→3) else degrades to no-UR (warn). **M5/D7-1 parse guard: `-rot` and `-pressure` mutually exclusive** (2D ndf=3 ambiguity). **M1/ES-1 per-DOF-class bipenalty**: rotational inertia `I_p=K_r·(dt/2)²` lumped on the cNode rotation DOFs (same budget as `m_p` but with `K_r` ⇒ `dt_r=dt_u`; a translational-only `m_p` can't bound the rotation mode). UR is APPROXIMATE/mesh-limited (UR-4: CST/TET4 host → single rigid-spin tie; moment-critical needs higher-order host). `-k auto` = `K_u = kAlpha·max\|K_host(i,i)\|` (M2-corrected: getInitialStiff diagonal, no lch). Parse `element LadrunoEmbeddedNode tag cNode {nHost h1..hN \| -host eleTag} {-shape N1..NN \| -xi x1..x_ndm} [-k {Ku\|auto}] [-kAlpha a] [-pressure [-kp Kp]] [-rot [-kr {Kr\|auto}] [-krAlpha a] {-dNdx .. \| -gradXi ξ..}] [-enforce {penalty\|al}] [-bipenalty {-dtcr dt \| -wcap β}]`; setResponse `force`/`gap`/`k`/`penaltyEnergy`/`constraintViolation`/`augLambda`/`mpenalty`/`dtcr` + `pgap`/`pforce`/`plambda` (UP) + `rgap`/`rforce`/`rlambda`/`kr` (UR). **Phase 2b adds D9 — the material-driven interface** (`-matN`/`-matT1`/`-matT2`): any translational local direction (normal `e_0`, tangents `e_1/e_2` from `-normal nx ny [nz]` + optional `-orient`) carries a **uniaxial material** instead of the bare penalty K_u (penalty fallback where no `-mat*`). `t=Σ_d f_d(g·e_d)·e_d`, `D=Σ_d k_d·e_d⊗e_d`, `(f_d,k_d)`=mat_d stress/tangent in **FORCE units** (D9-4 — NO bondScale). Models: cohesive (softening uniaxial), unilateral gap (ENT/ElasticPPGap on the normal), elastic bedding (Elastic), bond. Coulomb friction only APPROXIMATE (uncoupled = fixed ElasticPP slip, not μ·N; rigorous → `LadrunoContact`). Full material lifecycle + broker serialization. **M4/D9-3:** AL augments only PENALTY directions and re-projects λ off material directions each commit. **M6/ES-4:** bipenalty `k_eff=max(K_u, material initial tangents)` (stiffening re-contact breaks dt_cr — use `-dtcr` from closed-contact stiffness or `-enforce al`). setResponse +`localGap`/`localForce`/`normal`. **Phase 2b v2 — `-corot` frame co-rotation:** the material frame tracks the host continuum rotation `θ_host=skew(∇u)\|_ξ` at the embedded point, `frameCur=R(θ_host)·frame` (3D Rodrigues exp-map of the axial vector / 2D drilling planar rotation), so a contact normal follows the deformed host. Reuses the Phase-2 `rotOper`/`∂N/∂x` machinery (`-dNdx`/`-gradXi`/`-xi`; `θ_host` factored into `hostContinuumRotation`). Material-mode-only (parse-time reject otherwise); resolves `corotActive` in `setDomain` (needs `gradN`). The co-rotated frame drives the traction/tangent, the AL λ-reprojection (frame rotation alone can no longer leak λ onto a material axis — M4/D9-3 now under rotation), and the local responses. **Dropped `∂e_d/∂u` tangent (R7/D9-5):** residual exact, tangent inexact (frame-objective explicit / step-halving implicit; exact when host DOFs are prescribed since `θ_host` is host-only). Serialization extends to `corot`+`gradN` (hdr→27). **Still DEFERRED:** auto host-face-normal (→`LadrunoContact` surface projection). **Phase 2c — initial-gap (offset) capture (D10, stress-free staged activation):** v1 tied ABSOLUTELY (`g = u_c − Σ N_i u_host`), so an element added mid-analysis to an already-deformed host yanked the slave by the accumulated host disp (the parent `ASDEmbeddedNodeElement` m_U0 behavior v1 dropped). Now at `setDomain` the element captures each active gap ONCE (`g0`/`gp0`/`gr0`, `g0Computed` guard) and drives ALL traction from the RELATIVE gap `(g−g0)` — subtracted INSIDE `computeGap`/`computeGapP`/`computeGapR` so every consumer (penalty, tangent, AL Uzawa, recorder probes, **and the D9 material `setTrialStrain` ⇒ interface materials born UNSTRAINED**) sees it. Force- AND stress-free; composes with `-corot` (`g0` global, subtracted before frame projection). No-op at the undeformed state (`g0=0`) ⇒ v1 battery byte-identical. ON by default; `-absolute`/`-noInitGap` keeps the legacy absolute tie / snap-to-host. UR linearized ⇒ `gr0` exact small-rotation, approximate large. Serialization +`initGapCapture`/`g0Computed`+`g0`/`gp0`/`gr0` (hdr→29; `g0Computed` restored ⇒ no re-capture). setResponse +`initGap`/`offset`. 2D + 3D (`ndm`-parametric). | Element | **ELE_TAG 33006** (Ladruno element band, after LadrunoEmbeddedRebar=33005) | `SRC/element/ladrunoEmbeddedNode/{LadrunoEmbeddedNode.{cpp,h},OPS_LadrunoEmbeddedNode.cpp,CMakeLists.txt}` (+ shared `../ladrunoEmbeddedRebar/LadrunoEmbeddedKernel.{cpp,h}`; + vanilla `Element::getInterpolationGradients` overridden on `LadrunoBrick`/`BezierTet10`), `tests/test_ladrunoEmbeddedNode_element.py` | Phase 1a (U) shipped + Zone-A green on ladruno (#202); Phase 1b (UP) shipped (#205); Phase 2 (UR) shipped (#208); Phase 2b v1 (D9 material interface) shipped (#210); Phase 2b v2 (`-corot` frame co-rotation) shipped (#212); **Phase 2c (initial-gap capture)** — Zone-A battery (staged born-stress-free on a deformed LadrunoBrick, -absolute jolt/yank negative control, undeformed no-op byte-identical, FE_Datastore send/recv roundtrip). **getDamp crash fix (2026-06-09):** the no-op `setRayleighDampingFactors` suppresses base `Element::getDamp`'s lazy scratch alloc ⇒ `theMatrices[-1]` OOB crash on the first IMPLICIT (Newmark/HHT) transient step (the quasi-static + explicit legs never exercise it); added zeroed element-owned `getDamp`/`getRayleighDampingForces` overrides (D==0 for a pure penalty coupling) + a `Newmark` smoke regression `test_transient_newmark_smoke`. Mirrors the RBE3 (33011) fix; see [[LEDGER_quirks]]. | shared kernel [#199](https://github.com/nmorabowen/OpenSees/pull/199), Phase 1a [#202](https://github.com/nmorabowen/OpenSees/pull/202), Phase 1b UP [#205](https://github.com/nmorabowen/OpenSees/pull/205), Phase 2 UR [#208](https://github.com/nmorabowen/OpenSees/pull/208), Phase 2b v1 D9 [#210](https://github.com/nmorabowen/OpenSees/pull/210), Phase 2b v2 corot [#212](https://github.com/nmorabowen/OpenSees/pull/212), Phase 2c initial-gap [#214](https://github.com/nmorabowen/OpenSees/pull/214) |
| **LadrunoDistributingCoupling** — RBE3 / distributing-coupling element ([[28_ladruno_distributing_coupling_rbe3_adr]]; user/dev guide [[LadrunoDistributingCoupling_guide]]): a **reference node R** (6-DOF) whose motion is the **weighted least-squares rigid-body fit** of a set of **independent nodes** (3-DOF) — equivalently, a load at R **distributes** to the set as a statically-equivalent force set with **no stiffening** of the set. = Abaqus `*COUPLING,distributing` / LS-DYNA `*CONSTRAINED_INTERPOLATION` / Nastran RBE3. **Driver = the ndf-mismatch MOMENT-transfer interface** (a 6-DOF beam/shell node into a face of 3-DOF solid nodes — the moment enters the continuum as a distributed force couple, the face stays free to deform; contrast RBE2/kinematic-coupling which rigidifies the patch). Weighted centroid `x_c=Σw_i x_i/W`, `r_i=x_i−x_c`, weighted position-inertia `I_c=Σw_i[(r_i·r_i)1−r_i⊗r_i]`. Gaps (kinematically complete for an OFFSET reference): `g_t=(u_R+θ_R×(x_c−x_R))−(1/W)Σw_i u_i`, `g_r=P θ_R−I_c⁺ Σw_i(r_i×u_i)` with `I_c⁺` the **spectral pseudo-inverse** (drops collinear/degenerate rotation axes — flat face is FULL-rank, the debunked false premise) and `P` its range projector. Penalty `P=Bᵀt, K=BᵀDB, D=diag(K_t I, K_r I)`; force split `f_i=(w_i/W)t_t+w_i(α×r_i), α=I_c⁺t_r` is the work-conjugate transpose (net force AND moment balance exact for any K). The TRANSLATION block reuses the weight-agnostic `LadrunoEmbeddedKernel` math; the ROTATION + transport blocks are RBE3-specific (cross-DOF lever-arm coupling the per-node kernel can't assemble) and built via a constant `B` resolved at setDomain from node coords (3×3 Jacobi eigensolve for I_c). **Explicit-safe**: R is the massless node ⇒ derived rotational penalty `K_r=K_t·(Σw_i|r_i|²/W)` so the rotation mode self-bounds (else the zero-mass-rotational CriticalTimeStep trap); bipenalty `-bipenalty -dtcr` lumps `m_p`/`I_p` on R; AL `-enforce al` implicit-only; `-k auto` needs a representative `-host`. g0 initial-gap capture for stress-free staged activation. 2D + 3D. Plan/ADR: [[28_ladruno_distributing_coupling_rbe3_adr]] (formulation locked + 4-lens adversarial review). | Element | **ELE_TAG 33011** (Ladruno element band, after LadrunoCST=33008; 33009/33010 reserved for VEM/SBFEM) | `SRC/element/ladrunoDistributingCoupling/{LadrunoDistributingCoupling.{cpp,h},OPS_LadrunoDistributingCoupling.cpp,CMakeLists.txt}` (reuses `../ladrunoEmbeddedRebar/LadrunoEmbeddedKernel`), `tests/test_ladrunoDistributingCoupling_element.py` | shipped (**MERGED to ladruno**) — Zone-A battery 15/15 (weighted-avg translation, LSQ rotation fit, offset-reference transport, rigid-body zero gap, force/moment balance, flat-face full-rank + collinear drop, derived K_r, bipenalty dt_cr, AL, transient Newmark getDamp regression, FE_Datastore roundtrip) + 4-lens code review. **ADR #218 auto-merged the doc only → impl recovered via #219** (the [[feedback_stranded_commits_after_automerge]] trap) | ADR [#218](https://github.com/nmorabowen/OpenSees/pull/218), impl [#219](https://github.com/nmorabowen/OpenSees/pull/219) |
| **LadrunoKinematicCoupling** — RBE2 / kinematic-coupling element ([[29_ladruno_kinematic_coupling_rbe2_adr]]; user/dev guide [[LadrunoKinematicCoupling_guide]]): a **reference node R** that **RIGIDLY drives** a set of **slave nodes** via a per-slave rigid tie `u_i=u_R+θ_R×d_i` (and `θ_i=θ_R` where the slave's rotation is tied), `d_i=x_i−x_R`. = Abaqus `*COUPLING,kinematic` / LS-DYNA `*CONSTRAINED_NODAL_RIGID_BODY` / Nastran RBE2 — the **rigid sibling of RBE3** (adds stiffness to the slaves; R is the MASTER, not the dependent — the roles flip). Gaps `g_i^t=u_i−(u_R+θ_R×d_i)` (translation; transport block `∂/∂θ_R=+[d_i]_×` = a **sign FLIP** of RBE3's transOp since RBE2 carries `−θ_R×d_i`), `g_i^r=θ_i−θ_R` (rotation; identity blocks). `P=Bᵀt, K=ΣBᵢᵀD_iBᵢ`, self-equilibrated for any K. **NO least-squares fit, NO weighted centroid, NO `I_c`/eigensolver/pseudo-inverse, NO weights** (much simpler than RBE3). **`-dof` selects the dependent components** on each slave (1..ndm trans, ndm+1..ndm+nrot rot; default = all the slave possesses) — one element subsumes translation-only / translation+transport / full-rigid; parse-time range validation; mixed 3-/6-DOF slave sets → **RAGGED per-row gap layout** (gapNode/gapDof/gapIsRot resolved once at setDomain). **Explicit-safe with the RBE3→RBE2 role-inversion fixes (4-lens review):** R is frequently a **real massed node** (platen/footing/equipment) so **`-bipenalty` DEFAULTS OFF** and, when on, lumps a penalty mass only on tied DOFs that are ACTUALLY massless via a **scan over R AND every slave** (a massless *slave* is the RBE2 hazard RBE3 never had), sized from the **Gershgorin row-sum** of the assembled penalty tangent (≥ λ_max ⇒ conservative, no eigensolve); default `K_r=K_t·ℓ²` with **floored ℓ²** (all-coincident slaves can't zero a tied rotation); refuses self-tie / duplicate slave; AL `-enforce al` implicit-only; getDamp/getRayleighDampingForces overridden (the transient C-tangent index-landmine). g0 initial-gap capture; 2D + 3D. Plan/ADR: [[29_ladruno_kinematic_coupling_rbe2_adr]] (formulation locked + 4-lens adversarial review, 2 CRITICAL + 6 MAJOR folded in). | Element | **ELE_TAG 33012** (Ladruno element band, after LadrunoDistributingCoupling=33011) | `SRC/element/ladrunoKinematicCoupling/{LadrunoKinematicCoupling.{cpp,h},OPS_LadrunoKinematicCoupling.cpp,CMakeLists.txt}` (reuses the scalar bipenalty helpers from `LadrunoEmbeddedKernel.h`), `tests/test_ladrunoKinematicCoupling_element.py` | shipped (**MERGED to ladruno**) — MSVC OpenSeesPy clean; **Zone-A battery 16/16** (rigid-body zero gap, rotation+transport SIGN, -dof full-rigid vs translation-only, force/moment transfer into a 3-DOF face, reduce-to-rigidLink, AL, bipenalty massless-REF + massless-SLAVE scan + massed-R no-double-count, transient Newmark getDamp regression, self-tie/all-coincident degeneracy negatives, derived K_r, FE_Datastore roundtrip); full Zone-A **633 passed / 1 xfail no-regression** + 4-lens code review (2 CRITICAL + 6 MAJOR folded in) | [#221](https://github.com/nmorabowen/OpenSees/pull/221) |
| **LadrunoProjectionHandler** — explicit constraint-PROJECTION handler ([[30_ladruno_explicit_constraint_projection_adr]]; design [[_adr30_p1_design]]): enforces `MP_Constraint`s in explicit dynamics by **M-orthogonal projection of the solved accelerations** `a_proj = L(LᵀML)⁻¹LᵀM a_raw` per connected-component group (L=[I;C]), instead of penalty (eats `dt_cr`) or Transformation+Diagonal (silently drops the condensed off-diagonal mass — falsified in P0/T6). = LS-DYNA `*CONSTRAINED_LINEAR` / nodal-set `a=ΣMᵢaᵢ/ΣMᵢ` (Theory §28). **Exact, momentum-conserving, Δt-neutral (Galerkin-reduced pencil interlaces), keeps the O(n) `system Diagonal` recipe.** Plain-style assembly does NOT eliminate slave DOFs (they keep their own equation + diagonal mass); a small `LadrunoProjectionConsumer` interface lets `CentralDifferenceLadruno` adopt it (project a0 in the starter + a_{n+1} in update); ExplicitBathe/LNVD + their SMS variants adopt it in P5 (the whole explicit family is now projection-aware). Mass read from the assembled Diagonal-SOE diagonal (the exact inverted M) between formTangent and solve (Gate-A R2). IC check in the MP increment convention `(u_c−Uc0)=C(u_r−Ur0)` (R1); SP-fixed-retained → slave zeroed (R4); consistent-mass guard (R5); handle()-time diagnostics (chain/double-slave/SP∩slave/zero-free) + buildMass massless/singular-LᵀML refusal. **v1 = equalDOF (P1); rigidLink/rigidDiaphragm transport = P2, EQ + tie-force query = P3.** Gate-A (15-agent pre-code math panel): algebra core SOUND, 10 seam/hook fixes folded. | Constraint handler (+ projector + consumer interface) | **HANDLER_TAG 33001** (first fork handler, private band) | `SRC/analysis/handler/{LadrunoProjectionHandler.{cpp,h},LadrunoConstraintProjector.{cpp,h},LadrunoProjectionConsumer.h}`, hooks in `SRC/analysis/integrator/CentralDifferenceLadruno.{cpp,h}` (+ inherited by CentralDifferenceSMS/Consistent) and `ExplicitBathe.{cpp,h}` / `ExplicitBatheLNVD.{cpp,h}` (P5; + inherited by the 4 SMS/Consistent subclasses 33009–33012), `tests/test_adr30_projection_p0.py` + `tests/test_adr30_projection_p1.py` (T1-T5+UAF) + `tests/test_adr30_projection_p2.py` (T3/T6fix/T7+bdry) + `tests/test_adr30_projection_p3.py` (T8/T9/EQ/guard) + `tests/test_adr30_projection_p4{,b,c}.py` + `tests/test_adr30_projection_p5.py` (EB/LNVD/SMS adoption) | shipped — **P0 MERGED [#300]; P1 equalDOF MERGED [#305]; P2 general-C transport MERGED [#307]** (Gates A/B/C + general pre-merge review all SOUND; fixes the P0/T6 mass-drop, rel<1e-6 vs dense, gap~1e-12; §2.5: every tied DOF needs lumped mass). **P3 (tie-force query `ladrunoProjectionTieForce` + EQ_Constraint groups + -verbose) DONE** — projector caches `f=M(a_raw−a_proj)`; EQ = multi-master row reusing the general-C path; T8 (±F·m₂/M equal/opposite), T9 (energy drift<1e-3), EQ enforced, query-guard. v1 COMPLETE (P3 MERGED [#312] + handoff [#313]). **P4a native tie-force recorder DONE** — dedicated nodal `Node::projTieForce` slot, CentralDifferenceLadruno scatters it each commit, new `ConstraintTieForceSource`/`constraintTieForce` writes `CONSTRAINT_TIE_FORCE` to the .ladruno HDF5 (test p4 records + h5py-reads ±F·m₂/M); focused review PASS, full ZoneA 942 (MERGED [#317]). **P4b prescribed-motion (non-homogeneous SP / imposedMotion) Tier 1 DONE** — handler `applyLoad()` override imposes the prescribed DISPLACEMENT on the node each step (mirrors `TransformationDOF_Group::enforceSPs`: `setTrialDisp(getValue()+getInitialValue(),dof)`); `ImposedMotionSP` supplies vel/accel itself; the DOF stays eqn-excluded (−1) so the integrator is UNCHANGED and the free DOFs feel the support through `F_int`/damping (`DOF_Group::setNode*` skips eqn<0, preserving the imposed value). Pre-code design gate (5 read-only refute lenses + verify) found the approach SOUND. A prescribed DOF used as a constraint MASTER (would silently force slaves' accel to 0) or SLAVE (overconstraint) is REFUSED with named errors; Tier 2 (drive slaves from a prescribed master = the literal "overwrite a before projecting" known-RHS projection) deferred to P4c. ZERO vanilla touch (fork handler only). Tests `tests/test_adr30_projection_p4b.py` (TP1 base-excitation vs Transformation+FullGeneral rel<1e-6, TP2 constant settlement, TP3/TP4 refusals). Remaining P4 deferred (chain composition / ExplicitBathe / near-singular gate / frozen-Ccr guard). **P4c prescribed MASTER (Tier 2) DONE** — a slave driven PURELY by prescribed master(s) is KINEMATICALLY imposed (excluded from the eqn set + u/v/a set from the masters each step in applyLoad, Tier-1 extended to slaves), tracking the master EXACTLY (drift 0, vs Transformation 0). The ADR §2.4 known-RHS *acceleration* projection was implemented first then ABANDONED: a prescribed master's disp is externally imposed while the slave leap-frogs, so accel-only projection leaves a NON-converging O(1e-3) displacement-tie drift (the drift grew as dt diverged from the GM's dtInt). Mixed (free+prescribed master on one slave) refused; zero-free-DOF refused (clean error — was a segfault, ADR §5.2). Handler-only (`classifyDerivedSlaves`/`applyLoad`/`doneNumberingDOF`), projector UNTOUCHED, ZERO vanilla. Tests `tests/test_adr30_projection_p4c.py` (TC1 imposedMotion→equalDOF master, TC2 constant SP master, TC3 rigidLink derived-ux+projected-uy, TC4 mixed refused, TC5 prescribed-slave refused, TC6 zero-free-DOF refused). **P5 ExplicitBathe / LNVD adoption DONE** — the `LadrunoProjectionConsumer` is now implemented by the Noh–Bathe family: only `ExplicitBathe` and `ExplicitBatheLNVD` were edited (the 4 SMS/Consistent subclasses 33009–33012 inherit it, all chaining `domainChanged()` to the base; the CentralDifference family already had it via inheritance). Each base reads `diag(M)` once per stage (one-shot `formTangent`+`buildMass` in `newStep` before the algorithm's solve), projects the committed a0 at the starter, and projects BOTH Noh–Bathe sub-step accelerations in `update()` (after any consistent-mass `refineAccel`); load-bearing carry `A_t=A_tdt` (projected) at commit; `commit()` tie-force scatter replicated so the P4a recorder works under these integrators. Handler-only besides the two integrators; ZERO vanilla (all six integrators are fork-authored). Tests `tests/test_adr30_projection_p5.py` (TP5-1 tie-exact, TP5-2/3 projection==Transformation under EB/LNVD, TP5-4 FLAC-damped tie, TP5-5/6 inherited ExplicitBatheSMS + ExplicitBatheLNVDSMSConsistent, TP5-7 no-projection regression, TP5-8 constraintTieForce recorder readback) — 8/8; full ADR-30 battery 44/44. **P6 condition gate + frozen-Ccr staleness guard DONE (closes ADR O1)** — two warn/refuse-only robustness guards, NO change to the projection math. (3a) `buildMass` replaced its exact-pivot test-solve with a real **condition-number gate**: a self-contained symmetric **Jacobi** eigensolve (`ladrunoSymEigJacobi`, no LAPACK) estimates `cond=λmax/λmin` of each SPD `LtML`; **refuse** >1e12 (also catches `λmin≤0`), **warn** >1e8 — catches the near-singular sets (disparate tied masses, near-redundant constraints) the old check missed. (3b) handler **`flagRotMonitor`** records masters whose Ccr couples a rotational master DOF into a translational slave DOF (rigidLink-beam / rigidDiaphragm lever arm; equalDOF rotation-ties NOT flagged), captures `θ0`, and `applyLoad()` warns ONCE per master when rotation drift crosses 0.1 rad. Tests `tests/test_adr30_projection_p6.py` (TP6-1 healthy group runs, TP6-2 cond~1e13 refused, TP6-3 cond~1e10 warns-but-runs, TP6-4/5 staleness NOTE fires >0.1 rad / silent below) — 5/5; full ADR-30 battery 49/49. ZERO vanilla. | [#300](https://github.com/nmorabowen/OpenSees/pull/300) (P0), [#305](https://github.com/nmorabowen/OpenSees/pull/305) (P1), [#307](https://github.com/nmorabowen/OpenSees/pull/307) (P2), [#312](https://github.com/nmorabowen/OpenSees/pull/312) (P3), [#317](https://github.com/nmorabowen/OpenSees/pull/317) (P4a), [#323](https://github.com/nmorabowen/OpenSees/pull/323) (P4b), [#329](https://github.com/nmorabowen/OpenSees/pull/329) (P4c), [#334](https://github.com/nmorabowen/OpenSees/pull/334) (P5), _pending (P6)_ |
| **LadrunoContactHandler + LadrunoContactFE** — ContactDomain broad-phase contact subsystem ([[39_ladruno_contact_domain_adr]]; design [[_adr39_p1_design]]): a custom `ConstraintHandler` that injects contact `FE_Element` adapters into the assembly via `handle()` (the ONLY legal FE factory — runs inside `domainChanged` after `AnalysisModel::clearAll`). The hybrid implicit+explicit design rides the `FE_Element` contract: a `LadrunoContactFE` adapter computes its own narrow phase in `getResidual()`/`getTangent()` (reads `Node` trial disp like a real Element) — explicit consumes only the residual (CDL `formEleTangent` is mass-only), implicit consumes both (tangent scaled by `c1`). The adapter is a **stateless VIEW**: path-dependent pair state lives on the Domain-owned `LadrunoContactDomain` (P1b), committed via `Domain::commit()`/`revertToLastCommit()` `// Ladruno` hooks, so adapters are freely destroyed/rebuilt each `handle()`. Handler **REPLICATES** the PlainHandler DOF/FE loop (mirrors `LadrunoProjectionHandler` — composing collides FE tags) + appends the adapter above `numFe`. **P1a (first phase) = empty-connectivity zero adapter** proving the injection is graph-neutral (BITWISE-identical to no-contact under explicit CDL **and** implicit Newmark) — adapter owns its `Vector`/`Matrix` buffers (bare ctor ⇒ `myEle==0` ⇒ base helpers `exit(-1)`), force variants overridden to size-0 (silence the modal-damping `getM_Force` warning). Phased explicit-first: P1b (`LadrunoContactDomain`+surface+lifecycle hooks+parser) → P2 NTS penalty (per-segment connectivity + 1e-12 gate) → P2.5 bucket sort → P3 Coulomb friction (SHIP; clean direct return map — IMPL-EX dropped from v1) → P3.5 consistent friction tangent. Two adversarial gates so far: 4-agent design gate (SALVAGEABLE — 3 lifecycle BLOCKERs folded) + 1-agent code gate (4 fixes: broker reg, Tcl branch, ledgers, force-variant warning). | Constraint handler (HANDLER) + FE_Element adapter + Domain-owned engine | **HANDLER_TAG 33002** (after LadrunoProjection 33001); ELE_TAG **33016** reserved for the future P2 contact element (33015 was taken by `LadrunoRigidBody`, ADR-58 — see [[LEDGER_quirks]]) | `SRC/analysis/handler/{LadrunoContactHandler.{cpp,h},LadrunoContactFE.{cpp,h}}` + `SRC/domain/contact/{LadrunoContactDomain.{cpp,h},LadrunoContactSurface.{cpp,h}}` + commands in `OpenSeesOutputCommands.cpp`/`PythonWrapper.cpp`/`TclWrapper.cpp` + vanilla `SRC/analysis/fe_ele/FE_Element.cpp` (P2a subtype-ctor scratch-alloc fix) + **NEW header-only `SRC/domain/contact/LadrunoContactKernel.h`** (P2b NTS **normal-law only** — penalty traction tn=kn·<−gap>₊; projection EXTRACTED to LadrunoContactProjection.h at A2, friction EXTRACTED to LadrunoFrictionKernel.h at A1) + **NEW header-only `SRC/domain/contact/LadrunoContactProjection.h`** (ADR-41 / capstone Track A2: closest-point projection geometry shared by the NTS path AND the mortar GP loop — adds `projectFull` returning the covariant surface metric g[2][2] + φ_master; bit-for-bit with the moved P2b code; oracle `proto_a2_projection.py` 7/7) + **NEW header-only `SRC/domain/contact/LadrunoFrictionKernel.h`** (ADR-41 / capstone Track A1: Coulomb/Tresca friction return map + consistent tangent moved bit-for-bit from the kernel, GENERALIZED to the unified cone `cap=min(μN+c, τmax)` with the min()-selected non-symmetric Csl coupling; cohesion/τmax default to 0 ⇒ existing P3/P3.5 call sites unchanged; oracle `proto_a1_friction.py` Css/Csl vs FD ~1e-12) + **NEW header-only `SRC/domain/contact/LadrunoMortarKernel.h`** (ADR-41 / capstone Track C1: the first NET-NEW mortar mechanics — segment-to-segment overlap CLIP [Sutherland-Hodgman of slave∩master on a master-built auxiliary plane, fan-triangulated] → sub-triangle Gauss [3-pt / 6-pt order-4] → STANDARD-basis `D_IJ=∫N_I^s N_J^s`, `M_IK=∫N_I^s φ_K^m` + weighted gap `g̃_I=∫N_I^s·((x_s−x_m)·n)` [normal INSIDE the integral], `J=A_aux/|n_s·n0|`; each GP back-mapped to ξ_s by inverse 2D iso-map + projected to master via the SHIPPED `projectFull` [no re-derived geometry]; linearization `dD,dM,dn,dξ` a documented C2 stub. Oracle `proto_c1_mortar.py` 24/24: clip area incl. the 2(√2−1) octagon, partition of unity `ΣM=ΣD` 1e-12 matched+non-matched, **HEADLINE constant-pressure patch test on a non-matched 2-block mesh `D⁻¹M` 9.7e-16**, clip-exact-at-low-order vs clip-blind 43%-biased, g̃ linear-exact + FD; C++ standalone bit-for-bit. **Adversarial gate (3 parallel source-grounded reviewers → PASS/PASS/SALVAGEABLE, no blocker): the gates are STRUCTURALLY robust — `ΣM=ΣD` + the patch test both reduce per-GP to `Σφ=1`, unfalsifiable by geometry (≤5.6e-17 measured on tilted/distorted/OOB/sliver). Folded: `isConvex2` guard REFUSES non-convex (concave/bow-tie) facets — they'd otherwise integrate over the convex hull + truncate the C++ clip ~30% off the oracle; `inverseIsomap2D` gained a convergence flag (skip GP, no wrong-root); the warped non-planar `J=A_aux/|n_s·n0|` measure bias (~0.7%@0.3·edge lift) DOCUMENTED + deferred to C2/ADR-47's per-sub-tri Jacobian. Oracle now 30/30 (T8 concave-refused, T9 tilted-master ΣM=ΣD + ∫dΓ=area, T10 warped-bias bounded); see [[LEDGER_quirks]].** PURELY ADDITIVE — no classTag/wiring/manifest, null path byte-identical) + **NEW header-only `SRC/domain/contact/LadrunoContactBucketSort.h`** (P2.5 broad-phase grid), `tests/test_adr39_contact_p1.py` + `tests/test_adr39_contact_p2a.py` + `tests/test_adr39_contact_p2b.py` + `tests/test_adr39_contact_p2b2.py` + `tests/test_adr39_contact_p2b2b.py` + `tests/test_adr39_contact_p25_bucketsort.py` + `tests/test_adr39_contact_p3_friction.py` + `tests/test_adr39_contact_p35_implicit.py` | **P1 MERGED (#345); P2a = first LOAD-BEARING phase (PR #346).** P1a/P1b: zero-force plumbing, 7/7 Zone-A green (bitwise CDL+Newmark, commit-hook fires, wipe-clears, null=pure-Plain). **P2a: rigid analytical plane, penalty NTS, frictionless** — `contactPlane tag slaveSurf nx ny nz px py pz kn` + `LadrunoContactDomain::addRigidPlane` + `LadrunoContactFE` rigid-plane ctor (connectivity={slave}, B=nᵀ, `gap=n·(X+u−p0)`, `resid=−kₙ⟨−g⟩₊n`, tangent `kₙ(n⊗n)` routed through `integrator->formEleTangent` so CDL pulls mass-only / Newmark pulls c1·K_c). Tests: static pen=P/kₙ (1e-6) + static inclined pen=P/(kₙ·nx²) + explicit impact e=1.0/pen exact + inclined rebound + element-free teardown. **Adversarial gates: P2 design (SALVAGEABLE, folded) + P2a code-gate (SALVAGEABLE→PASS): 1 BLOCKER (FE_Element subtype-ctor scratch null-deref) FIXED at source + kₙ≤0 guard + surface-kind guard + ndf<ndm guard + `addKiToTang` override + 2 tests folded.** No new classTag (P2a rides HANDLER 33002); ELE_TAG **33016** reserved for the future P2b contact element (33015 was taken by `LadrunoRigidBody`, ADR-58). **P2b-1: faceted node-to-segment (NTS) penalty contact vs a FIXED master segment** — header-only `LadrunoContactKernel.h` (bilinear/linear shape, BOUNDED projection Newton w/ degenerate-segment reject, DIRECTION-oriented winding-immune normal, gap, penalty; oracle-first in `contact_prototypes/proto_p2b_nts.py` 7/7) + `LadrunoContactFE` SEGMENT ctor (conn={slave}∪{seg nodes}, resid=Bᵀtₙ, tangent kₙBᵀB routed thru formEleTangent; ∂n/∂u block + auto-kn = P2b-2) + handler one-adapter-per-(slave,segment) brute-force pairing (bucket sort = P2.5) + `contact … -outward ox oy oz` orientation. 19/19 green (static pen=P/kₙ quad+tri, oblique-30° pen=P/(kₙnz²), winding-flip immunity, OOB pass-through, explicit e≈1, ZeroLengthContactASDimplex cross-check rel-1e-6; P1b bitwise survives via the degenerate-segment sentinel under Plain numberer). **P2b-2a: DEFORMABLE-master validation (no new C++ — the P2b-1 FE/handler already read master trial-disp + assemble master tangent blocks):** `tests/test_adr39_contact_p2b2.py` 2/2 — slave on a fixed-bottom deformable LadrunoBrick top face (static Newton converges w/ the main kₙBᵀB tangent; gap=P/kₙ + brick comp=PL/EA exact) + block-on-block deformable-vs-deformable interface (per-node pen=(P/4)/kₙ). ⇒ the deferred ∂n/∂u block (gate TANG-1) is a CONVERGENCE REFINEMENT (large-rotation/curved), not a correctness gap. **P2b-2b: `-kn auto` automatic penalty sizing** — `contact … auto` (literal in the kn slot) sizes kₙ per (slave,segment) pair at `handle()` time from the OWNING solid element's initial stiffness, sourced GENERICALLY via base-`Element` virtuals (`getExternalNodes`/`getNumDOF`/`getInitialStiff`): auto-detect the owning 3-DOF/node solid by face-node-subset match, then **kₙ = f_si·mean_over_seg_nodes(nᵀ K_block_node n)**, f_si=0.10 (LS-DYNA SLSFAC). Element-stiffness form of LS-DYNA 26.14a `f·K·A²/V` (K_diag~E·L~E·A²/V) ⇒ NO element-type coupling (any solid), NO modulus extraction, NO new vanilla file (only the parser keyword). Oracle-first (`contact_prototypes/proto_p2b2b_autokn.py` 6/6: reduction + scaling kₙ∝E, kₙ∝L + mesh-objectivity). No-owning-solid/non-3-DOF/ambiguous-normal ⇒ pair skipped (warn, inert). SOFT Courant floor (26.15) deferred to P2b-2c. **P2.5: bucket-sort BROAD PHASE** — NEW header-only `LadrunoContactBucketSort.h` (§26.11 spatial grid: median-diag cell, bbox + runaway percentile clip, cap NX·NY·NZ≤min(nSeg,5000), segment-SPAN registration type-13/eq26.44, 27-neighbour `candidates()` with sparse-set de-dup) replaces the handler's brute-force O(nSlave·nSeg) pairing — per slave emit adapters only for the 27-neighbour candidate segments (a SUPERSET of every near pair ⇒ narrow phase unchanged ⇒ result identical, dead far-pairs dropped). `contact … -cell <frac>` knob (default 1.0; huge ⇒ 1 bucket = brute force). Oracle `proto_bucket_sort.py` P0a 4/4 (superset 0-miss, jittered, runaway, 33× prune). Gate=**verify==brute force** (`test_adr39_contact_p25_bucketsort.py`: 5×5-grid default-cell vs `-cell 1e12` bitwise-identical + interior-seg + single-seg regression). Code-gate (1 reviewer, 250k-probe superset re-derivation) NO BLOCKER/MAJOR; folded MINOR (wide de-dup counter) + doc reconcile. SCOPE: grid at handle() from REFERENCE coords ⇒ ==brute force for static/small-motion; large sliding needs epoch re-emit (follow-on). P2b-2c=∂n/∂u tangent+Hertz. **P3: Coulomb friction — v1 EXPLICIT ship, FORCE-only** (the v1 SHIP, critical junction) — adds the tangential traction to the shipped frictionless NTS contact. Kernel `frictionReturnMap` (clean Coulomb DIRECT return map, NOT the ASDimplex apparent-damage form; `tT*=kt(gTeff−gpT)`, cone `μN`, returns the already-NEGATED applied force `tFric=−tT` so the SIGN lives in ONE auditable place ⇒ friction OPPOSES the slave motion; `gpTtrial` a PURE fn of committed state ⇒ idempotent across CDL firstStep double-eval). Per-pair path-dependent friction STATE `{gpT,gpTtrial,gT0,engaged}` on `LadrunoContactDomain`, keyed `(contactTag,slaveTag,GLOBAL segIndex)`, committed/reverted at the `Domain::commit/revertToLastCommit` hooks (replaces the P1b counters), with dead-slot GC each `handle()` (live key-set, ADR-30 theEQs leak class) + cross-contact shared-slave warning. Adapter: ENGAGEMENT-referenced slip `gT0` captured at first activation (else late-engaging drift→spurious stick); `mu≤0` short-circuits before any slot touch ⇒ BYTE-IDENTICAL frictionless P2b; lazy engine re-fetch via `Domain*` (wipe deletes it). Tangent UNCHANGED (friction tangent + IMPL-EX = P3.5). Parser already threads `kt/mu` (no new surface). Oracle-first `contact_prototypes/proto_p3_friction.py` 6/6; design gate (2 reviewers→SALVAGEABLE) folded 9 findings (SIGN BLOCKER, engagement origin, drop-IMPL-EX-from-v1, GC leak, mu≤0 byte-identity). Tests (10): incline `a=g(sinθ−μcosθ)` through REAL CDL+addB (the sign gate) + slip-caps-μN + stick + energy-balance dissipation + frictionless regression + commit-fires + wipe-reanalyze + late-engagement gT0 + revert-path + deformable-master drag. **Code gate (3 parallel source-grounded reviewers): 2× PASS (sign/mechanics traced end-to-end through real addB/CDL; state/lifecycle all MINOR/NIT) + 1 SALVAGEABLE (tests) → folded the 3 missing tests (revert MAJOR-7, deformable-master friction block, late-engagement gT0).** **P3.5: implicit frictional Newton — the consistent friction TANGENT.** P3 is force-only ⇒ a free tangential DOF is SINGULAR under an implicit solve; P3.5 assembles `K_fric = Gᵀ[D_TT·P_t + d_TN⊗n]G` in `addKtToTang` (`LadrunoContactKernel::frictionTangentBlock` 3×3 K_ss + the G=[I|−N_i I] scatter) so static/Newmark Newton converges. **DEFAULT = the SYMMETRIC tangent (drop the d_TN⊗n column) — correct on ANY solver** (design-gate Q2 BLOCKER: symmetric SOEs ProfileSPD/BandSPD/SymSparse read only the upper triangle + silently drop the lower, and the FE can't see the SOE type ⇒ a non-sym default would silently corrupt). `contact … -consistanttan` opts INTO the non-symmetric consistent tangent (true quadratic, needs FullGeneral/UmfPack; parser warns). Residual unchanged (current-N, correct equilibrium); `addKiToTang` = stick-only `kt·GᵀP_tG` (SPD contraction); explicit (CDL) byte-identical (addMtoTang no-op ⇒ tangent never assembled). Oracle `proto_p35_implicit_tangent.py` 4/4 (slip==FD non-sym, stick==kt·P_t, symmetric variant, self-equilibrium). Design gate (2 reviewers: tangent-mechanics PASS — full-ndof FD 1.5e-9 + solver/active-set SALVAGEABLE → default flipped to symmetric); code gate (1 reviewer) PASS (transcription matches oracle exactly). Tests (6): static stick converges (THE gate — singular in P3) + symmetric-solver-safe (ProfileSPD) + `-consistanttan` ≤-iters + static slip w/ anchor + Newmark dynamic + explicit byte-identity. Active-set freeze + Hertz = P3.5b/P2b-2c. | P1 [#345](https://github.com/nmorabowen/OpenSees/pull/345); P2a [#346](https://github.com/nmorabowen/OpenSees/pull/346); P2b-1 [#354](https://github.com/nmorabowen/OpenSees/pull/354); P2b-2a [#355](https://github.com/nmorabowen/OpenSees/pull/355); P2b-2b [#357](https://github.com/nmorabowen/OpenSees/pull/357); P2.5 [#358](https://github.com/nmorabowen/OpenSees/pull/358); P3 friction [#360](https://github.com/nmorabowen/OpenSees/pull/360); P3.5 implicit tangent [#361](https://github.com/nmorabowen/OpenSees/pull/361) MERGED. **ADR-41 capstone: A1 [#367](https://github.com/nmorabowen/OpenSees/pull/367) (friction extract) + A2 [#365](https://github.com/nmorabowen/OpenSees/pull/365) (projection extract) + C1 [#369](https://github.com/nmorabowen/OpenSees/pull/369) (mortar kernel — clip→Gauss→D,M,g̃; oracle 24/24, patch test 9.7e-16) + C1 hardening (convexity guard + back-map convergence flag + warped-facet doc; oracle 30/30) + C2 design [#371](https://github.com/nmorabowen/OpenSees/pull/371) + C2 ALM oracle [#372](https://github.com/nmorabowen/OpenSees/pull/372) (proto_c2_alm.py 18/18: F/K assembly, K_c-vs-FD rotated, Uzawa epsN-independent, release, no-new-DOF) + C2.0 [#373](https://github.com/nmorabowen/OpenSees/pull/373) (mortar COMMAND SURFACE: `SLAVE_SEGMENTS` faceted-slave kind + `contact … -mortar -epsN/-augTol/-maxAug/-ngp` → `LadrunoContactDomain::addMortarContact`, stored separate from NTS ⇒ byte-identical/inert until C2.1; `ladrunoContactInfo` 4th element; test_adr41_mortar_c2_0 4/4; full contact battery 50/50, OpenSeesPy built) + C2.1 [#374](https://github.com/nmorabowen/OpenSees/pull/374) (mortar PENALTY adapter — the first load-bearing mortar phase: a `MORTAR` LadrunoContactFE mode [conn = slave∪master facet nodes, NO λ DOF] + a handler facet-vs-facet pairing loop; `getResidual` integrates the pair at the trial config via `integratePair` → penalty `F^s=−(D·t)n, F^m=+(Mᵀ·t)n`, `t_I=min(0, epsN·ḡ_I)`; `addKtToTang` = `epsN·B̃ᵀdiag(act/a)B̃⊗(n⊗n)`, SPD, geometric terms deferred; `c1` via formEleTangent. test_adr41_mortar_c2_1: matched + non-matched penetration δ=P/(epsN·A), Newton converges [the K_c check], disjoint byte-identical; only fork files, no classTag. **Adversarial code gate (2 reviewers → PASS/SALVAGEABLE): mechanics verified (resid/K_c FD ~1e-15 on rotated+mixed-active configs, self-equilibrium via Σφ=1, c1-single-scaling, explicit-CDL zero-tangent). Folded 1 MAJOR — the NTS bucket-sort superset contract is POINT-only, so a slave-FACET CENTROID query silently dropped ~44% of contact area on coarse-slave/fine-master; C2.1 now uses BRUTE-FORCE pairing (clip rejects non-overlaps), slave-aware broad phase deferred to "mortar P2.5"; +coverage-hole regression test → battery 54/54. See [[LEDGER_quirks]].** + C2.2 [#375] (frictionless commit-cycle **Uzawa ALM** — the headline accuracy win: penetration → an `epsN`-INDEPENDENT tol at FINITE `epsN`, which penalty alone cannot reach. Per-GLOBAL-slave-node multiplier `λ_N` on `LadrunoContactDomain` [`MortarNormalState{lambdaN,gtGlobal,aGlobal,epsN}` keyed `(contactTag, slaveNodeTag)`], COMMITTED-ONLY [mutated solely in `commit()` ⇒ `revertToLastCommit` automatically safe — the EmbeddedRebar invariant], augmented ONE Uzawa step per `Domain::commit()` `λ_I ← min(0, λ_I + epsN·ḡ_I^global)`. **Crux resolved [[_adr41_c2_design]] §crux:** per-node `λ` but per-facet adapters — NewtonRaphson forms the residual facet-by-facet, so a shared node reading a RUNNING global gap sees a different pressure per facet (order-dependent, non-Newton). Shipped scheme: the force/tangent use each facet's LOCAL gap `p_I = min(0, λ_I + epsN·ḡ_I^facet)` [deterministic, exactly C2.1] + the global multiplier `λ_I` [assembles globally — `Σ_facets D_KI^facet λ_I = D_KI^global λ_I`]; the GLOBAL weighted gap is accumulated [idempotent delta updates keyed `(c,node,feTag)`, cleared each `handle()`] ONLY for the commit-time Uzawa update + the `‖ḡ‖` query. λ-term consistent + penalty→0 under augmentation ⇒ converged state epsN-independent. `MortarNormalState` GC [`mortarNormalGC{Begin,Mark,End}`] mirrors `frictionGC*`. New `Domain*` ctor arg on the MORTAR adapter [lazy engine re-fetch like friction]. Held-load `analyze_augmented` Python proc [`Ladruno_scripts/analyze_augmented.py`: zero-increment LoadControl re-commits to `augTol` — the Q-DRIVER resolution, NO custom EquiSolnAlgo] + `ladrunoMortarPenetration` query [Py+Tcl]. **Oracle `proto_c2_alm.py` 28/28** [+T7 crux: per-facet sum w/ shared global pressure == global assembly 1e-17, delta idempotent; +T8 SHIPPED scheme: local-gap force + global-λ Uzawa → epsN-indep penetration]. `test_adr41_mortar_c2_2` 4/4: penetration→epsN-INDEPENDENT tol [headline; penalty O(1/epsN) shown], release/reopen→F=0 & λ→0, SOE eqn count CONSTANT across augmentations [no new DOFs], ALM on a REAL elastic LadrunoBrick mesh [gap→0 epsN-indep — where the deferred Hertz gate lands]; full contact battery 58/58.) + C3.0 [#376](https://github.com/nmorabowen/OpenSees/pull/376) (frictional-mortar design/handoff [[_adr41_c3_design]] + oracle `proto_c3_mortar_friction.py` 20/20 — docs+numpy only) + C3.1 [#377] (mortar friction FORCE — Coulomb/Tresca, penalty `λ_T≡0`: `MortarNormalState` folds in per-global-slave-node `gpT/gpTtrial/gT0/engaged` [same `(contactTag,slaveNodeTag)` key as `λ_N`, committed in `Domain::commit`, reverted in `revertToLastCommit`]; `LadrunoContactFE::addMortarFriction` reuses the SHIPPED `LadrunoFrictionKernel::frictionReturnMap` VERBATIM per in-contact node with `N_I=−p_normal,I`, builds the per-node LOCAL weighted tangential slip from DISPLACEMENTS [`r_I=ΣD_IJ u_s,J−ΣM_IK u_m,K`, tangential part, gT0-referenced — NOT positions: the closest-point projection makes the weighted relative position purely normal, the NTS SEGMENT lesson, see [[LEDGER_quirks]]], scatters `tFric` via D[slave]/−M[master] [self-equilibrating Σφ=1]; new `-mu/-epsT(auto)/-cohesion/-tauMax` mortar command surface → `MortarContact`; handler threads them into the MORTAR ctor [epsTAuto⇒epsN]. `mu≤0∧c≤0∧τmax≤0` short-circuits ⇒ byte-identical frictionless C2. Force-only ⇒ explicit (CDL); the friction TANGENT is C3.2. test_adr41_mortar_c3_1 3/3 (Tresca driven `a=(Q−cap·A)/m` N-independent via the pressure-independent cone, Coulomb friction-opposes-motion sign gate through real CDL+D/M assembly, μ=0 byte-identical); battery 61/61. Only fork files + the parser; no classTag) + C3.2 [#378] (consistent SYMMETRIC mortar friction TANGENT — implicit frictional Newton converges, singular without it: `addMortarTang` scatters the per-node 3×3 `K_ss` [`LadrunoFrictionKernel::frictionTangentBlock`] via `B̃=[D,−M]` — `K[A][B]+=fact·Σ_I(b_IA b_IB/a_I)K_ss_I` — at the SAME displacement-based slip / `N_I` / active-set the residual used. Default SYMMETRIC [`consistent=false` drops the non-sym Coulomb `Csl`, solver-safe per the P3.5 Q2 rule; the full mortar `Csl` couples through the normal-gap operator + is deferred with the geometric terms]. **MAJOR-2 [C3.1 gate] fixed:** `gT0/engaged` double-buffered [`gT0committed/engagedCommitted`] + restored in `revertToLastCommit` so a rejected IMPLICIT step can't latch a stale engagement origin [now reachable]. Oracle T6 FD-checks the ASSEMBLED tangent vs the assembled friction force [rel ~1e-10, symmetric]; `test_adr41_mortar_c3_2` 4/4: static stick CONVERGES [the gate — free tangential DOFs non-singular only via `kt·P_t`], slip CONVERGES [spring-restrained, friction saturates at cap], symmetric-solver-safe [ProfileSPD==FullGeneral], Coulomb static; battery 65/65) + C3.3 [#379] (**C3 COMPLETE** — tangential `λ_T` Uzawa + functional non-sym mortar `Csl`: `MortarNormalState` gains `lambdaT/lambdaTtrial`; the residual+tangent inject `λ_T` via the OFFSET TRICK `gTeff_eff = gTeff + λ_T/epsT` [the penalty return map then yields the augmented `tT* = λ_T + epsT·(gTeff−gpT)` — kernel UNCHANGED], one Uzawa step per commit `λ_T ← −tFric` ⇒ the spurious STICK elastic creep → 0 at FINITE epsT [epsT-INDEPENDENT tangential position — the λ_N headline, tangential side]. Committed-only [from `lambdaTtrial`], reverted. `-consistanttan` now FUNCTIONAL: `addMortarTang` passes `consistent = consistentTan && !initialStiff` ⇒ the non-sym Coulomb `Csl` [−μ·epsN·t̂⊗n] scatters through the SAME `b·b/a` [kn=epsN]; needs a non-symmetric solver [handler warns]; initial-stiffness path stays SPD stick. Oracle 30/30 [+T7 CONSISTENT tangent w/ N(u) FD ~1e-11 + non-sym; +T8 λ_T stick-creep→0 epsT-indep, λ_T→stick traction]; `test_adr41_mortar_c3_3` 2/2 [held-load augmentation removes the creep epsT-indep vs penalty ∝1/epsT; `-consistanttan` Coulomb slip → SAME root as symmetric on FullGeneral]; battery 68/68. **C3 COMPLETE: penalty + ALM friction, symmetric + consistent tangent, explicit + implicit. Next track: C4 mesh-tying.**) + C4 [#381] (**ADR-41 MORTAR LANE DONE** — mesh-tying: a PERMANENT bond, the zero-gap limit of contact. Reuses the shipped mortar machinery with the inequality STRIPPED: the active set frozen ON for every slave node + the FULL 3-vec weighted relative DISPLACEMENT `r_I = Σ_J D_IJ u_s,J − Σ_K M_IK u_m,K` driven to ZERO [normal AND tangential, no KKT/clamp/friction]. `MortarNormalState` folds in `lambdaTie[3]/rtGlobal[3]/isTie` [SAME `(contactTag,slaveNodeTag)` key as `λ_N`]; `LadrunoContactFE::addMortarTieForce` builds `r_I` from `getTrialDisp` [NO `gT0` — the bond exists from the as-built config], forms `t_I = λ_tie,I + epsTie·(r_I/a_I)` [full 3-vec, NO clamp] + scatters via D[slave]/−M[master] like the normal force [`f^s_K=−Σ D_KI t_I`, `f^m_L=+Σ M_IL t_I`, self-equilibrating Σφ=1]; `addMortarTang` assembles the SPD `epsTie·B̃ᵀB̃⊗I₃` [the FULL identity — all 3 components tied — W_I=1/a_I frozen ON, no n⊗n, no Csl, no `-consistanttan` needed]. **ALM** [the headline accuracy win]: one NO-CLAMP Uzawa step per `Domain::commit()` `λ_tie,I ← λ_tie,I + epsTie·(rtGlobal/aGlobal)` ⇒ `‖r‖ → 0` at FINITE epsTie [epsTie-INDEPENDENT bond — penalty alone leaves `O(1/epsTie)`]. Committed-only [the `λ_N` invariant ⇒ revert automatically safe]. **SHARED-NODE pre-req resolved [[LEDGER_quirks]] MAJOR-1/MINOR-1 [fenced "before non-matching friction"]:** `r_I` is a LINEAR accumulation [no return map], so the tie state uses the ORDER-INDEPENDENT global accumulator `λ_N` already uses [`accumulateMortarTie` delta-update keyed `(c,node,feTag)`] — unlike the friction slip [a return-map OUTPUT, last-writer-wins]; the FORCE uses each facet's LOCAL `r` [the C2.2 deterministic-R(u) rule], the GLOBAL `r` drives only the commit Uzawa + the `ladrunoMortarTieResidual` `‖r̄‖_∞` query [Py+Tcl]. Command surface: `contact … -mortar -tie [-epsTie auto|val] [-augTol]`, MUTUALLY EXCLUSIVE with `-mu/-cohesion/-tauMax` [a tie has no cone — refused at the command surface AND in `addMortarContact`]; `analyze_augmented(query=ops.ladrunoMortarTieResidual)` drives the held-load tie augmentation. NO new kernel, NO new DOF, NO classTag [rides the handler]; `-tie`-absent path byte-identical [battery 68→75]. **Oracle `proto_c4_mortar_tie.py` [PATCH TEST: a linear field across a non-matching tie transmits EXACTLY, `r≡0`, `D⁻¹M` recovers the field; penalty `‖r‖=O(1/epsTie)`; ALM no-clamp → `r→0` epsTie-indep == the EXACT Lagrange-constrained solution; tangent `epsTie·B̃ᵀB̃⊗I₃` FD-checked symmetric+PSD+rank=3·ns; full 3-vec/no-clamp; shared-node order-independence].** `test_adr41_mortar_c4_1` 5/5 [headline split-column==monolithic to penalty tol w/ a SHARED edge node + TENSION holds; SPD Newton converges + no new DOFs; `-tie`⊥friction refused; shared-node facet-order BIT-identical; compression mirrors tension] + `test_adr41_mortar_c4_2` 2/2 [penalty `O(1/epsTie)` → ALM augTol epsTie-INDEPENDENT; patch test to ~machine precision under augmentation]; battery 75/75. **ADR-41 mortar lane DONE [C1→C4]; ADR-47 = dual basis / true-LM / self-contact.**) + D2.1 [#385] (**viscous contact stabilization** — resolves the OPEN Q-VISCOUS residue into a committed phase: a gated, off-by-default velocity-proportional NORMAL damper `p_visc = μ_c·ġ` that bleeds contact chatter/snap-through energy in the flagship pounding/rocking/uplift regime. Reuses the normal-penalty operator driven by VELOCITY: gap rate `ġ = B·v` [`getTrialVel`], viscous force `f_visc = −μ_c·ġ·B` [added in `getResidual` beside the penalty force] + the SPD damping tangent `C_visc = μ_c·B Bᵀ` [in `addCtoTang`]. Active ONLY in contact [RIGID_PLANE/SEGMENT `gap<0`]; **statics identically inert** [`v≡0` AND `StaticIntegrator` never calls `addCtoTang` ⇒ byte-identical to `μ_c=0`]. Force-only under the mass-only `CentralDifferenceLadruno` [explicit], force + the consistent C tangent under Newmark/HHT [implicit] — the P3 friction explicit/implicit split. `Contact`/`RigidPlane` gain a `muc` field; `LadrunoContactFE` a `muc` member [init in ALL 4 ctors] + the viscous force/tangent on the RIGID_PLANE + SEGMENT modes. Command `contact … -visc <μ_c>` + `contactPlane … -visc <μ_c>` [off by default; refused on `-tie` — see D2.2]. It is a Kelvin–Voigt [spring+dashpot] contact ⇒ exact restitution `e = exp(−ζπ/√(1−ζ²))`, `ζ = μ_c/(2√(kₙ·m))`. NO classTag, NO new DOF; `-visc`-absent / `μ_c=0` byte-identical [battery 75→81]. **Oracle `proto_d2_viscous.py` [operator `C_visc=μ_c·B Bᵀ` FD sym+PSD + frame-invariant + opposes-approach; static-inert; restitution vs Kelvin–Voigt analytic across the underdamped sweep; energy balance ΔKE==∫μ_c ġ² dt to 0.005%; active mask].** `test_adr41_viscous_d2` [explicit-impact monotone dissipation + Kelvin–Voigt restitution through real CDL, static byte-identical [`v≡0`], implicit Newmark CONVERGES + damps [the `C_visc` tangent], SEGMENT [faceted NTS] viscous]; **2-reviewer adversarial gate → PASS** [no bug: sign opposes approach, tangent = consistent `c2`-riding ∂f/∂v, active-mask residual==tangent, static inert verified via StaticIntegrator]) + D2.2 [#387] (**mortar viscous — D2 COMPLETE**: extends `-visc` to the MORTAR CONTACT mode. Per in-contact slave node [mask `p_I<0`, same as the C2 penalty] the weighted normal gap RATE `ḡ̇_I = n·(Σ_J D_IJ v_s,J − Σ_K M_IK v_m,K)/a_I` [`getTrialVel`], viscous pressure `p_visc_I = μ_c·ḡ̇_I` [NO clamp — a dashpot active while in contact], scattered like the normal force `f^s_K += −(D·p_visc)_K n` / `f^m_L += +(Mᵀ·p_visc)_L n`; the damping tangent `C_visc = μ_c·B̃ᵀ diag(W) B̃ ⊗ n⊗n` in the `addCtoTang` MORTAR branch — the C2 `addMortarTang` normal block with `epsN→μ_c`, in C not K. `MortarContact` gains `muc`; the MORTAR `LadrunoContactFE` ctor a `muc` arg. Command `contact … -mortar -visc <μ_c>` now ACCEPTED; **`-visc`⊥`-tie` refused** [a bond has no contact-chatter regime — at the command surface AND in `addMortarContact`]. Uses each facet's LOCAL gap rate [the C2.2 deterministic-R(u) rule] — touches NO Domain accumulator ⇒ no shared-node order dependence. `μ_c=0` byte-identical. Oracle +T6 [mortar B̃ operator `C_visc` FD sym+PSD + self-equilibrium + opposes-approach + frame-invariant]; `test_adr41_viscous_d2` 8/8 [+mortar Newmark damped-oscillation CONVERGES & decays monotone in μ_c [the mortar C tangent], mortar static byte-identical, `-mortar -visc` accepted / `-visc`⊥`-tie` refused]; battery 81→83/83. **1 adversarial reviewer → PASS** [sign mirrors C2 penalty, active-mask residual==tangent, exact `c2`-riding ∂f/∂v, byte-identical at μ_c=0, tie excluded at 3 layers]. **D2 COMPLETE — viscous stabilization on NTS [RIGID_PLANE/SEGMENT] + MORTAR contact.**) + B3 [#389](https://github.com/nmorabowen/OpenSees/pull/389) (**ADR-39 P2b-2c — consistent ∂n/∂u NORMAL tangent + `ladrunoContactForce` query**: closes the geometric-tangent deferral the NTS lane carried since P2b [C2/C3/C4 each deferred it]. The shipped SEGMENT tangent was `kn·BᵀB` only [EXACT for a flat/fixed master, n constant]; B3 adds the GEOMETRIC block `K_geom = kn·gN·∂²gN/∂u²` [the ∂n/∂u + ∂N_i/∂u curvature / large-sliding terms] ⇒ implicit Newton QUADRATIC on curved / large-sliding interfaces. NEW pure fns `LadrunoContactProjection::normalGeomTangent` [+ `shapeD2`] — the reduced-Hessian closed form [projection-sensitivity metric `H2_αβ=m_αβ−gN·κ_αβ`, `∂ξ̄^α/∂u=H2^{αβ}(Q_β+gN·P_β)`, `∂n/∂u=−(P_γ+κ_γα·∂ξ̄^α)a^γ`, κ=2nd fundamental form], oracle `proto_b3_normal_tangent.py` [analytic==FD ~1e-10 on a warped quad + tilted large-slide; SYMMETRIC to 1e-17; slave block ≡0 for a flat facet ⇒ byte-id] + C++ standalone `b3_cpp_check.cpp`. `LadrunoContactFE::addNormalGeomTang` called in `addKtToTang` behind the existing active mask [re-projects deterministically ⇒ the SAME ξ̄/n/gN the residual used]; `Contact.consistentNormal` + the SEGMENT ctor arg; command `contact … -geomtan` [off ⇒ byte-identical `kn·BᵀB`; REFUSED with `-mortar` — the mortar geometric tangent is separately deferred]. SYMMETRIC [Hessian of the scalar gap] ⇒ solver-safe on ProfileSPD [unlike the friction Csl]; GATED off-default because the block is INDEFINITE [`kn·gN·H`, gN<0] ⇒ improves LOCAL convergence but can shrink the global basin on large soft patches [opt-in like `-consistanttan`; 749-node soft-patch geom-on diverges, geom-off converges — documented]. **NEW `ladrunoMortarPenetration`-style query `ladrunoContactForce slaveNodeTag`**: the SEGMENT adapter reports its `tn=kn·<−gap>₊` into a Domain snapshot [`LadrunoContactDomain::set/getNtsForce` keyed `(contactTag,slaveTag,segIndex)`, cleared in `frictionGCBegin`; pure side-channel, no resid/tang effect] ⇒ direct nodal contact-pressure readout [the adapter traction is NOT in `nodeReaction`] for the Hertz benchmark; Py+Tcl dual-wired. Tests `test_adr39_contact_p2b2c_hertz.py` [8]: flat byte-identical [same disp+iters], curved quadratic [geom 4 vs main 7 iters, SAME root], geometric tangent SYMMETRIC [ProfileSPD==FullGeneral, same iters], `ladrunoContactForce`==applied P, Hertz rigid-sphere/elastic-half-space [converges; patch radius ≈ geometric `√(2Rδ)` <15%; pressure center-peaked & decreasing; `-geomtan` ON converges to the SAME patch; load+radius grow with depth ∝√δ on a fixed cloud]. **Adversarial gate (3 source-grounded reviewers — math/plumbing/edge-case): math PASS [faithful oracle transcription, signs, byte-identity, symmetry, guards all verified], plumbing PASS [no uninit member, dual-wired, byte-identity, GC lifecycle correct], edge-case found 2 TEST-honesty issues, both FOLDED [symmetric-solver test reframed to claim SYMMETRY-safety not indefinite-safety; the Hertz tests gained a `-geomtan`-ON variant + a fixed-cloud falsifiable load/radius scaling].** Quantitative elliptic-`p(r)` / compliant-radius `√(Rδ)` is resolution-bound by NTS-penalty pressure recovery [4 LEDGER_quirks entries: handler ignores imposed-disp, force not in nodeReaction, geom-tangent indefinite, curved-indenter penalty bootstrap] — finer-mesh + displacement-control/D1 follow-up. NO classTag, NO new DOF; `-geomtan`-absent byte-identical; battery 83→91/91. **B3 closes the NTS lane's deferred geometric tangent.**) + B1 [#402](https://github.com/nmorabowen/OpenSees/pull/402) (**ADR-39 P4 — SOFT=1 Courant-stable explicit penalty**: the explicit-direction enabler. Under `CentralDifferenceLadruno` the contact penalty kn lives in the RESIDUAL and adds to the effective stiffness, so a stiff/auto kn raises the contact mode ω=√(kn/m_eff) and THROTTLES the central-difference `dt_cr=2/ω` [the CDL CFL eigensolve sees only the structural elements, not the contact spring ⇒ a run at the structural dt silently DIVERGES]. B1 adds the LS-DYNA §26.15 SOFT=1 option `contact … -soft <SOFSCL>` / `contactPlane … -soft <SOFSCL>` [NTS lane: RIGID_PLANE + SEGMENT; refused on `-mortar`]: under EXPLICIT the adapter REPLACES kn with the Courant-stable `k_soft = SOFSCL·4·m_eff/dt²` each step [SOFSCL default 0.10, the LS-DYNA SLSFAC; ≤0 off, >1 warns], so the contact's own `ω·dt = 2√SOFSCL ≤ 2` and it NEVER shrinks dt_cr below the structural step ⇒ explicit impact/pounding/recontact runs at the STRUCTURAL dt [the 50_/51_ progressive-collapse / element-removal enabler]. `m_eff = 1/(B M⁻¹ Bᵀ) = 1/(invMproj_slave + Σ N_i²·invMproj_seg_i)` [the gap-mode generalized mass — the harmonic combination of the slave + shape-projected segment masses; a FIXED master = ∞ mass ⇒ 0 contribution ⇒ m_eff=m_s; rigid plane ⇒ m_eff=m_s]. The adapter reaches the current dt by `dynamic_cast`-ing the `Integrator*` handed to `getResidual` to `CentralDifferenceLadruno` [catches its SMS subclasses; FAILS for any implicit integrator ⇒ SOFT inert ⇒ the configured kn ⇒ byte-identical] + `getCurrentDeltaT()` [new public getter]. **m_eff uses the ASSEMBLED mass the integrator inverts, NOT `Node::getMass()` [which holds only the nodal `mass`; element-density mass never reaches the node]:** the handler pre-computes per contact node `m[d] = nodal mass(d) + Σ_elements diag(M_e) at the node` [`ladrunoBuildNodalMass`, one element pass, ONLY when a soft contact exists ⇒ off-path byte-identical] and stores it on the engine [`LadrunoContactDomain::{clear,set,get}NodalMass`]; the stateless adapter reads it in `softKn` [fallback to `Node::getMass` if uncached]. `Contact`/`RigidPlane` gain a `softScale` field; `LadrunoContactFE` a `softScale` member [+ `theDomain` now passed to the RIGID_PLANE ctor for the mass-cache lookup]. `-soft` is a MODIFIER on the penalty [needs a base positional `auto`/`kn` — that base kn is what an implicit run uses; refused without one]. Oracle `contact_prototypes/proto_b1_soft_penalty.py` [T1 ω_max=√(k/μ) + the coefficient `k_soft=SOFSCL·4·m_eff/dt² ⇒ ω·dt=2√SOFSCL` + contact dt_cr=dt/√SOFSCL≥dt; T2 m_eff closed-form == `B M⁻¹ Bᵀ` == projected-inverse-mass, rigid-plane limit; T3 mass-vs-wall stable at the structural dt + energy error BOUNDED & SOFSCL-convergent [soft kn∝1/dt² ⇒ steps-per-contact ≈π/√SOFSCL constant ⇒ the discrete one-sided-engagement error is a fn of SOFSCL, NOT a formulation leak — honest framing, the chatter D2 viscous damps]; T4 two-mass impact momentum-exact + velocity-exchange + energy bounded; T5 an accuracy-stiff kn throttles dt by √(R·SOFSCL) while soft does not — the LS-DYNA accuracy/stability trade]. `test_adr39_contact_p4_soft.py` 7/7 [stiff DIVERGES vs soft BOUNDED at the structural dt; ELEMENT-assembled-mass [truss-ρ, no nodal mass] soft-stabilized — the headline fix; energy restitution soft e≈1 vs stiff e≫1; IMPLICIT byte-identical [static `==`]; SEGMENT path stabilized; command refusals; bare `-soft` default SOFSCL]; full contact battery 91→98/98. **Adversarial gate (3 source-grounded reviewers — explicit-stability / byte-identity / energy+oracle): byte-identity PASS [no bug — softScale=0 fast-return + the explicit-only `dynamic_cast` gate], energy+oracle PASS [scheme faithfully mirrored, m_eff matches; folded an OVERCLAIMING oracle label "conservative/no leak" → "bounded & SOFSCL-convergent"], explicit-stability found 1 MAJOR — m_eff was sourced from `Node::getMass()` ⇒ element-density mass invisible ⇒ SOFT silently fell back to the stiff kn for solid-body models [the headline use case]; FIXED [the assembled-mass cache above] + a 4th-reviewer verification PASS + the `test_soft_uses_element_assembled_mass` regression. 1 MINOR deferred/documented: a parallel/distributed row-sum-lumped SOE would mismatch the diagonal reconstruction — CDL+soft is serial `system Diagonal` only [the contact handler `sendSelf` is a P1a stub], lumped/HRZ element mass is diagonal anyway]. SOFT vs auto-kn = larger penetration for a non-throttled dt [the SOFSCL knob]. NO classTag, NO new DOF; `-soft`-absent + any-implicit byte-identical. **B1 unblocks explicit contact at the structural timestep — the progressive-collapse direction.** **B1 kt follow-up [#409](https://github.com/nmorabowen/OpenSees/pull/409):** SOFT=1 originally sized only the NORMAL kn — under explicit a stiff friction `kt` still throttled dt_cr via the tangential STICK mode `ω_t=√(kt/m_eff_t)` [continuous stick at a high cone DIVERGES when `ω_t·dt>2`; confirmed kt=1e8 diverge vs kt=1e6 bounded]. NEW `LadrunoContactFE::softKt` [+ the extracted `gapModeInvMass(dir,N)` helper, `softKn` refactored onto it bit-identically] sizes the friction stick penalty Courant-stably `k_soft_t = SOFSCL·4·m_eff_t/dt²` [`m_eff_t` = the gap-mode mass for `B_t=[t|−N_i t]`, the WORST-CASE over the two tangents to n — EXACT for isotropic lumped mass `system Diagonal` uses, approximate-but-documented for anisotropic] ⇒ `ω_t·dt = 2√SOFSCL ≤ 2`. Used in the SEGMENT friction residual under explicit ONLY [implicit / `-soft`-absent ⇒ the configured kt ⇒ byte-identical; the implicit friction TANGENT `addFrictionTang` keeps the configured kt, consistent since CDL never forms the tangent]. Oracle `proto_b1_soft_penalty.py` +T6 [m_eff_t==`B_t M⁻¹ B_tᵀ`, isotropic m_eff_t==m_eff_n, anisotropic worst-case, central-difference stick stiff-diverges/soft-bounded]; `test_adr39_contact_p4_soft.py` +2 [stiff-kt stick diverges vs soft bounded; implicit byte-identical]; battery 104→106/106. **2-reviewer adversarial gate → PASS** [no bug: softKn refactor bit-identical, byte-identity + residual/tangent consistency verified; 2 NITs folded into the softKt doc — the multi-node per-pair conservatism inherited from SOFSCL + the anisotropic-tangent-basis approximation]. + B2 [#406](https://github.com/nmorabowen/OpenSees/pull/406) (**ADR-39 P5 — SOFT=2 segment-based explicit penalty**: the segment-to-segment generalization of B1. The shipped B1 SOFT=1 is NODE-to-segment ⇒ it MISSES three regimes — partial facet overlap where NO slave node projects in-bounds, a node sliding off a master segment edge [projection OOB ⇒ inert], a T-intersection with no unique owning segment. B2 `contact … -mortar -soft <SOFSCL>` adds the SEGMENT-BASED penalty: REUSE the shipped `LadrunoMortarKernel` clip→Gauss [D,M,g̃,n over the slave-facet∩master-facet overlap] and distribute a Courant-stable penalty `p_I = min(0, k_soft,I·ḡ_I)`, `ḡ_I=g̃_I/a_I`, with `k_soft,I = SOFSCL·4·m_eff,I/dt²` and the SEGMENT-BASED gap-mode mass `m_eff,I = 1/(B_I M⁻¹ B_Iᵀ)`, `B_I = [D_IJ/a_I·n | −M_IK/a_I·n]` [the B1 NTS `[n|−Nᵢ n]` operator generalized; reuses the assembled nodal-mass cache] ⇒ each contact mode `ω_I·dt = 2√SOFSCL ≤ 2`, dt_cr un-throttled. Catches the corner/edge/T cases NTS misses [NTS net force ZERO where no slave node is in-bounds; SOFT=2 integrates the overlap] ⇒ explicit contact survives arbitrary-edge contact once bodies fragment [the 50_/51_ element-removal / collapse direction]. New `LadrunoContactFE::addSoft2Penalty` + an explicit-only fast path at the TOP of the MORTAR `getResidual` [`softScale>0 && dynamic_cast<CentralDifferenceLadruno*>` ⇒ assemble + early-return; strictly additive ⇒ softScale=0 byte-identical]; under IMPLICIT the cast fails ⇒ falls through to the shipped penalty/ALM with the configured epsN [explicit-only, like B1]. Scatter `f^s=−(D·p)n, f^m=+(Mᵀ·p)n` [self-equilibrating Σφ=1, exactly the C2 normal block]. `MortarContact`/the MORTAR ctor gain a `softScale`; the handler's `anySoft` mass-cache trigger now includes mortar soft. FRICTIONLESS MVP — `-mortar -soft` ⊥ `-tie/-mu/-cohesion/-tauMax/-visc` [refused at the command surface AND in `addMortarContact`]; SOFT=1 vs SOFT=2 selected by the formulation. Oracle `proto_b2_soft2_segment.py` [T1 segment m_eff closed-form == `B_I M⁻¹ B_Iᵀ` + matched/fixed-master limits; T2 corner/edge/T NTS-MISSES-SOFT=2-CATCHES — NTS net force 0, SOFT=2 nonzero == reference penalty integral; T3 central-difference stability+energy [soft bounded e≈1, stiff diverges, SOFSCL-convergent]; T4 ASSEMBLED K_c eigenbound `ω_max·dt ≤ 2`]. `test_adr39_contact_p5_soft2.py` 6/6 [edge-overlap NTS-passes-through vs SOFT=2-restrained; stiff diverges vs soft bounded; energy restitution; implicit byte-identical; refusals; bare `-soft`]; full contact battery 98→104/104. **3-reviewer adversarial gate → PASS [no BLOCKER/MAJOR — m_eff matches the oracle, the B1 `Node::getMass()` bug is AVOIDED (reads the assembled cache), byte-identity + sign/KKT + self-equilibrium verified]. 1 MINOR FOLDED: the per-node Courant bound is necessary-not-sufficient — inter-node coupling in the ASSEMBLED K_c raises ω_max·dt ~2× [oracle T4: 1.19 vs per-node 0.63 at SOFSCL=0.1; unstable near SOFSCL≈0.3] ⇒ a `-mortar -soft SOFSCL>0.25` warning [the per-node `>1` warning understates the segment lane] + a [[LEDGER_quirks]] note; default 0.10 safe.** NO classTag, NO new DOF; `-soft`-absent + any-implicit byte-identical. **B2 makes explicit contact survive arbitrary-edge contact once bodies fragment — the element-removal / collapse direction.**) + D1 [#411](https://github.com/nmorabowen/OpenSees/pull/411) (**ADR-41 D1 — within-step augmentation sign-off**: the held-load `analyze_augmented` proc promoted to a FIRST-CLASS no-corruption recipe. It brackets its zero-increment LoadControl re-commit loop with NEW `ladrunoBeginAugment`/`ladrunoEndAugment` commands [Py+Tcl, no args, idempotent] that set a `Domain::contactAugmenting` flag making `Domain::commit()` SKIP the recorder loop + `commitTag++` [the contact Uzawa λ update `theContactDomain->commit()` + `committedTime=currentTime` STILL run; committedTime a no-op under the held load]. So the within-step sweep drives `‖ḡ‖→augTol` [contact] / `‖r̄‖→augTol` [tie] inside ONE physical step with NO spurious recorder samples, NO time/commitTag advance, NO load double-count [a zero-increment LoadControl SETS the load factor each re-solve, never accumulates] — the capstone contract #3 "no recorder/load corruption" clause. The proc's begin()+first query() are INSIDE a `try/finally` ⇒ the flag can NEVER stick ON [even on a throwing query]. The within-step loop control is distinct from the across-step Uzawa C2.2/C4 already ship [ONE augmentation per physical step]; the convergence criterion is the standard augmented-Lagrange outer loop [Wriggers Ch.6 / Laursen Ch.8], unchanged — D1 only removes the side-effects of the held-load re-commits. NEW vanilla edits: `Domain.{h,cpp}` [the flag + the `if(!contactAugmenting)` commit guard, byte-identical when off] + the interpreter quad [the two commands]. NO classTag, NO new DOF; flag-off / `analyze_augmented`-unused byte-identical to stock. Oracle `proto_d1_augmentation.py` 11/11 [within-step monotone ‖g‖↓ + augTol stop + the recorder/commitTag/load no-corruption accounting + the εN-independence]; `test_adr41_d1_augmentation.py` 6/6 [within-step contact+tie convergence; recorder no-corruption + an UNBRACKETED NEGATIVE control proving the bracket — not the zero increment — is what fixes it; no-load-corruption w/ a 10-extra-held-pass reaction/time discriminator; opt-in byte-identity]; full contact battery 106→112/112. **3-reviewer adversarial gate → PASS [no BLOCKER/MAJOR — the commit/recorder/commitTag/load protocol verified end-to-end: `Domain::commit()` is the SOLE recorder path under static `analyze(1)` (StaticAnalysis has no separate `record()`), `LoadControl(0.0)`'s `applyLoad` is idempotent (SET not accumulate), the λ committed-only invariant survives the sweep (revert never touches λ)]. 2 MINOR FOLDED: begin()+first query() moved INSIDE the try [a throwing query no longer leaves the flag stuck ON / recorders muted]; the no-load-corruption gate strengthened with the 10-extra-held-pass discriminator [`getTime()==1.0` alone was structurally forced]; + an in-class `=false` flag-init safety net.** **D1 = the ADR-41 within-step augmentation sign-off; the committed contact roadmap (Tracks A→D) is COMPLETE — remaining items are ADR-47 deferrals + small optional follow-ups.** + SOFT=2 VISCOUS [#412](https://github.com/nmorabowen/OpenSees/pull/412) (**B2 follow-up — viscous stabilization on the SOFT=2 segment-based explicit penalty**: `contact … -mortar -soft <SOFSCL> -visc <μ_c>` now ACCEPTED [was refused as the frictionless MVP]. `LadrunoContactFE::addSoft2Penalty` gains the D2.2 velocity-proportional NORMAL damper on the SOFT=2 active set — per in-contact slave node [the soft-penalty KKT mask `p_I<0`] the weighted normal gap rate `ḡ̇_I = n·(ΣD v_s−ΣM v_m)/a_I` drives `p_visc_I = μ_c·ḡ̇_I`, scattered via D/−M·n EXACTLY like the normal force [`+=`, not clobbering the `=`-assigned penalty]. It is the SAME oracle-validated D2.2 operator [`proto_d2_viscous.py` T6] verbatim, on the soft path ⇒ bleeds the explicit-impact recontact bounce [the progressive-collapse regime SOFT=2 targets]. Both refusal choke points [parser + `addMortarContact`] relaxed to allow `-visc` while still refusing `-tie/-mu/-cohesion/-tauMax`. `μ_c=0` byte-identical [`if(muc>0)` guard]; explicit-only [the new block is inside `addSoft2Penalty`, reached only under CDL; implicit falls through to the regular mortar viscous path]. `test_adr41_soft2_visc.py` 3/3 [explicit restitution monotone-↓ in μ_c on the matched facet; viscous on a PARTIAL clip [the edge-overlap geometry NTS misses]; static byte-identical] + `test_adr39_contact_p5_soft2` refusal test updated [`-visc` now accepted]; battery 112→114/114. 2-reviewer adversarial gate → code PASS [verbatim D2.2 operator, correct `+=`/mask/byte-identity/both-choke-points], tests MINOR [the static gate exercises the implicit fall-through path not the new block — honest, documented; closed the edge-overlap+visc gap].** + SOFT=2 FRICTION [#414](https://github.com/nmorabowen/OpenSees/pull/414) (**B2 follow-up — Courant-stable Coulomb/Tresca on the SOFT=2 segment overlap**: `contact … -mortar -soft <SOFSCL> -mu <μ> [-cohesion -tauMax -epsT]` now ACCEPTED; only `-tie` still refused with `-soft`. `addSoft2Penalty` gains a friction block — per in-contact slave node [the soft mask `p_I<0`] the SHIPPED `LadrunoFrictionKernel::frictionReturnMap` runs with the SOFT pressure `N_I=|p_I|` and a per-node Courant-stable STICK penalty `k_soft_t,I=SOFSCL·4·m_eff_t,I/dt²` sized from the SEGMENT tangential gap-mode mass [the B2 normal `m_eff` with n→t, worst-cased over the two tangents — the B1-kt rule generalized to the segment operator, so a stiff friction kt never throttles dt_cr via the stick mode]. Penalty friction [λ_T≡0 single-pass; `gTeff=gbarT−gT0`, `lambdaTtrial=0`]; DISPLACEMENT-based slip [the C3.1 lesson]; tFric via D/−M like C3.1. Reuses the committed `MortarNormalState` slot [`gpT/gT0/engaged`, already GC-marked for ALL mortar pairs ⇒ survives+commits]. Explicit-only [friction tangent never formed under CDL; implicit falls through to the regular `addMortarFriction`+tangent — same slot, mutually exclusive per eval ⇒ no double-count]. μ≤0∧c≤0∧τmax≤0 short-circuits ⇒ byte-identical frictionless SOFT=2. Oracle `proto_soft2_friction.py` 12/12 [tangential `m_eff_t==B_t M⁻¹ B_tᵀ`, isotropic⇒direction-indep, fixed-master limit; Courant stick soft-stable/stiff-diverges; cone min(μN+c,τmax) over the soft pressure; D/M self-equilibrium]; `test_adr41_soft2_friction.py` 4/4 [friction arrests the drag monotone-↓ in μ; softKt keeps a STIFF `-epsT 1e9` stick STABLE under explicit — the headline; the cohesion/Tresca cone THROUGH the FE; μ=0 byte-identical exact-`==`] + `test_adr39_contact_p5_soft2` refusal test updated [`-mu` accepted]; battery 115→119/119. 2-reviewer adversarial gate → code PASS [softKt sizing matches oracle, slip/sign/scatter mirror `addMortarFriction`, λ_T≡0, N_I≥0, no double-count] + lifecycle/tests PASS [GC survival+commit, μ=0 exact byte-identity, both choke points refuse `-tie`]; folded 1 MINOR [stale "MVP frictionless" comment] + closed the cohesion/Tresca-cone-through-FE gap. **SOFT=2 now supports viscous + Coulomb/Tresca friction; parallel/row-sum `m_eff` remains blocked by the serial-only handler (`sendSelf`/`recvSelf` P1a stubs) — its own parallelization effort, not a small follow-up.**).** |
| **LadrunoEdgeKernel** — perpendicular edge-edge (segment-to-segment) contact narrow phase ([[57_ladruno_edge_edge_contact_adr]]; 3-reviewer design gate PASS-equiv). The cos_t→0 case the mortar face-overlap clip degenerates on (two facets meeting edge-on) and NTS misses (no in-bounds slave node — the contact is between the interiors of two line features). **E0 (geometry) SHIPPED:** header-only `closestPtSegSeg` (Ericson RTCD §5.1.9 EXACT clamp ladder over the 8 Voronoi sub-regions), the dual-purpose degeneracy gauge `denom=a·e−b²=‖d1‖²‖d2‖²sin²θ_edge` (parallel/ill-conditioned-normal REFUSAL `denom<τ_par·a·e`, τ_par=sin²15° conditioning-justified), zero-length DEGENERATE guard, the common-perpendicular normal `n=(d1×d2)/‖·‖` with a BODY-FIXED committed sign (the self-referential `w·n` rule masks interpenetration through contact — the design-gate Lens-A A-4 fix), the signed gap `gN=w·n` (`=±‖w‖` only in the MARGIN-interior set), and the first-variation B-operator `B=[(1−s)n|s n|−(1−t)n|−t n]` (envelope-theorem-exact). Oracle `proto_e0_closest_point.py` 28/28 (independent scan reference across all sub-regions incl. vertex-vertex double-clamp; parallel/near-parallel/zero-length refusals; conditioning bounded at the band; the gN=±‖w‖ interior identity + its failure clamped; B==FD 1.1e-9; committed-sign monotone-through-contact + the buggy-rule penetration-mask demo) + standalone `e0_cpp_check.cpp` 13/13 bit-for-bit (B==FD identical 1.13e-9). **E1 (routing logic) ORACLE SHIPPED:** `proto_e1_router.py` 16/16 — the `align_fn=|n̂_s·n̂_m|` REGIME-SPLIT precedence (facing `align_fn≥τ_face` ⇒ FACE-MORTAR owns; non-facing ⇒ NTS≻edge-edge≻sliver-mortar), proximity-gated guaranteed owner (no coverage hole), NTS-vs-edge double-count falsifier, oblique-band falsifier, hysteresis no-chatter. Oracle-first CAUGHT a design refinement: flat NTS-precedence steals facing-facet contact from the mortar lane ⇒ the regime split (folded into ADR-57 §2). The C++ routing loop folds into E2 (routing without an adapter to inject is inert). **E2 (penalty NORMAL force + main tangent) SHIPPED — the first live C++/build phase:** a 4th LadrunoContactFE mode `EDGE_EDGE=4` (a STATELESS 4-node adapter [sa,sb,ma,mb], FE_Element(tag,4,12), no new class tag — mirrors RIGID_PLANE/SEGMENT/MORTAR sharing the runtime tag): `getResidual` runs `closestPtSegSeg` at the trial config → the body-fixed COMMITTED sign (Domain-owned `EdgeEdgeState`) → assembles `f=tN·B`, `tN=εN⟨−gN⟩`; `addKtToTang`/`addKiToTang` assemble the main tangent `K=εN·BᵀB` (symmetric, rank-1, PSD); `addCtoTang` no-op (viscous is later). New `LadrunoContactDomain::EdgeEdgeState` (signN + double-buffer; friction E3 / one-scalar ALM E6 fields zeroed/inert) keyed (contactTag, ordered slave-edge nodes, ordered master-edge nodes) — a COMPOSITE key, never a lossy hash; `getOrCreateEdgeEdgeState` + `edgeGC{Begin,Mark,End}` dead-slot pruning; commit()/revertToLastCommit() EXTENDED to iterate `theEdgeEdgeStates` (the explicit obligation — the shipped hooks iterated only friction+mortar). Handler routing loop (the E1 align_fn regime-split precedence, now non-inert) over the mortar lane's BRUTE-FORCE facet pairing (NOT the bucket sort): on an EDGE_EDGE route, enumerate the (≤4)×(≤4) boundary-edge pairs, keep the margin-interior/well-conditioned/in-band crossings, de-dup by ordered edge-node-tag tuple, inject one `EDGE_EDGE` adapter per kept pair. `-edgeedge` opt-in modifier on `-mortar` (`-edgeKn auto\|val`, `-edgeBand <d>`), OFF by default ⇒ byte-identical; a perpendicular pair's mortar adapter is geometrically inert (clip degenerates) ⇒ no double-count with the injected edge-edge force. Oracle `proto_e2_penalty.py` 23/23 (tN=εN⟨−gN⟩, Σf=0, force ⟂ both edges, K=εN BᵀB sym/PSD/rank-1 FD-checked, δ=P/εN, monotone-gN-through-contact committed-sign falsifier + the w·n-rule penetration-mask demo). `tests/test_adr57_edge_edge_1.py` 3/3 (crossed bars restrained at δ≈P/εN Newton-converges; THE HEADLINE FALSIFIER — same geometry NTS contact transmits ZERO force ⇒ the edge sinks ~10³× deeper / passes through; byte-identity when no pair routes / -edgeedge absent; **EE-1 regression `test_e2_oblique_no_double_count`** — an oblique 60° pair where the mortar clip is non-degenerate yet edge-edge owns, asserting the single-primitive δ≈P/εN). Full contact battery **145 passed** (no regression). **3-reviewer adversarial gate: PASS after folding 2 findings** — **EE-1 (MAJOR):** the mortar lane injected its adapter UNCONDITIONALLY, so in the oblique band (50°–90°, clip non-degenerate) the mortar pressure + the edge-edge force SUPERPOSED — fixed by a `ladrunoEdgeEdgeOwns` single-source-of-truth routing decision the MORTAR loop now consults to STAND DOWN (ADR §2 "face-mortar vacates only if edge-edge succeeds"); **E2-2 (MINOR):** the default sign-anchor `orientDir` used a centroid difference that can vanish near-coincident centroids — fixed to the always-non-degenerate slave facet normal (ADR §2). Rejected: the per-pair rank-deficiency (inherent to penalty contact, shared by NTS/mortar) + an unreachable null-deref. **E3 (friction) SHIPPED — the 4th consumer of the ONE `LadrunoFrictionKernel`** (NTS, mortar, SOFT=2, now edge-edge — NO new friction code; only the slip MEASURE + the SCATTER are edge-specific): new `edgeSlip` builds the tangential slip from the relative DISPLACEMENT of the two closest points (the C3.1 trap — at the closest point the relative POSITION is purely normal), `gT0`-engagement-referenced; `getResidual` runs the shipped return map (`N=t_N=ε_N⟨−g_N⟩`, unified cone `min(μN+c, τmax)`) and scatters the already-negated `tFric` via the SAME edge weights as the normal force (`Σw=0 ⇒ Σf=0`); `addKtToTang` assembles `K[A][B]=w_A w_B·K_ss` (symmetric default — the Coulomb Csl behind `-edgeConsistentTan`), `addKiToTang` the SPD stick tangent. The `EdgeEdgeState` friction fields (`gpT/gT0/engaged` + double-buffer — wired into commit/revert in E2, inert) are now live; `μ≤0∧c≤0∧τmax≤0` SHORT-CIRCUITS the block (byte-identical — the kernel's `cap≤0` returns RAW elastic, so the GUARD gives byte-identity, in all 3 of getResidual/addKtToTang/addKiToTang). Command `-edgeMu`/`-edgeKt`/`-edgeCohesion`/`-edgeTauMax`/`-edgeConsistentTan`; `-edgeKt` defaults to the edge normal penalty (warn-once, no div-by-zero). Oracle `proto_e3_friction.py` 12/12 (stick/slip, `a=g(sinθ−μcosθ)` incline == cone threshold, Tresca, slip-from-displacement, Σf=0 + symmetric tangent FD, the non-sym Csl branch, the μ=0-byte-identity contract). `tests/test_adr57_edge_edge_2.py` 4/4 (EXPLICIT raking bar Coulomb-opposes-motion [monotone decel, no energy injection] vs μ=0 coast; implicit stick converges [SPD stick tangent]; symmetric tangent ProfileSPD==FullGeneral; μ=0 byte-identical). Full contact battery **149 passed**. **3-reviewer adversarial gate (Gate 3) → PASS, NO findings** (mechanics, state-lifecycle/byte-identity, tangent/solver/rank all verified sound; the point-pair tangential rank-deficiency is the inherent point-like property E2 already ruled non-blocking — the static tests regularize it with a soft tangential spring, as E2 did for the normal mode). **E5 (explicit Courant-stable SOFT) SHIPPED:** the edge-edge analogue of the B1 (NTS) / B2 (mortar) SOFT lane — under the explicit `CentralDifferenceLadruno` the edge penalty εN is REPLACED by `k_soft = SOFSCL·4·m_eff/dt²` so `ω·dt = 2√SOFSCL ≤ 2` never throttles `dt_cr` (edge-on impact at the STRUCTURAL dt). `m_eff = 1/(B M⁻¹ Bᵀ)` is the 4-node edge gap-mode mass — the B1 closed form generalized to the edge operator `1/((1−s)²invMproj_a0+s²invMproj_a1+(1−t)²invMproj_b0+t²invMproj_b1)`, REUSING the assembled `ladrunoBuildNodalMass` cache + `ladrunoNodeMass`/`ladrunoInvMassProj` verbatim (fixed/massless node ⇒ ∞ mass ⇒ 0). New `edgeGapModeInvMass`/`softKnEdge`/`softKtEdge` (friction `softKt` = the B1-kt n→t worst-case rule); `getResidual` swaps `kn→softKnEdge` (normal) + `kt→softKtEdge` (friction stick) RESIDUAL-ONLY (the tangent uses `kn`, assembled only under implicit where `softKnEdge≡kn` ⇒ byte-identical — the B1/B2 contract). `-edgeSoft <SOFSCL>` opt-in on `-edgeedge` (default 0.10); the handler `anySoft` detection now also fires on an `-edgeSoft` contact (shared cache). SOFSCL≤0 / implicit ⇒ the configured εN ⇒ byte-identical. Oracle `proto_e5_soft.py` **26/26**; `tests/test_adr57_edge_edge_3.py` 3/3 (SOFT impact at the structural dt bounded+rebounds; the SAME dt with stiff εN DIVERGES; implicit byte-identical); full contact battery **130 passed**; 3-reviewer adversarial gate PASS. **E6 (one-scalar commit-cycle ALM) SHIPPED:** the OPTIONAL per-pair normal multiplier λ_N — the point-like analogue of the mortar C2.2 Uzawa-on-commit (ONE scalar gN per pair, NO shared-node accumulator). Augmented pressure `p = min(0, λ_N + εN·gN)`, `tN = −p`; `getResidual` injects committed λ_N + stashes `gN_committed`/`εN`; `Domain::commit()` runs ONE Uzawa `λ_N ← min(0, λ_N + εN·gN_committed)` (mirrors the MortarNormalState block in the same loop) ⇒ penetration → an εN-INDEPENDENT tol at FINITE εN. The tangent (implicit-only) uses the SAME augmented active mask `λ_N+εN·gN<0` + cone N (the C2.2 frozen-active-set rule; penalty Gram unchanged). λ_N committed-only ⇒ revert leaves it untouched. New `EdgeEdgeState.epsN` (the only struct add — lambdaN/gN_committed were reserved inert in E2); new `getMaxEdgePenetration()` + `ladrunoEdgePenetration` command (Py+Tcl) read by the SHIPPED held-load `analyze_augmented` proc UNCHANGED. `-edgeAlm` opt-in (+ `-edgeAugTol`); IMPLICIT-ONLY (mass-only explicit tangent degenerates Uzawa to single-pass penalty — disclosed ADR-47, inherited). OFF ⇒ λ_N≡0 ⇒ E2 penalty (commit Uzawa is a no-op when epsN/gN_committed unwritten) ⇒ byte-identical. Oracle `proto_e6_alm.py` **24/24**; `tests/test_adr57_edge_edge_4.py` 3/3 (held-load augmentation drives `ladrunoEdgePenetration`→augTol εN-independently; release opens; first-solve opt-in byte-identity); full contact battery **133 passed**; 3-reviewer adversarial gate PASS. **E7 (integration + regression — the trust gate) SHIPPED:** the full E2–E6 stack validated on REAL `LadrunoBrick` bodies (mass + stiffness from the element ⇒ the body regularizes the point-pair differential mode, NO artificial springs). `tests/test_adr57_edge_edge_5.py` **8/8**: cross-stacked bars restrained at the **mechanism-specific** δ=P/εN (bottom-edge reaction = the load by static equilibrium, brick-stiffness-INDEPENDENT); NTS-passes-through survives real bodies; **E5 SOFT from the ASSEMBLED element-density mass** (explicit drop bounded vs stiff εN ejecting ≫100×); **E6 ALM on a deformable body** (gap→augTol εN-independently); a **non-axis-aligned crossing** (inclined master ⇒ n≠ẑ — guards the normal/sign/B-operator/routing off-axis); **E3 friction on a real brick** (stick vs μ=0 slide); **byte-identity at scale** (all 8 brick nodes, bars far apart); the **large-sliding LIMIT** as a GRACEFUL release (engaged→slid-off→inert, finite, query→0 — the §7 fence + the E6 stale-gap fold). Full contact battery **141 passed**; 3-reviewer adversarial gate PASS (folds: mechanism-specific assertions, the off-axis + real-body-friction tests, the engaged-then-released proof, all-8-node byte-identity; rejected "reset-redundant-with-edgeGCEnd" — edgeGCEnd runs once per handle(), not per step). **The edge-edge lane is COMPLETE** (E0–E3 + E5 + E6 + E7); only E4 (geom tangent) remains DEFERRED/demand-driven. | Header-only contact kernel (geometry) + LadrunoContactFE `EDGE_EDGE` mode (penalty E2 + friction E3 + explicit SOFT E5 + ALM E6) + Domain `EdgeEdgeState` + `ladrunoEdgePenetration` query | — (EDGE_EDGE is a MODE of LadrunoContactFE — no new class tag; an active adapter declares real 4-node connectivity) | `SRC/domain/contact/LadrunoEdgeKernel.h`, `SRC/analysis/handler/LadrunoContactFE.{h,cpp}`, `SRC/domain/contact/LadrunoContactDomain.{h,cpp}`, `SRC/analysis/handler/LadrunoContactHandler.cpp`, `SRC/interpreter/OpenSeesOutputCommands.cpp`, `Ladruno_implementation/contact_prototypes/proto_e0_closest_point.py`, `.../e0_cpp_check.cpp`, `.../proto_e1_router.py`, `.../proto_e2_penalty.py`, `.../proto_e3_friction.py`, `.../proto_e5_soft.py`, `.../proto_e6_alm.py`, `tests/test_adr57_edge_edge_1.py`, `tests/test_adr57_edge_edge_2.py`, `tests/test_adr57_edge_edge_3.py`, `tests/test_adr57_edge_edge_4.py`, `tests/test_adr57_edge_edge_5.py`, `SRC/interpreter/OpenSeesCommands.h`, `SRC/interpreter/PythonWrapper.cpp`, `SRC/interpreter/TclWrapper.cpp`, `Ladruno_scripts/stamp_headers.py` (allowlist) | ✅ **shipped — E0–E3 + E5 (explicit SOFT) + E6 (ALM) + E7 (integration) COMPLETE; only E4 (geom tangent) deferred/demand-driven** (E0 oracle 28/28 + C++ 13/13; E1 16/16; E2 oracle 23/23 + battery 4/4; E3 oracle 12/12 + battery 4/4; E5 oracle 26/26 + battery 3/3; E6 oracle 24/24 + battery 3/3; E7 battery 8/8; full contact battery 141 green; 6 adversarial gates PASS; `-edgeSoft`/`-edgeAlm`/`-edgeedge` off ⇒ build byte-identical). | [#421](https://github.com/nmorabowen/OpenSees/pull/421) (E0), [#422](https://github.com/nmorabowen/OpenSees/pull/422) (E1), [#425](https://github.com/nmorabowen/OpenSees/pull/425) (E2), [#431](https://github.com/nmorabowen/OpenSees/pull/431) (E3), [#434](https://github.com/nmorabowen/OpenSees/pull/434) (E5), [#437](https://github.com/nmorabowen/OpenSees/pull/437) (E6), [#438](https://github.com/nmorabowen/OpenSees/pull/438) (E7); design [#420](https://github.com/nmorabowen/OpenSees/pull/420) |
| **Explicit bulk viscosity** (`-bulkViscosity b1 b2`) ([[52_ladruno_integrator_strengthening_adr]] W2-E1) — LS-DYNA/Abaqus-style artificial bulk-viscosity pressure (linear `b1` + quadratic `b2` on the volumetric strain rate) on all three Ladruno continuum elements, to damp shock/impact ringing under explicit integration. Off by default ⇒ byte-identical. | Element flag | — (no new tag) | `SRC/element/ladrunoBrick/`, `SRC/element/ladrunoPlane/` (LadrunoQuad + LadrunoCST) | shipped | [#399](https://github.com/nmorabowen/OpenSees/pull/399) (LadrunoBrick), [#403](https://github.com/nmorabowen/OpenSees/pull/403) (LadrunoQuad/CST) |
| **LadrunoDispBeamColumn2d / 3d** ([[32_ladruno_dispbeamcolumn_regularization_adr]], [[33_ladruno_dispbeamcolumn3d_hinge_adr]]) — regularized displacement-based frame element: **Tier-1** per-IP crack-band `lch` channel (mirrors `ForceBeamColumn`, fixes [[LEDGER_quirks]] §59) + Corotational large-disp + `-nl` ½θ² bowing; **Tier-2** embedded strong-discontinuity cohesive rotation-jump hinge (`-hinge`/`-hingeY`/`-hingeBiaxial`, Armero–Ehrlich, guarded static condensation to the basic system before `crdTransf`). No-hinge path bit-identical. Full build history in the section below (Stage 1 + Tier-2 PR-2a…PR-4b). | Element | **ELE_TAG 33013 (2d) / 33014 (3d)** (Element band; numerically equal to ND_TAG 33013/33014 — per-registry, not a collision) | `SRC/element/ladrunoDispBeamColumn/`, `tests/test_ladrunoDispBeamColumn{2,3}d*.py` | shipped | [#251](https://github.com/nmorabowen/OpenSees/pull/251), [#254](https://github.com/nmorabowen/OpenSees/pull/254), [#255](https://github.com/nmorabowen/OpenSees/pull/255), [#258](https://github.com/nmorabowen/OpenSees/pull/258), [#260](https://github.com/nmorabowen/OpenSees/pull/260), [#267](https://github.com/nmorabowen/OpenSees/pull/267), [#269](https://github.com/nmorabowen/OpenSees/pull/269), [#271](https://github.com/nmorabowen/OpenSees/pull/271), [#274](https://github.com/nmorabowen/OpenSees/pull/274) |
| **LadrunoCohesiveHinge** ([[32_ladruno_dispbeamcolumn_regularization_adr]]) — discrete cohesive moment–rotation `UniaxialMaterial` `M([[θ]])` carrying `Gf` per hinge (rigid-softening penalty w/ guarded floor `Mc²/2Gf`; exp/linear envelope calibrated to `∫M d[[θ]]==Gf`; irreversible secant damage; `getEnergy()`) — the cohesive law consumed by the DispBeamColumn `-hinge`. | Uniaxial material | **MAT_TAG 33003** | `SRC/material/uniaxial/LadrunoCohesiveHinge.{h,cpp}`, `tests/test_ladrunoCohesiveHinge_material.py` | shipped | [#264](https://github.com/nmorabowen/OpenSees/pull/264) |
| **LadrunoCohesiveHingeBiaxial** ([[34_ladruno_cohesive_hinge_biaxial_adr]]) — coupled Mz–My cohesive interaction-surface `nDMaterial` (order 2): elliptical onset `√((Mz/Mcz)²+(My/Mcy)²)=1` + isotropic secant damage on the normalized norm, per-axis `Gf`, Benzeggagh-Kenane mode-mix (`-bk η`); reduces EXACTLY to the scalar `LadrunoCohesiveHinge` on each pure axis. Consumed by `LadrunoDispBeamColumn3d -hingeBiaxial`. | nD material | **ND_TAG 33004** | `SRC/material/nD/LadrunoCohesiveHingeBiaxial.{h,cpp}`, `tests/test_ladrunoCohesiveHingeBiaxial_material.py` | shipped | [#275](https://github.com/nmorabowen/OpenSees/pull/275), [#302](https://github.com/nmorabowen/OpenSees/pull/302) |
| **LadrunoRigidBody** ([[58_ladruno_rigid_body_adr]]) — 6-DOF rigid-body Element (LS-DYNA `*CONSTRAINED_NODAL_RIGID_BODY` / Abaqus `*RIGID BODY`): a zero-stiffness Element owning a private internal 6-DOF CoM `Node` (condensed mass + body-frame inertia tensor) with rigid-link `MP_Constraint`s to each slave; body-frame momentum-conserving SO(3) integrator in `commitState` (integrated OFF the global solve); finite-rotation slave-following (`u_i=u_R+(R−I)d_i⁰` imposed in `update()`) + CoM moment gather + Housner rocking MVP. Explicit-only, serial-only v1. Full build history (P1 ballistic → P2 SO(3) → P2-S2 slaving/gather/rocking) in the section below. | Element | **ELE_TAG 33015** | `SRC/element/ladrunoRigidBody/`, `tests/test_ladrunoRigidBody_element.py` | shipped | [#426](https://github.com/nmorabowen/OpenSees/pull/426), [#427](https://github.com/nmorabowen/OpenSees/pull/427), [#432](https://github.com/nmorabowen/OpenSees/pull/432), [#435](https://github.com/nmorabowen/OpenSees/pull/435) |

## Documentation / ADR PRs + shipped-feature build history

The first bullets shaped the design but shipped docs only — recorded for
traceability. The later bullets (DispBeamColumn / CohesiveHinge /
CohesiveHingeBiaxial / RigidBody) are the **detailed build logs** of features
that DID ship source; each now also has a summary row in the feature table
above, so the table stays the authoritative shipped-feature index.

- [#4](https://github.com/nmorabowen/OpenSees/pull/4) — ADR: explicit Noh–Bathe integrators + energy-balance recorder
- [#11](https://github.com/nmorabowen/OpenSees/pull/11) — ADR: reframe energy-recorder future work (apeGmsh is our viewer)
- [#13](https://github.com/nmorabowen/OpenSees/pull/13) — ADR: apeGmsh-native energy result type into Ladruno (D8)
- [#95](https://github.com/nmorabowen/OpenSees/pull/95) — ADR: uniaxial combined-hardening J2 (`LadrunoUniaxialJ2`) — oracle + ratcheting fiber + steel roadmap ([[13_ladruno_uniaxial_j2_adr]])
- [#203](https://github.com/nmorabowen/OpenSees/pull/203) — 2D continuum elements: basic-elements ADR ([[25_ladruno_plane_elements_adr]]) + frontier/contribution thesis ([[26_ladruno_plane_frontier_adr]]: gradient-damage T1, phase-field T2, VEM T3, SBFEM T4, S-FEM T5; 2D as the proving ground for lifting coupled-field localization to the brick). Docs only, no `SRC/` change. **Tags renumbered after merge** (a sibling PR took the ADR-reserved 33006 for LadrunoEmbeddedNode): `LadrunoQuad`=33007, `LadrunoCST`=33008 (shipped P1, rows above); `LogStrain2D`=33016, VEM=33009, SBFEM=33010 still RESERVED
- [#203](https://github.com/nmorabowen/OpenSees/pull/203) — 2D continuum elements: basic-elements ADR ([[25_ladruno_plane_elements_adr]], `LadrunoQuad` 33007 / `LadrunoCST` 33008 / `LogStrain2D` 33016 — RESERVED, not yet built) + frontier/contribution thesis ([[26_ladruno_plane_frontier_adr]]: gradient-damage T1, phase-field T2, VEM T3 33009, SBFEM T4 33010, S-FEM T5; 2D as the proving ground for lifting coupled-field localization to the brick). Docs only, no `SRC/` change
- [#158](https://github.com/nmorabowen/OpenSees/pull/158) — RC-3D reinforcement: scoping/gap-map ([[19_rc3d_modeling_recipe]]), embedded-rebar + bond-slip ADR ([[20_ladruno_embedded_reinforcement_adr]], `LadrunoEmbeddedRebar` 33005 / `LadrunoBondSlip` 33002 — RESERVED, not yet built), + executed pre-build validation gates ([[21_rc3d_validation_gates]], `rc3d_gates/`): Gate 2 confinement PASS (ASDConcrete3D ≈Mander ±5%), Gate 1 conformal viable ≤8-bar. Adversarially reviewed (64-agent, proceed-with-changes). Docs + validation scripts only, no `SRC/` change
- **ADR scoping doc (now SHIPPED — see the `LadrunoDispBeamColumn2d/3d`, `LadrunoCohesiveHinge` + `LadrunoCohesiveHingeBiaxial` rows in the table above)** — Regularized displacement-based frame ADR ([[32_ladruno_dispbeamcolumn_regularization_adr]]): `LadrunoDispBeamColumn2d` 33013 / `LadrunoDispBeamColumn3d` 33014 (Element registry; numerically equal to `ND_TAG` 33013/33014 — not a collision) / `LadrunoCohesiveHinge` (uniaxial, MAT_TAG 33003). Two tiers: Tier-1 per-IP `lch` channel (mirrors `ForceBeamColumn`, fixes [[LEDGER_quirks]] §59), Tier-2 embedded strong-discontinuity hinge (Armero–Ehrlich). Large-disp via Corotational with the pinned 3-DOF-basic condensation contract. Scoped via 11-agent workflow (regularization/kinematics/cohesive/plumbing/validation × verify + synthesis). Docs only, no `SRC/` change
- **Stage 1 SHIPPED:** `LadrunoDispBeamColumn2d` 33013 + `LadrunoDispBeamColumn3d` 33014 (Tier-1 per-IP `lch` channel + Corotational large-disp + `-nl` ½θ²/½(θy²+θz²) bowing), `SRC/element/ladrunoDispBeamColumn/`, `tests/test_ladrunoDispBeamColumn{2,3}d_element.py` (28/28). PRs [#251](https://github.com/nmorabowen/OpenSees/pull/251) [#254](https://github.com/nmorabowen/OpenSees/pull/254) [#255](https://github.com/nmorabowen/OpenSees/pull/255) [#258](https://github.com/nmorabowen/OpenSees/pull/258) [#260](https://github.com/nmorabowen/OpenSees/pull/260)
- **Stage 2 (cohesive material SHIPPED [#264](https://github.com/nmorabowen/OpenSees/pull/264)):** `LadrunoCohesiveHinge` `MAT_TAG 33003` (uniaxial) **BUILT** — discrete cohesive moment–rotation law `M([[θ]])` carrying `Gf` per hinge (rigid-softening penalty w/ guarded floor `Mc²/2Gf`; exp/linear envelope calibrated to `∫M d[[θ]]==Gf`; irreversible secant damage; `getEnergy()`). `SRC/material/uniaxial/LadrunoCohesiveHinge.{h,cpp}`, `tests/test_ladrunoCohesiveHinge_material.py` (10/10, energy gate exact 1e-9 LINEAR).
- **Stage 2 PR-2a (element-side embedded hinge, 2D) SHIPPED:** `LadrunoDispBeamColumn2d` `-hinge $matTag` (reuses `ELE_TAG 33013`) — single scalar rotation jump `α` carried by any UniaxialMaterial, Armero–Ehrlich strong-discontinuity split (bulk sees `κ_bulk=B·v−α/L` → unloads, no double-count; cohesive `M([[θ]])` carries `Gf`), `α` inner-Newton + **guarded** static condensation to the 3-DOF basic system **before** `crdTransf` (pinned invariant). Gated (`hingeOn`) → no-hinge path bit-identical; `-hinge`+`-nl` rejected. `tests/test_ladrunoDispBeamColumn2d_hinge.py` (8/8): patch test 1e-9, energy gate `∫M d[[θ]]==Gf` (LINEAR ~4e-7), element total-dissipation==Gf (no double-count), tangent-consistency via tight Newton through softening, commit/revert + DB roundtrip. ADR 32 carries a 4-agent adversarial review that corrected the plan (E–B not Timoshenko; no section "freeze"; `K_αα` indefinite not zero-at-peak; Dirac out of quadrature).
- **Stage 2 PR-2b (2D objectivity/invariance/robustness gates) SHIPPED (test-only):** the deferred Stage-2 validation gates, verified by probing first (the PR-2a hinge already passes them). `tests/test_ladrunoDispBeamColumn2d_hinge.py` (8→15): corotational large-rotation `Gf`-dissipation at 74° tip rotation (pinned invariant under finite rotation), orientation invariance (0° vs 90° → identical M–θ to round-off), integration-objectivity nIP sweep (Lobatto 2..6 → invariant, no residual nIP drift), solver robustness (Newton/ModifiedNewton/NewtonLineSearch/KrylovNewton all dissipate `Gf`). The non-Newton "stale-α" hole does NOT bite (residual always post-`update()`), so that hardening is unneeded. **2D Stage-2 is gate-complete** except the `ladruno_drive` collapse test (blocked on RESERVED dissipation arc-length) and `-nl`+hinge.
- **Stage 2 PR-3a (3D strong-axis embedded hinge) SHIPPED ([[33_ladruno_dispbeamcolumn3d_hinge_adr]]):** `LadrunoDispBeamColumn3d` `-hinge $matTag` (reuses `ELE_TAG 33014`) — the strong-axis (Mz) rotation jump `α_z`, the literal 2D scalar algebra on the Mz row of the 6-DOF 3D basic system. One guarded rank-1 condensation to the basic system **before** `CorotCrdTransf3d` (pinned invariant through the quaternion triad); `hingeKvZ` a 6-vector incl. cross-tangent rows. Gated → no-hinge bit-identical; `-hinge`+`-nl` rejected. `tests/test_ladrunoDispBeamColumn3d_hinge.py` (12/12): patch test 1e-9, `∫Mz d[[θz]]==Gf` (19.999992), total-dissipation==Gf, **finite-rotation invariance under `CorotCrdTransf3d`** (>0.5 rad member rotation still dissipates Gf), solver robustness, nIP-objectivity, DB roundtrip. Full 65/65 (2D+3D). ADR 33 scoped + adversarially reviewed by a 17-agent workflow (killed the two-rank-1 / 2D-twice / det-floor shortcuts). **PR-3b** = weak-axis `α_y` + true coupled 2×2 `K_αα` (eigenvalue-floored inverse). Reserve `MAT_TAG_LadrunoCohesiveHingeBiaxial = 33004` (coupled biaxial cohesive, v2, not yet built).
- **Stage 2 PR-3b (3D biaxial coupled embedded hinge) SHIPPED ([[33_ladruno_dispbeamcolumn3d_hinge_adr]]):** `LadrunoDispBeamColumn3d` `-hingeY $matTag` (reuses `ELE_TAG 33014`; requires `-hinge`) — the weak-axis (My) jump `α_y` making the hinge biaxial. `solveHingeJumpBiaxial()` runs ONE unified inner Newton on `α=[α_z,α_y]` (a single `setTrialSectionDeformation`/IP sets both bulk curvatures); the TRUE coupled 2×2 `K_αα` carries the off-diagonal `(1/L)Σwt·½(ks(MZ,MY)+ks(MY,MZ))`, inverted by an **eigenvalue-floored** symmetric inverse (`ladrunoFlooredInv2x2` — bounds every mode, sign per eigenvalue preserved; NOT two rank-1, NOT det-floored adjugate). Rank-2 condensation `kb −= K_vα·K_αα⁻¹·K_vαᵀ` (6×2 `K_vα`) before `CorotCrdTransf3d`. Gated → `-hinge` alone bit-identical to PR-3a; `-hingeY`+`-nl` rejected. Serialization grows `data` 21→23 + the hinge metadata is ONE combined ID (size 2/4) to dodge an `FE_Datastore` key collision. `tests/test_ladrunoDispBeamColumn3d_hinge_biaxial.py` (14/14): reduce-to-PR-3a, independent-axis dissipation, constant biaxial patch (1e-8), energy partition `Gf_z`+`Gf_y`, **coupled-2×2 gate** (product-of-inertia section, staggered softening, tight Newton converges), finite-rotation invariance (>1 rad, Corot), solver robustness, nIP-objectivity, DB roundtrip both-open. Full 79/79 (2D+3D). **PR-3c+** = coupled biaxial cohesive (`33004`, v2), torsion `α_t`, `-nl`+hinge.
- **ADR 34 PR-4a (coupled biaxial cohesive material) SHIPPED ([[34_ladruno_cohesive_hinge_biaxial_adr]]):** `LadrunoCohesiveHingeBiaxial` `ND_TAG 33004` (NDMaterial **order 2**) — a single coupled cohesive law with an Mz–My **interaction surface** (ellipse): strain = jump vector `[α_z,α_y]`, stress = moment vector `[M_z,M_y]`, tangent = 2×2. Elliptical onset `√((Mz/Mcz)²+(My/Mcy)²)=1` + isotropic secant damage on the normalized norm `r`, per-axis `Gf`, mixed-mode (linear mode-mix) — **reduces EXACTLY to the scalar `LadrunoCohesiveHinge` on each pure axis**. Registered at the 4 nD sites. Element: `LadrunoDispBeamColumn3d -hingeBiaxial $ndMatTag` (`NDMaterial *theHingeC`, exclusive with `-hinge`/`-hingeY`/`-nl`) REUSES the PR-3b biaxial inner Newton — branches the cohesive eval to feed it the full 2×2 cohesive tangent (off-diagonal `Czy` enters `K_αα`); same eigenvalue-floored inverse + rank-2 condensation. Added **adaptive inner-Newton step damping** (no-op when the residual decreases → block-diagonal path unchanged) for the sign-discontinuous elliptical-onset/frozen-mix tangent. Serialization `data` 23→26 (nD classTag/dbTag folded into data to dodge an FE_Datastore ID collision; sent via `getNewNDMaterial`). `tests/test_ladrunoDispBeamColumn3d_hinge_coupled.py` (9/9): on-axis reduction == scalar element (1e-6), elliptical onset, mixed-mode dissipation `Gf_y..Gf_z`, coupled-section tight-Newton, finite-rotation invariance, DB roundtrip. Full **88/88** (2D+3D). **NEXT:** B-K/power-law mode-mix, consistent off-radial tangent, torsion.
- **ADR 34 PR-4b (B-K mode-mix + consistent off-radial tangent) SHIPPED ([[34_ladruno_cohesive_hinge_biaxial_adr]]):** closes handoff items #1+#2 (inseparable). KEY FINDING: the v1 "frozen-mix" tangent was **already exact** off-radial — the `Kpen_i ∝ Mc_i²/Gf_i` calibration makes `S ∝ Gf_lin` so `A=S/Esoft=2/(ratio−1)` is mix-INDEPENDENT and `D=D(r)` alone (the `∂mode-mix/∂α` term is identically zero at the linear mix; see [[LEDGER_quirks]]). To make it live: **Benzeggagh-Kenane** fracture-energy mix `Gf_mix = Gf_z + (Gf_y−Gf_z) w_y^η` via `-bk η` on `LadrunoCohesiveHingeBiaxial` (`ND_TAG 33004`, no new tag; default `η=1` = linear, bit-identical → the 88 stay green). `S`/onset stay linear → exact pure-axis reduction preserved. `setTrialStrain` adds the consistent `dD/dw·dw/dα` tangent term (guarded `η≠1` + both axes active); `effectiveLaw` uses the B-K `Gf_mix`; new raw `tangent` material response (id 207); serialization `data` 13→14 (`bkEta`). `tests/test_ladrunoCohesiveHingeBiaxial_material.py` (46, NEW) — `zeroLengthND` material point, the capstone FD-tangent gate (ratio=10, η∈{0.5,1,2}) pins C++ tangent == central-FD of `M(α)` (~1e-7) AND that the frozen-mix tangent deviates for η≠1; + 2 element B-K tests. Full **136/136**. Caveat: the term is significant only for soft penalties (~1e-2 at ratio=10; ~1e-4 at the near-rigid default 1000). **NEXT:** inner-Newton onset line search (the real robustness lever), torsion `α_t`, `-nl`+hinge.
- **Modal-analysis gap family (4 feature ADRs + program ADR 45 + cross-code theory study) — [#351](https://github.com/nmorabowen/OpenSees/pull/351):** bridges OpenSees's modal-analysis weaknesses against Abaqus / Kratos / LS-DYNA. Deep-theory dossiers in [[modal_gap_study/00_SYNTHESIS]] (+ `01_opensees_current_state` ground-truth audit, `02_abaqus_theory`, `03_kratos_source`, `04_lsdyna_theory`). **Reserved tags (RESERVED, not yet built):** `LadrunoComplexEigen` **33019** — complex / state-space modal for non-classically-damped systems (modal-projection QZ), [[46_ladruno_complex_modal_adr]]; `LadrunoBuckle` **33021** — prestressed modal + linear buckling (Jia–Mang two-tangent `K_ref φ=λΔK φ`), [[42_ladruno_buckling_adr]] (**33020 deliberately skipped — already reserved for `LadrunoSolidShell`**, [[19_ladruno_rc_shell_adr]]); `FeastEigenSOE` **33022** + `FeastEigenSolver` **33023** — band-targeted parallel eigensolver (FEAST/PFEAST contour integration over `MumpsParallelSOE`; unifies the SP/MP split + Sturm certification), [[43_ladruno_feast_eigensolver_adr]]; `LadrunoModalResponse` **33024** — frequency-domain FRF/SSD/random + exact modal-superposition transient, [[44_ladruno_frequency_domain_adr]]. Program/roadmap umbrella [[45_ladruno_modal_family_roadmap_adr]] (**ADR 45** — sequencing P-A…P-G, cross-cutting decisions, program gates; no tag). Candidate **ADR 47** = ROM / Craig–Bampton substructuring (no tag yet, pending decision). Load-bearing verdict (synthesis §6): infrastructure = ADR 43 (eigensolver + the general SP/MP parallel-composition fix) + ADR 46 (the non-classically-damped research domain); ADR 42/44 opportunistic. Docs only, no `SRC/` change.
- **ADR 58 P1 (RigidBody element, translation/ballistic) BUILT** ([[58_ladruno_rigid_body_adr]]): `LadrunoRigidBody` **ELE_TAG 33015** (next free ELE slot after 33014; per-registry band) — a 6-DOF rigid-body Element (LS-DYNA `*CONSTRAINED_NODAL_RIGID_BODY` / Abaqus `*RIGID BODY`). **Realized as a zero-stiffness Element** (`K=M=P=C=0`) owning a **private internal 6-DOF CoM `Node`** (Joint3D pattern) that carries the **condensed** mass (m_body diagonal + body-frame inertia tensor), with **rigid-link `MP_Constraint`s** tying each slave (`u_i=u_R+(R−I)d_i`; rectangular 3×6 for 3-DOF solid slaves, square 6×6 for 6-DOF). **This is ADR-58's D1 *fallback* (D1b): the DomainComponent route tripped the >15-file kill-criterion AND `Domain::commit()/revertToLastCommit()` iterate Nodes+Elements only — a DomainComponent gets no per-step callback, an Element inherits commit/revert/update for free.** P1 = translation/ballistic (R=I); SO(3) self-integrates in `commitState` at P2. `command: element LadrunoRigidBody tag N s1..sN [-mass m] [-internalNode tag]`. Files `SRC/element/ladrunoRigidBody/{LadrunoRigidBody.{cpp,h},OPS_LadrunoRigidBody.cpp,CMakeLists.txt}` + classTags + element dispatch (Tcl/Py) + broker (5 wiring edits); test `Ladruno_scripts/rigidbody_tests/test_p1_ballistic.py` (**P1 gate PASS:** ballistic free-fall rel-err 5e-4, exact node-follower 0.0; rectangular + square Ccr both verified). Scoped + strengthened by a 6-lens adversarial review (v2 — D3 diagonal-mass-split blocker, D1 in-tree precedents Joint3D/Subdomain/Pressure_Constraint, D2 in-tree GroupSO3/Versor reuse); implementation adversarially reviewed pre-build (cleared Ccr signs + mass double-count) — the runtime `setDomain` valid-flag control-flow bug surfaced only on the actual run, as that review predicted. **Status: wip — no banner line until the rocking MVP lands.** P1 (translation/ballistic) MERGED [#426](https://github.com/nmorabowen/OpenSees/pull/426) (+ G9 manifest row [#427](https://github.com/nmorabowen/OpenSees/pull/427)). **P2 Stage 1 (free-spin SO(3) rotation):** body-frame momentum-conserving integrator in `commitState` (carry spatial `L`; 2nd-order midpoint exp-map `q←q·exp(ω_mid·dt)` reusing `Versor`/`GroupSO3`; rotation integrated OFF the global solve per D3), D9 recorder (`orientation`/`omega`/`omegaBody`/`angularMom`), `-omega` IC; test `test_p2_dzhanibekov.py` (**gate PASS:** `‖L‖` exact, energy drift 7.6e-7 2nd-order, 7 Dzhanibekov flips); focused adversarial review (revert-clock B1, time-jump `started` B3, anisotropic-mass N4 folded in). Applied moments + slave-following deferred to P2 Stage 2 (the §6 architectural design + Housner gate). **P2 Stage 2 (finite-rotation slave-following + moment gather + Housner rocking) BUILT:** the crux §6 tension (MP constraints are linear/homogeneous; the side-channel `q` is not a retained DOF) resolved by **mechanism C3 — the element imposes each slave's exact trial displacement** `u_i=u_R+(R−I)d_i⁰` in `update()` (runs after the Transformation handler's incremental `slave += T·δu_retained` and BEFORE residual formation, so the element wins the last write — VERIFIED by a probe: slaves track `(R−I)d⁰` to 5e-16, one-step explicit half-step lag only). **This contradicts the design pass's pessimistic crux finding** (which was about `setTrialDisp` inside `getConstraint()`, a different/overwritten path; the time-varying-MP `LadrunoMP_RigidOffset.h` skeleton it spawned was discarded — C3 needs no custom MP). **Moment gather** `gatherCoMTorque()=Σ(R·d_i⁰)×f_i` with the CURRENT spatial lever arm (a frozen `d⁰` keeps a constant restoring moment and destroys the rocking equilibrium at θ=α); `f_i=−P` read from external elements incident on the slaves, **cached by TAG at setDomain** (re-resolved via `Domain::getElement` each gather so element removal is skipped not deref'd; re-built on every setDomain; reads `getResistingForce()` directly to avoid the reentrant shared `SingleDomEleIter` that `Domain::commit` already walks). `commitState` reordered: gather at the pre-advance orientation (matches the force's config) → midpoint exp-map advance → `L+=m·dt`. **v1 preconditions:** incident toe/contact elements must be defined BEFORE the body and be displacement-only (slave vel/accel not re-imposed under rotation). Tests `test_p2_slavetrack.py` (slaving 5e-16), `test_p2_gather.py` (torsional-oscillator sign/drive), `test_p2_housner.py` (**HOUSNER GATE PASS:** free-rocking quarter-period 1.22% vs analytic `(1/p)cosh⁻¹(1/(1−θ0/α))` on a stiff-pinned single toe — penalty/dt convergence verified, `k·dt` bias →0 as dt→0; impact restitution intentionally NOT gated — not reliably reproducible with a penalty toe). No-regression: ballistic + Dzhanibekov unchanged (`nIncident==0` ⇒ gather≡0). Focused adversarial review (reentrancy/sign/force-layout verified correct; H1 dangling-pointer-under-removal → tag-resolve fix folded in). **P2-S2 follow-ups SHIPPED:** Zone-A pytest `tests/test_ladrunoRigidBody_element.py` (6 cases: ballistic/Dzhanibekov/slave-tracking/slave-velocity/gather-sign/Housner; manifest flipped `planned→shipped`, warn 4→3) + splash-banner line, [#435](https://github.com/nmorabowen/OpenSees/pull/435); **M2 consistent slave velocity** `v_i=v_R+ω×(R·d_i⁰)` re-imposed in `imposeSlaveKinematics` so velocity-dependent incident elements (dashpot/viscous contact) + slave vel recorders are coherent (test_slave_velocity to machine precision) — only slave ACCEL remains un-imposed. P2-S2 MERGED [#432](https://github.com/nmorabowen/OpenSees/pull/432) (+ quirks [#433](https://github.com/nmorabowen/OpenSees/pull/433)). **Status: SHIPPED** (manifest `shipped` + banner); remaining v1 limits = serial-only `sendSelf/recvSelf`, explicit-only, accel-dependent incident elements; parallel + implicit tangent deferred to follow-up ADRs.
